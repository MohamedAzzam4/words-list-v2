# AUDIO-002 / Attempt 01 — Evidence Index

All evidence below was produced in the external sandbox (`/home/z/my-project/words-list-v2`, Debian, Node v24.19.0, npm 11.17.0, Playwright 1.61.1 lockfile, Python 3.12.14 for the static server on port 9012). Sandbox clock: 2026-09-01 ~00:30–02:30. Every log is the sanitized text output of the exact command listed (sanitization: CR removal, ANSI escape strip, `[WebServer]` request-log line removal, trailing-whitespace scrub; disclosed exceptions: `01-red-unit.log` and `01-red-unit-b11-supplement.log` also had node:test assertion stack-frame lines (leading-whitespace `at …`) removed during capture to keep the RED logs readable; failure messages, counts, and titles are unaltered). No credentials or user data appear in any file — every published copy was scanned for credential patterns: zero hits.

Tested revisions: accepted base `4bd13a94df52755c27bb352ef5b87d1198a4fe21` (AUDIO-001-C2 delivery), tests commit `d5e5949` (RED-first tests; production source still the unmodified base at RED capture time), final tested code `e32352c` (implementation: planner adapter in `js/core/verbs-engine.js` + SpeechQueue ownership hardening in `js/core/tts.js`), and tests supplement `300a9c2` (adds the B11 empty-list characterization after mapping review of verification item 22; production source unchanged). The RED, GREEN, regression, and probe evidence below was captured at these revisions; the E2E logs (05–09) are valid for the delivered state because the Playwright `testDir` is `tests/e2e` — the unit-test supplement cannot be loaded by any browser run — and the implementation revision is identical; the unit-level evidence (04, 08, 11, 01-supplement) was re-captured at `300a9c2` after the supplement. The docs commit that publishes this directory does not move the tested code revision.

Environment setup event (disclosed): the sandbox lacked the Playwright 1.61 Chromium build (`chromium_headless_shell-1228`); the first verbs baseline attempt failed with `browserType.launch: Executable doesn't exist` (5/5 failed, no test logic executed). `npx playwright install chromium` (download only; system dependencies were already validated in this sandbox from earlier builds) fixed the environment; every subsequent run used the installed build. No browser test result was fabricated from the failed attempt.

## Checkpoint gate evidence (pre-edit)

- `git fetch origin` exit 0; accepted base `4bd13a9…` verified present; remote `codex/glm-audio-002-01` verified absent (0 matching heads, 17 heads total); working tree clean on `codex/glm-audio-001-c2-01` at exactly `4bd13a9…`.
- Branch `codex/glm-audio-002-01` created exactly from `4bd13a9…`; unchanged initial checkpoint pushed non-forced via a transient chmod-600 credential file outside the repository (written by the executor, never printed/logged/committed/URL-embedded, deleted immediately after the push and verified absent). Remote checkpoint SHA verified `4bd13a94df52755c27bb352ef5b87d1198a4fe21` == local HEAD.

## RED evidence (production source = the unmodified base)

| File | Command | Outcome |
|---|---|---|
| `01-red-unit.log` | `node --test tests/unit/verbs-audio.test.mjs` (at tests-commit `d5e5949` content) | **17 passed / 5 failed / 22 total, exit 1** — the failing-acceptance evidence for the genuine defects (TS-PRE-003 / CM-FIX-001): A9 out-of-range start leaves an empty queue, A11 missing-text steps are queued instead of planner-skipped, A12 an empty planned sequence puts the controls into a playing state with no queue, B8 the untracked no-synthesis fallback fires after pause/stop (advances the cursor and fires completion early), B10 a replaced queue's untracked fallback advances the replacement. The 17 passing tests are the characterization set the planner adoption must preserve exactly (CM-REF-002/005). |
| `01-red-e2e.log` | `npx playwright test tests/e2e/verbs-audio.spec.js --project=chromium --reporter=line` | **8 passed / 0 failed, exit 0** — the new E2E characterization suite passes against the UNMODIFIED base controller through the real page with deterministic speech mocks: the refactor's behavior-preservation baseline (re-run green after the implementation in `05-…`). |
| `01-red-unit-b11-supplement.log` | `node --test tests/unit/verbs-audio.test.mjs` (disposable worktree at `d5e5949` with the final 23-test file copied in; base production files) | **18 passed / 5 failed / 23 total, exit 1** — the same five genuine defects; B11 (empty-`playAll` cleanup, verification item 22) passes at base, so it is characterization, not a defect test. |

## Final evidence (source `e32352c`; unit evidence at tests revision `300a9c2`)

| File | Command | Exit | Counts / duration | sha256 |
|---|---|---|---|---|
| `02-syntax-production.log` | `node --check js/core/speech-plan.mjs`; `node --input-type=module --check < js/core/tts.js`; `node --input-type=module --check < js/core/verbs-engine.js` | 0 | parses cleanly ×3 (TS-LOOP-001) | `63317c5b…` |
| `03-syntax-tests.log` | `node --check tests/unit/verbs-audio.test.mjs`; `node --input-type=module --check < tests/e2e/verbs-audio.spec.js` | 0 | parses cleanly ×2 | `4d0d8f28…` |
| `04-green-focused-unit.log` | `node --test tests/unit/verbs-audio.test.mjs` | 0 | **23 passed / 0 failed / 0 skipped** (A1–A12 adapter, B1–B11 queue ownership incl. the empty-list cleanup) | `06971fcc…` |
| `05-green-focused-e2e-chromium.log` | `npx playwright test tests/e2e/verbs-audio.spec.js --project=chromium` | 0 | **8 passed** (12.5s) — the same E2E suite that was green at base, still green on the adapter | `de1bbfe3…` |
| `06-affected-chromium.log` | `npx playwright test tests/e2e/verbs.spec.js tests/e2e/phrases-conversations.spec.js tests/e2e/words-audio.spec.js --project=chromium` | 1 | **16 passed / 1 failed / 1 skipped** (37.1s) — the only failure is the pre-existing `words-audio.spec.js:48` words-page highlight-clear defect (fails identically at the base; out of AUDIO-002 scope; F-A002-1) | `1f4fef8c…` |
| `07-affected-mobile-chrome.log` | `npx playwright test tests/e2e/verbs-audio.spec.js tests/e2e/verbs.spec.js tests/e2e/phrases-conversations.spec.js tests/e2e/words-audio.spec.js --project="Mobile Chrome"` | 1 | **25 passed / 1 failed** (54.4s) — the new 8 audio tests pass on the mobile project; the only failure is the same pre-existing words-audio defect | `cbf3371b…` |
| `08-full-units.log` | `npm run test:units` | 0 | **138 passed / 0 failed / 0 skipped** — 115 pre-existing + 23 new | `2b1580a7…` |
| `09-full-e2e-both-projects.log` | `npx playwright test` (both projects, whole tracked suite) | 1 | **303 passed / 2 failed / 1 skipped** (8.6m) — the only failures are the pre-existing `words-audio.spec.js:48` on both projects (identical at base and documented since SHARED-CARD-002/003); final broad rerun after the flake investigation below | `fe9dc81e…` |
| `09-full-e2e-first-run-flakes.log` | `npx playwright test` (first capture run) | 1 | 300 passed / 5 failed / 1 skipped (9.3m) — the two pre-existing words-audio failures plus three load-dependent flakes: verb-guided R2 (chromium), GC-07 (Mobile Chrome), and verbs.spec.js:33 (Mobile Chrome; the OLD spec has no dataset-readiness wait and races `window.verbsEngine.queue` against the 2 MB verbs JSON; the empty-queue early-return it hits is unchanged base code) | `5f51af52…` |
| `09-flake-isolation-reruns.log` | single-test isolation reruns of the three first-run flakes | 0 | **3/3 pass in isolation** (2.1s / 3.3s / 2.7s) — TS-LOOP-005 failure protocol: isolate, rerun the affected case, then one broad rerun (the `09-full-e2e-both-projects.log` above) | `ddbefc7e…` |
| `10-diff-check.log` | `git diff --check`; `git diff --cached --check`; `git status --short` at `300a9c2` | 0 | clean (no whitespace errors; clean tree; the docs-only commit that follows adds report/evidence only — the same self-reference constraint disclosed in AUDIO-001-C2 applies: the final-HEAD rerun is reported in the handoff/worklog) | `dc4e43eb…` |

## Fault-probe evidence (disposable worktree at `300a9c2`, tests unmodified; script `/home/z/my-project/scripts/audio002-probes.py`, discarded after use)

| File | Probe | Mutation (production code only, worktree copy) | Detecting tests + actual failure | Classification | Integrity proof |
|---|---|---|---|---|---|
| `11-fault-probes.log` | 1. language-mislabel | `js/core/verbs-engine.js`: the adapter maps `lang: step.language` to the opposite language (`de`↔`en`) | **11 tests fail** (A1–A11): every sequence table and the A10 identity/language mapping assert the planner language per step — German text would be sent to the English voice and vice versa | **KILLED** | `verbs-engine.js` sha256 `d3f2aecc…` re-verified identical after restore |
| (same file) | 2. stopped-delayed-speech-executes | `js/core/tts.js`: `stop()` drops the `_speakDelayTimer` clearTimeout block AND the delayed-speak callback drops its ownership guard — a stopped queue's pending delayed speech executes | **4 tests fail**: B2 (stop during the delay still speaks), B3, B9, B10 — the leak also surfaces as an orphaned old-queue timer overwriting the replacement's timer handle | **KILLED** | `tts.js` sha256 `8ca70a9e…` re-verified identical after restore |
| (same file) | 3. cursor-advances-during-pause | `js/core/tts.js`: `pause()` drops its ownership reset (`_generation++` and `currentUtterance = null`) | **1 test fails**: B3 — the paused utterance's late `onend` advances the cursor, clears the queue, and fires completion early | **KILLED** | sha256 re-verified identical after restore |
| (same file) | 4. repeat-adapter-bypass | `js/core/verbs-engine.js`: the planner call hardcodes `repeatCount: 1`, bypassing the repeat control | **2 tests fail**: A1 and A2 — the repeat blocks per verb are missing | **KILLED** | sha256 re-verified identical after restore |
| (same file) | 5. stale-callback-affects-replacement | `js/core/tts.js`: `utterance.onend` drops both ownership guards (generation + utterance identity) and always advances | **3 tests fail**: B10 (a stale `onend` from a replaced queue advances the replacement) and B3 are the intended detectors; B11 also fails as documented collateral — when B10 aborts mid-test it leaves pending mocked timers in the shared process, poisoning the next test's mock-timer context (a node:test cross-test effect that only occurs after a test has already failed; failures cascade, they never mask). B11 passes in isolation under this mutant (verified `--test-name-pattern B11`: 1/1) | **KILLED** | sha256 re-verified identical after restore |

Probe discipline: the worktree's unmodified baseline passed 23/23 before the first probe; every mutant passed `node --input-type=module --check` before tests ran (behavioral mutants, not syntax crashes); the main working tree was never mutated; the worktree was removed after the probes (verified). One corrected probe attempt is documented transparently inside the log per TS-MUT-005: probe 2's first mutation (removing only the delayed-speak guard) SURVIVED as an equivalent mutant because `stop()` still clears the timer — the guard is mandated defense-in-depth; the corrected behavioral mutation (both cancellation layers removed) is the actual defect family and is KILLED. Probe-5's B11 collateral was investigated in disposable worktrees (instrumented diagnostic runs, all discarded) before being classified as cross-test contamination rather than an independent detection.

## Hygiene

- `git diff --check` and `git diff --cached --check` at the tests commit, the implementation commit, the tests-supplement commit, and the docs commit: clean.
- Complete diff vs accepted base `4bd13a9…`: only `js/core/tts.js`, `js/core/verbs-engine.js`, `tests/unit/verbs-audio.test.mjs`, `tests/e2e/verbs-audio.spec.js`, `docs/cefr/reports/AUDIO-002-01.md`, and `docs/cefr/evidence/AUDIO-002/01/**` (this directory). No level configuration, content, contracts, dependencies, lockfile, Playwright config, CI, storage, or unrelated test file changed. `js/core/speech-plan.mjs` and `tests/unit/speech-plan.test.mjs` are byte-identical to the accepted AUDIO-001-C1 revision.
- Every published log was scanned for credential patterns before publication: zero hits. The transient Git credential used for the pushes never entered the repository, logs, reports, or URLs, and was deleted immediately after each Git operation (verified absent).
- Redactions/missing evidence: none. `02-syntax-production.log` and `03-syntax-tests.log` are minimal because `node --check` prints nothing on success; exit codes are recorded inside the logs.
