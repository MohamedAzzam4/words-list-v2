# REVIEW-002 / attempt 01 — Evidence Index

- Tested base commit: `7481c74d848f49274e10dc8017d1a44ad8664b77` (the owner-accepted REVIEW-001-C1 delivery).
- Tested code commit (final material revision): `fa14a982f83228f5a26b54578997c1f20e852c85` (implementation; the RED capture was produced at the tests-only commit `c43787644e6cb683c01e4ab60eb8bd3e79d0f439` with production byte-identical to the base, proof in evidence 02).
- Environment: Debian Linux container, Node v24.19.0, npm 11.17.0, git 2.47.3. No browser binaries used (the package creates a pure module with no browser integration; the assignment's ladder explicitly omits Playwright for REVIEW-002 — REVIEW-003 owns browser integration).
- All logs are raw captured output (stdout+stderr or redirected command output). Sanitization check (scripts/review002-sanitize.py): zero carriage returns, zero ANSI escape sequences, zero trailing-whitespace lines, zero credential patterns (`github_pat_`, `ghp_`, `gho_`, `ghs_`, `ghu_`, `GITHUB_PAT`, `Authorization: Bearer`) in every file; the published copies are byte-identical to the raw captures; no redactions were necessary.
- Hashes are SHA-256 of the published files. They identify file content, not proof of execution; the reviewer can reproduce every command.

| File | Command (working directory `/home/z/my-project/words-list-v2` unless noted) | Outcome | Revision tested | Duration | SHA-256 |
|---|---|---|---|---|---|
| `00-baseline-full-units.log` | `node --test "tests/unit/**/*.test.mjs"` in a disposable git worktree at the base commit | Pre-edit baseline: exit 0; 184 tests / 184 pass / 0 fail / 0 skipped (the full suite before REVIEW-002, matching the REVIEW-001-C1 delivery) | `7481c74` (base, pre-edit) | ~1 s | `7b29fe7a312463ff786c3b851751a2ff74aacc64727ee1b8bacc292edef48af5` |
| `01-red-session-plan.log` | `node --test tests/unit/review-session-plan.test.mjs` | RED: exit 1; ERR_MODULE_NOT_FOUND for `js/core/review-session-plan.mjs` — the intentional failure of all 21 cases against the missing production module | `c437876` (tests-only commit; production byte-identical to base) | ~46 ms | `a70d5e29ca71a16ce2553efd7f6d37aca79afee4ac20823a01c6029bdc35a07e` |
| `02-red-commit-identity.log` | `git diff 7481c74..HEAD -- js/` (0 lines); `git diff --name-status`; sha256 of every `js/core/*` file at RED commit vs base | RED-commit identity proof: zero production diff lines, only the test file added, all 23 production modules under `js/core/` sha256-identical to the base | `c437876` vs base `7481c74` | <1 s | `f666515ffd6760ae730f38419bfe2c7d8ef40dc5c06f1740e95aebc78f5fedde` |
| `03-ladder01-syntax-import.log` | `node --check js/core/review-session-plan.mjs`; `node --check tests/unit/review-session-plan.test.mjs`; live ESM `import()` asserting the single function export | All OK; exports = `["createReviewSessionPlan"]` | `fa14a98` | <1 s | `27b97c9ad31f4b3e9c9d249510ef2472c05a37e25508212a1c50a9a44a1647c6` |
| `04-ladder02-focused-units.log` | `node --test tests/unit/review-session-plan.test.mjs` | GREEN: exit 0; 21 tests / 21 pass / 0 fail / 0 skipped | `fa14a98` | ~100 ms | `5d667edaa404cf7b6d03ef134aaf40585496f31e30d43cafb009ac366309884b` |
| `05-ladder03-full-units.log` | `npm run test:units` (underlying command `node --test "tests/unit/**/*.test.mjs"`) | GREEN: exit 0; 205 tests / 205 pass / 0 fail / 0 skipped (184 baseline + 21 new) | `fa14a98` | ~1 s | `bd886519fb6c947b95935e1cb8b1cdfe41404c82d4b6871635e9b45021c2bb9b` |
| `06-mutation-probes.log` | `python3 /home/z/my-project/scripts/review002-probes.py` (disposable git worktree at `fa14a98`; baseline 21/21; three production-only mutations of `js/core/review-session-plan.mjs`; `node --check` gate; sha256-verified restores) | 3/3 KILLED with intended detectors: P1 oversize-balance (greedy filling, 51 -> 50+1; killed by case 2 exact sizes, collateral cases 3/4/8); P2 membership-boundary (chunk-boundary duplicate + tail drop; killed by case 2 membership, collateral cases 3/4/8); P3 determinism (identity order, seed ignored; killed by case 5 pinned permutations, collateral cases 7/8/9). Baseline 21/21 before every mutation; module restored and hash-verified after every probe; worktree removed | `fa14a98` | 3 mutated runs + baseline, each <1 s | `de9373c8d9733ede4da1cb527e4afd69b74f6e3f79cdf230daa739351748178c` |
| `07-ladder05-08-diffcheck-scope-purity.log` | `git diff --check`; `git status --porcelain`; `git diff --name-status`/`--stat` vs base; 15 forbidden-reference scans; import-statement scan; hidden clock/random scan; dead-code reference scan | `git diff --check` clean (exit 0); status empty; scope exactly 2 added files (`js/core/review-session-plan.mjs` +207, `tests/unit/review-session-plan.test.mjs` +571); zero forbidden references (Math.random, clock, crypto, DOM, persisted-state, network, controller tokens); zero import statements; the only repo references to the new module are its own file and its test | `fa14a98` | <1 s | `eade841b557f296906b91f66df5fbd89a683cac61521c8d226bd0b86abe4a367` |
| `08-setup-events.txt` | (narrative record) | Environment record, checkpoint-gate events (blocked placeholder-PAT attempt with 401; verified non-forced checkpoint push with the actual PAT), mandatory reading, baseline runs, pre-edit reference-vector verification, RED provenance, ladder rerun disclosure, sanitization note | n/a | n/a | `be968ff3e20129f45f8dc8a495141607fcd22c96b181191758d9a584920b66eb` |

## Missing evidence

None. Every executed command of the package is represented above, including the pre-edit baseline, the RED capture with its byte-identity proof, the full command ledger in the report (`docs/cefr/reports/REVIEW-002-01.md` section 5), and the blocked first checkpoint attempt (recorded in `08-setup-events.txt` and the prior session's worklog; its one-line HTTP 401 code output was not retained as a file and is quoted verbatim in the setup events).
