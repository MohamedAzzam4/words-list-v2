# REVIEW-001-C1 / attempt 01 — Evidence Index

- Tested base commit: `1e9d58060052758cda8546637b9689b8ed5867a2` (the delivered REVIEW-001 revision the owner rejected).
- Tested code commit (final material revision): `282ddad03cfc9e76380de8360d4bfcff7ab34a19` (implementation; the RED capture was produced at the tests-only commit `b5fcc6f08e4580fb9b18809a13b07b041d9b84e5` with production byte-identical to the base, proof in evidence 02).
- Environment: Debian Linux container, Node v24.19.0, npm 11.17.0, git 2.47.3. No browser binaries used (the module remains pure and unintegrated; no existing browser runtime file was modified — Playwright not applicable per the assignment's ladder).
- All logs are raw captured output (stdout+stderr or redirected command output), sanitized only for carriage returns, ANSI escapes, and trailing whitespace (log 01 had trailing whitespace on its summary lines; no content was removed; no redactions were necessary). Sanitization check: zero carriage returns, zero ANSI escape sequences, zero trailing-whitespace lines, zero credential patterns (`github_pat_`, `ghp_`, `gho_`, `ghs_`, `ghu_`, `GITHUB_PAT`).
- Hashes are SHA-256 of the published files. They identify file content, not proof of execution; the reviewer can reproduce every command.

| File | Command (working directory `/home/z/my-project/words-list-v2`) | Outcome | Revision tested | Duration | SHA-256 |
|---|---|---|---|---|---|
| `00-owner-repro-base.log` | `node /home/z/my-project/scripts/review001c1-repro.mjs` (defect reproduction) | All four owner findings CONFIRMED: Date progress does not throw; Date srsData produces no SRS_STATE_MALFORMED; class-instance container/record admitted as due with zero diagnostics; class-instance card participates | `1e9d580` (base, pre-edit) | <1 s | `52f37186c71a823ec7538fb3f76d794e479bfb17a2247631d43abcbfbe4164ff` |
| `01-red-strict-validation.log` | `node --test tests/unit/review-aggregation.test.mjs` | RED: exit 1; 25 tests / 20 pass / 5 fail — the 5 intentional RED failures are C1 cases A, B, C, D, E; cases F and G pass by design (overcorrection guards); the 18 original cases pass | `b5fcc6f` (tests-only commit; production byte-identical to base) | ~0.1 s | `ff14244e45e6716045f6eb96962107d666c01eed9352434dc8b7612ea94f7039` |
| `02-red-commit-identity.log` | `git rev-parse`/`git show … \| sha256sum`/`git diff --name-status` (RED-commit identity proof) | sha256 of `js/core/review-aggregation.mjs` at `b5fcc6f` equals the base's (`df122bb545ff5b23165ae8e6e6ba2931dbc824b8a92e5ea0c8e540b1317ea9b0`); zero-line diff under `js/core/`; only the test file changed at the RED commit | `b5fcc6f` vs base `1e9d580` | <1 s | `dbaac427f1e61d9d1251b09b878117ed8868fa69d5a0f13b2ee9137bb03d9b30` |
| `03-ladder01-syntax-import.log` | `node --check js/core/review-aggregation.mjs`; `node --check tests/unit/review-aggregation.test.mjs`; live ESM `import()` asserting the single function export | All OK; `aggregateCurrentLevelReview` is the only export | `282ddad` | <1 s | `18530374e138d3ad94e8dfd8d177bfc208b5f939df2d8fcfe5cb03c31f68b931` |
| `04-ladder02-focused-units.log` | `node --test tests/unit/review-aggregation.test.mjs` | GREEN: exit 0; 25 tests / 25 pass / 0 fail / 0 skipped (18 original + 7 new C1 cases) | `282ddad` | ~94 ms | `53e526c94f1035a1647aa0ad9978a1f07a2bdabc098af543da134f0dd8c7b282` |
| `05-ladder03-full-units.log` | `npm run test:units` (underlying command `node --test "tests/unit/**/*.test.mjs"`) | GREEN: exit 0; 184 tests / 184 pass / 0 fail / 0 skipped (177 from REVIEW-001 + 7 new) | `282ddad` | ~1 s | `1b754196299538ee90cf63ea876c1fcf4791f5a6cdba1187fbc6663d84ae5921` |
| `06-mutation-probes.log` | `python3 /home/z/my-project/scripts/review001c1-probes.py` (disposable git worktree at `282ddad`; baseline 25/25; three production-only mutations of `js/core/review-aggregation.mjs`; `node --check` gate; sha256-verified restores) | 3/3 KILLED with intended detectors: P1 permissive-predicate-restored (20 pass / 5 fail; killed by case A, collateral B–E); P2 same-realm-only (24 pass / 1 fail; killed by case G alone); P3 silent-malformed-srsdata (22 pass / 3 fail; killed by case B, collateral original case 8 and case C). Baseline 25/25 before mutations; module restored and hash-verified after every probe; worktree removed | `282ddad` | 3 mutated runs + baseline, each <1 s | `8e913fa12cd8affbdae9a308232d2b206016c43a0a86e4ec5002d122ef0d0085` |
| `07-ladder05-06-diffcheck-scope.log` | `git diff --check`; `git status --porcelain`; `git diff --stat 1e9d580..HEAD`; `git diff --name-status 1e9d580..HEAD`; RED-commit identity re-proof | `git diff --check` clean (exit 0); status empty; scope exactly 2 modified files (`js/core/review-aggregation.mjs` +23/−10, `tests/unit/review-aggregation.test.mjs` +204; 227 insertions, 10 deletions, no other file touched) | `282ddad` | <1 s | `5fc0bc02a2545ee4e306dc8d5dd847e23eae281b23c5aff20b627f1f9ba6ab8d` |
| `08-setup-events.txt` | (narrative record) | Environment record, checkpoint-gate events (initial AUTHENTICATION_BLOCKED single attempt; owner PAT resume; verified non-forced checkpoint push), owner-reproduction note, RED-capture provenance, mutation-probe log double-listing note, sanitization note | n/a | n/a | `f39a82f58008695be7067078c508d3a40e4cd1bdaabd14e60beeb131855e8f71` |

## Missing evidence

None. Every executed command of the package is represented above, including the defect reproduction, the RED capture with its byte-identity proof, the full command ledger in the report (`docs/cefr/reports/REVIEW-001-C1-01.md` section 5), and the initial failed checkpoint push (recorded in `08-setup-events.txt` and the prior session's worklog; the raw terminal output of that single failed push is one line, quoted verbatim in the setup events).
