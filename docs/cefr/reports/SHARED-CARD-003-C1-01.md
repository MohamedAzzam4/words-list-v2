# SHARED-CARD-003-C1 — Attempt 01 Delivery Report

## 1. Identity and status

- Status: READY_FOR_REVIEW
- Assigned goal and task type: correct the incomplete AC-09 touch-target and keyboard-focus coverage in `#view-flashcard` — the seven visible level-owned toolbar controls (Back to List, Shuffle, All Cards, Still Learning filter, Favourites filter, German face, English/Translation face) were 37–38 CSS px tall and painted no visible outline on keyboard focus, and the existing `SC3-A11Y-FOCUS` / `SC3-A11Y-TARGETS` tests omitted them. Task type: **BUGFIX** (restores the already-approved AC-09 behavior for the omitted controls) + **MODIFICATION** secondary (test coverage is extended in place; no superseded assertions). CM gates FIX + MOD applied: failing-target RED evidence captured before the production edit (§5), root cause identified (§3), smallest CSS-only correction, regression test that fails before and passes after (the corrected tests themselves).
- Owner assignment reference: owner task message "TASK: Execute SHARED-CARD-003-C1 — correct incomplete touch-target and keyboard-focus coverage" with base, permitted paths, the owner finding, the thirteen required-work items, the two mandated fault probes, and the delivery rules.
- GitHub task branch: `codex/glm-shared-card-003-c1-01`; base commit: `e3d3c22b0a6efa0865fa336006158b9dfa2bdce0` (accepted SHARED-CARD-003 revision); commits: tests `3f82255101cb72589619ab1e75690628e38c28a6` (RED coverage, committed first per DR-007), final tested code `222819ca48cd602a28d09fc7f82e205a28be4dde` (CSS correction). All final evidence in `docs/cefr/evidence/SHARED-CARD-003-C1/01/` ran against exactly this tree; the report/evidence commit is docs-only and does not move the tested code revision.
- Evidence index: `docs/cefr/evidence/SHARED-CARD-003-C1/01/INDEX.md`
- Final delivered SHA is returned after commit/push in the handoff message, not embedded into its own commit.
- Executor/model and sandbox OS/runtime versions: Super Z (GLM executor); Debian sandbox, Node v24.19.0, npm 11.17.0, Playwright 1.61.1 (lockfile; chromium + chromium-headless-shell v1228 installed once at session start after a sandbox reprovision had cleared the browser cache).
- Start/end time; work directory: 2026-08-30 ~23:50–2026-08-31 ~02:00 UTC (sandbox clock); `/home/z/my-project/words-list-v2` (repo-relative paths in this report).
- Dependency acceptance references: SHARED-CARD-003 attempt 01 (base of this correction) delivered and owner-reviewed; its AC-09 coverage gap is the finding corrected here.
- Language: English only, per the owner's mandatory language requirement.

## 2. Required reading and pre-edit plan

- Paths fully read before editing: `AGENTS.md`; `contracts/README.md`; `contracts/PORTABLE_AGENT_EXECUTION.md`; `contracts/CHANGE_MANAGEMENT.md`; `contracts/CODE_FINGERPRINT.md`; `contracts/TESTING_AND_SUCCESS.md`; `contracts/DEAD_CODE_AND_REFACTORING.md`; `contracts/DELIVERY_REPORTING.md`; `contracts/LEVEL_FLASHCARD_STANDARD.md` (LF-CARD visible-focus + 44×44 clauses); `docs/cefr/ACCEPTANCE_MATRIX.md` (AC-09 row: "Mobile without horizontal overflow; visible keyboard focus, 44x44 primary targets, reduced motion, appropriate themes"); `docs/cefr/reports/SHARED-CARD-003-01.md` (complete); the complete current implementations and tests: `level.html` (the `#view-flashcard` toolbar markup, the B2 face-label script, the `#fc-card-mount` slot), `css/core.css` (global `button { outline: none }`, `.btn`/`.controls-row`/`.btn-group` styles, the SHARED-CARD-003 AC-09 block, the mobile media query), `verbs.html` (`#view-flashcard` reuse — lines 1252–1274 — and its inline-styled toolbar), `js/core/verbs-engine.js` grade-button classes (read-only), and `tests/e2e/cefr-cards.spec.js` (complete — the `tabWalk`/`snapshotFocusStyles`/`visibleFocusIndicator` helpers, `SC3-A11Y-FOCUS`, `SC3-A11Y-TARGETS`, the B2 describe blocks, and all regression tests); `playwright.config.js`, `package.json`, `docs/cefr/templates/WORK_PACKAGE_REPORT.md`, `docs/cefr/evidence/SHARED-CARD-003/01/INDEX.md`. No conflicts found.
- Before behavior (characterized at `e3d3c22`, `baseline-probe.log`): all seven toolbar controls are `.btn` elements inside the two `#view-flashcard .controls-row` rows; every one measured **37.0 CSS px tall** (widths 94–143 px); all are reachable by real Tab presses and match `:focus-visible`, but **zero computed-style properties change** between the unfocused and keyboard-focused states — the global `button, select { outline: none }` rule (core.css lines 84–91) suppresses the browser's default ring, and the SHARED-CARD-003 AC-09 block covered only `.fc-btn` (grade buttons) and `.fc-nav .btn` (Prev/Next), omitting the toolbars. The pre-correction tests passed at base (`baseline-omission-chromium.log`, 2/2) — the omission the owner finding describes, reproduced as evidence.
- Approved after behavior: every visible level-owned control in `#view-flashcard` — the five card-view controls already covered plus the seven toolbar controls — is at least 44×44 CSS px and paints a visible focus indicator when reached by keyboard Tab navigation (matching `:focus-visible`), proven on Chromium and Mobile Chrome for both A1 and B2 pages; verbs.html, Phrases, Conversation, SRS, favorites, and storage routing behave exactly as at base.
- Explicit non-goals: no JavaScript or HTML change; no change to the global `button { outline: none }` contract rule (the correction is scoped under `#view-flashcard`, exactly as the owner instructed); no shared-card (`js/core/shared-card.js` / `css/shared-card.css`) change; no verbs.html change; no vocabulary, contract, dependency, snapshot, or acceptance-threshold change; no Review Center or autoplay work.
- Exact allowed write paths (all writes stayed inside them): `css/core.css`, `tests/e2e/cefr-cards.spec.js`, `docs/cefr/reports/SHARED-CARD-003-C1-01.md`, `docs/cefr/evidence/SHARED-CARD-003-C1/01/**`. `git diff --name-only e3d3c22..222819c` = exactly `css/core.css` + `tests/e2e/cefr-cards.spec.js`.
- Risk: LOW-MEDIUM (CSS-only presentation correction + test extension; no state, storage, or JS touched). Fault families per the owner: sizing-rule removal, focus-rule removal — both probes executed and KILLED (§7). A scope hazard identified during planning: **verbs.html reuses the `id="view-flashcard"` for its own different toolbar** (Back to List + Shuffle + two selects), so a bare `#view-flashcard .controls-row .btn` selector would have silently restyled the German Verbs reference page; the correction is therefore scoped as `html[data-level] #view-flashcard .controls-row .btn` (only level.html sets `data-level`, synchronously in `<head>`), and verbs.html is proven untouched (`postfix-probe.log` + `final-reference-chromium.log`).
- Affected boundaries: presentation CSS only, scoped to the CEFR level pages' flashcard view; no storage, ID, schema, scheduler, TTS, audio, or public API boundary is affected.
- Baseline tests and known failures: units 75/75 at base; the pre-correction a11y tests 2/2 at base (the omission proof); no new baseline failures introduced (the words-audio and R1 flake findings from SHARED-CARD-003 are outside this package's touched paths and were not re-measured — no JavaScript, HTML, or runner file changed here, so those pre-existing classifications remain the referenced evidence).

## 3. Changes and rationale

| File | Change/purpose | Contract/WP criterion | Compatibility impact |
|---|---|---|---|
| `css/core.css` | The SHARED-CARD-003 AC-09 block now also covers the two `.controls-row` toolbars: the `:focus-visible` outline rule gains `html[data-level] #view-flashcard .controls-row .btn:focus-visible` and the 44×44 minimum rule gains `html[data-level] #view-flashcard .controls-row .btn` (min-width/min-height 44px, border-box, matching the existing `.fc-nav .btn` pattern and the `outline: 2px solid var(--primary); outline-offset: 2px` pattern used by `.tab-btn:focus-visible`). The comment documents the coverage and the `html[data-level]` scoping rationale. | LF-CARD / AC-09; FP-UI-005; owner items 5–7 | Level pages (A1 and B2): the seven toolbar controls grow from 37 to 44 CSS px height and gain the keyboard-focus outline; both rows' layout flexes (they are `align-items: center` flex rows) with no horizontal overflow (SC3-OVERFLOW green on both projects). verbs.html: no match — it never sets `data-level` (verified: `postfix-probe.log` shows its toolbar still 33.0 px with no new outline; reference spec 49/49). The global `button { outline: none }` rule and every other view are untouched. |
| `tests/e2e/cefr-cards.spec.js` | (1) `SC3-A11Y-FOCUS` corrected: the `wanted` list now includes all seven toolbar controls (`#view-flashcard .controls-row > .btn:first-child` for Back to List, `#shuffle-btn`, `#filter-all-btn`, `#filter-learning-btn`, `#filter-favorites-btn`, `#face-de-btn`, `#face-en-btn`) alongside the five previously covered controls — each must be reached by REAL Tab presses (the `tabWalk` helper presses keys; programmatic focus is never used), match `:focus-visible`, and paint a visible indicator (technique-agnostic baseline-vs-focused comparison via `visibleFocusIndicator`). (2) `SC3-A11Y-TARGETS` corrected: the 44×44 existence/width/height assertions now cover all twelve controls (five prior + seven toolbar). (3) NEW `SC3-B2-A11Y-TOOLBAR` (B2 real-data describe): the same seven controls on the B2 page — truthful "🌐 Translation" face label pinned, real Tab walk, `:focus-visible`, painted indicator, and 44×44 for each. | AC-09; TS-TEST-001/003/008; owner items 2–4 | No existing assertion was removed or weakened — the five prior selectors and their assertions are unchanged inside the two corrected tests; only coverage was added. The full spec grows 45 → 46 tests, all green on both projects. |

Root cause (CM-FIX-002): the SHARED-CARD-003 AC-09 patch styled `.fc-btn` and `.fc-nav .btn` but not the two `.controls-row` toolbars, while the global `button { outline: none }` rule suppresses every browser default ring; the then-new tests enumerated only the card affordances and the buttons below the card, so the omission was invisible to the suite. Approach: extend the SAME scoped block (same declarations, same file, same patterns) and correct the SAME two tests' coverage, plus one B2 variant because the owner's list names the per-level "English/Translation face" control; the `html[data-level]` scope addition is the only structural novelty and exists solely to keep verbs.html (which reuses the `#view-flashcard` id) byte-for-byte unaffected. Scope deviations: none. Superseded tests/assertions: **none** — no assertion was deleted, weakened, regenerated, or re-titled away from its stable `SC3-*` ID (both corrected tests keep their IDs; only their coverage lists grew).

## 4. Acceptance-to-evidence mapping

| Criterion / AC row | Test path + exact name | Result | Log/artifact | Tested revision |
|---|---|---|---|---|
| AC-09 / LF-CARD — visible keyboard focus on every visible level-owned flashcard control (owner items 2–3: real Tab navigation, `:focus-visible` match, painted indicator) | E2E `SC3-A11Y-FOCUS` (A1, 12 controls incl. the seven toolbar controls) + `SC3-B2-A11Y-TOOLBAR` (B2, 7 toolbar controls) | PASS | `green-a11y-chromium.log`, `green-a11y-mobile.log`, `red-a11y-chromium.log` (same assertions failed at base) | 222819c (RED at e3d3c22) |
| AC-09 / LF-CARD — ≥44×44 CSS px targets for every visible level-owned flashcard control (owner item 4) | E2E `SC3-A11Y-TARGETS` (A1, 12 controls) + `SC3-B2-A11Y-TOOLBAR` (B2, 7 controls incl. sizing) | PASS | same | 222819c (RED at e3d3c22: back-to-list height 37 < 44) |
| Owner finding itself — the seven listed controls were 37–38 px with no visible outline | `baseline-probe.log` (37.0 px, zero focus style changes, 7/7 Tab-reachable) + `baseline-omission-chromium.log` (old tests 2/2 at base = the omission) | REPRODUCED then FIXED | evidence dir | e3d3c22 → 222819c |
| Narrowly scoped CSS correction under `#view-flashcard` (owner item 5) | Diff inspection: one block in `css/core.css`; no other rule, file, or contract touched | PASS | `git diff --name-only e3d3c22..222819c` = 2 permitted files | 222819c |
| Global contract not weakened; shared-card behavior unchanged (owner item 6) | `button { outline: none }` global rule untouched; `js/core/shared-card.js` / `css/shared-card.css` untouched (diff proof); reference spec `SC-03`/`SC-04`/`C1-*` all green | PASS | `final-reference-chromium.log` 49/49 | 222819c |
| Preserve A1/B2 cards, Phrases, Conversation, SRS, favorites, storage routing (owner item 7) | complete `tests/e2e/cefr-cards.spec.js` both projects (46/46 each) + affected phrase/SRS regression suites | PASS | `final-cefr-cards-chromium.log`, `final-cefr-cards-mobile.log`, `final-affected-regression-chromium.log` | 222819c |
| Mandated verification ladder (owner item 8) | `npm run test:units` 75/75; corrected a11y tests Chromium 3/3 + Mobile Chrome 3/3; complete cefr-cards Chromium 46/46 + Mobile Chrome 46/46; affected phrase/SRS regressions 17 pass/1 pre-existing skip; `git diff --check` clean | PASS | all final logs | 222819c |
| Two mandated fault probes both KILLED (owner item 9) | §7 — sizing-rule removal KILLED by `SC3-A11Y-TARGETS` + `SC3-B2-A11Y-TOOLBAR`; focus-rule removal KILLED by `SC3-A11Y-FOCUS` + `SC3-B2-A11Y-TOOLBAR` | PASS | `probe1.log`, `probe2.log`, `probe-integrity.txt` | 222819c (disposable worktree) |
| verbs.html (German Verbs reference page) untouched | `postfix-probe.log` (verbs toolbar 33.0 px, no new outline, `sizedTo44=false`/`newOutline=false`) + `final-reference-chromium.log` 49/49 | PASS | evidence dir | 222819c |
| AC-21 / TS — tests cannot pass without checking the required behavior | RED evidence at base (3 intended failures) + both fault probes KILLED at the intended assertions + isolation of the two rule families (each probe fails exactly the tests that assert the mutated behavior) | PASS | RED + probe logs | e3d3c22 / 222819c |

## 5. Complete command ledger

Every test command attempted during this package, in execution order. Working directory: the repo root unless noted. `exit` is the command's own exit code (captured with `set -o pipefail` behind `tee`); artifacts live in `docs/cefr/evidence/SHARED-CARD-003-C1/01/`.

| Command (exact) | Phase + revision | Duration | Exit | Passed/failed/skipped | Artifact | Interpretation |
|---|---|---|---|---|---|---|
| `npx playwright test tests/e2e/cefr-cards.spec.js --project=chromium --grep "SC3-A11Y-FOCUS\|SC3-A11Y-TARGETS"` | baseline attempt 1, base `e3d3c22` | <1s | 1 | 0/2 (infrastructure) | (overwritten — see setup note) | Sandbox reprovision had cleared the browser cache: `browserType.launch: Executable doesn't exist`; no test executed. Honest-recorded in the INDEX setup note; the log file was later overwritten by the rerun of the same command |
| `npx playwright install chromium` | setup | ~40s | 0 | — | (not a test) | Pinned browsers installed (chromium + chromium-headless-shell v1228, per lockfile) |
| `node /home/z/my-project/scripts/sc003c1-baseline-probe.mjs` | characterization, base `e3d3c22` | ~10s | 0 | — | `baseline-probe.log` | The seven controls: 37.0 px tall; 7/7 Tab-reachable; `:focus-visible` matches; ZERO focus style changes (no indicator) — the defect quantified |
| `npx playwright test tests/e2e/cefr-cards.spec.js --project=chromium --grep "SC3-A11Y-FOCUS\|SC3-A11Y-TARGETS"` | baseline attempt 2, base `e3d3c22` | 2.8s | 0 | 2/0 | `baseline-omission-chromium.log` | Pre-correction tests pass at base — the omission proven |
| `npm run test:units` | baseline, base `e3d3c22` | <1s | 0 | 75/0 | `baseline-units.log` | Units green before edits |
| (test edits applied: `SC3-A11Y-FOCUS` + `SC3-A11Y-TARGETS` extended, `SC3-B2-A11Y-TOOLBAR` added) | RED prep, working tree | — | — | — | — | Tests only; production CSS still at base |
| `npx playwright test tests/e2e/cefr-cards.spec.js --project=chromium --grep "SC3-A11Y-FOCUS\|SC3-A11Y-TARGETS\|SC3-B2-A11Y-TOOLBAR"` | RED, base `e3d3c22` + corrected tests | 5.4s | 1 | 0/3 | `red-a11y-chromium.log` | All three fail at the intended assertions: indicator false (×2 tests) and height 37 < 44 — the corrected tests detect the defect before the fix |
| (production edit applied: `css/core.css` AC-09 block extended) | fix, working tree | — | — | — | — | One scoped block; nothing else |
| `npx playwright test tests/e2e/cefr-cards.spec.js --project=chromium --grep "SC3-A11Y-FOCUS\|SC3-A11Y-TARGETS\|SC3-B2-A11Y-TOOLBAR"` | focused verification | 5.0s | 0 | 3/0 | `green-a11y-chromium.log` | Corrected tests green on Chromium immediately after the fix |
| `node /home/z/my-project/scripts/sc003c1-postfix-probe.mjs` | focused verification | ~10s | 0 | — | `postfix-probe.log` | Seven controls now 44.0 px + painted outline; verbs.html toolbar untouched (33.0 px, no new outline) |
| `npm run test:units` | final ladder | <1s | 0 | 75/0 | `final-units.log` | Units unchanged |
| `npx playwright test tests/e2e/cefr-cards.spec.js --project="Mobile Chrome" --grep "SC3-A11Y-FOCUS\|SC3-A11Y-TARGETS\|SC3-B2-A11Y-TOOLBAR"` | final ladder | 3.5s | 0 | 3/0 | `green-a11y-mobile.log` | Corrected a11y tests green on Mobile Chrome |
| `npx playwright test tests/e2e/cefr-cards.spec.js --project=chromium` | final ladder | 1.3m | 0 | 46/0 | `final-cefr-cards-chromium.log` | Complete spec green, Chromium |
| `npx playwright test tests/e2e/cefr-cards.spec.js --project="Mobile Chrome"` | final ladder | 1.1m | 0 | 46/0 | `final-cefr-cards-mobile.log` | Complete spec green, Mobile Chrome |
| `npx playwright test tests/e2e/srs.spec.js tests/e2e/activity-streak.spec.js tests/e2e/favorites-filters.spec.js tests/e2e/phrases-conversations.spec.js --project=chromium` | final ladder (affected phrase/SRS regressions) | 37.0s | 0 | 17/0/1 skip | `final-affected-regression-chromium.log` | The 1 skip is the pre-existing desktop skip of the mobile-only phrases test (identical at base and in SHARED-CARD-003 evidence) |
| `npx playwright test tests/e2e/cefr-card-reference.spec.js --project=chromium` | final ladder (extra safety, not mandated: verbs.html loads css/core.css and reuses the `#view-flashcard` id) | 1.2m | 0 | 49/0 | `final-reference-chromium.log` | German Verbs reference untouched |
| `git diff --check` / `git diff --cached --check` (before each commit) / `git status --short` | gates | <1s | 0 | — | INDEX notes | Clean at every stage |
| `npx playwright test tests/e2e/cefr-cards.spec.js --project=chromium --grep "SC3-A11Y-FOCUS\|SC3-A11Y-TARGETS\|SC3-B2-A11Y-TOOLBAR"` | probe baseline, disposable worktree at `222819c` | 4.3s | 0 | 3/0 | `probe-baseline-worktree.log` | Unmodified worktree: corrected tests green (self-contained probe baseline) |
| same command | fault probe 1 (sizing selector removed from `css/core.css`) | 5.0s | 1 | 1/2 | `probe1.log` | KILLED — `SC3-A11Y-TARGETS` + `SC3-B2-A11Y-TOOLBAR` fail at height 37 < 44; `SC3-A11Y-FOCUS` correctly passes (focus rule intact) |
| same command | fault probe 2 (focus-visible selector removed from `css/core.css`) | 5.6s | 1 | 1/2 | `probe2.log` | KILLED — `SC3-A11Y-FOCUS` + `SC3-B2-A11Y-TOOLBAR` fail at the indicator assertion; `SC3-A11Y-TARGETS` correctly passes (sizing rule intact) |

Background/duplicate runs: none (TS-LOOP-006/PX-008) — every command above is a single sequential execution; the repeated grep command is the RED → GREEN → probe-baseline → probe-1 → probe-2 sequence against different trees/mutations, not a rerun of a failing suite. Zero-selection checks: every `--grep` produced non-zero test counts in its own output. Final evidence freshness: no source/test/fixture/dependency/config edit occurred after `222819c` (the only later commit is docs-only), so all final logs remain valid for the delivered code revision. Commit sequence per DR-007 and the owner's incremental-checkpoint instruction: tests `3f82255` → push checkpoint (new remote branch) → fix `222819c` → push checkpoint (fast-forward) → docs commit → final push; every push non-forced, single attempt, transient credential, exit 0.

## 6. Regression and integration

- A1 / B2: complete `cefr-cards.spec.js` green on both projects (46/46 each) — the shared card, secrecy, examples, faces, favorites, SRS writes, navigation, session completion, source boundary, and all regression groups are unchanged; the only behavioral delta is the intended one (toolbar controls ≥44×44 with visible keyboard focus).
- Ordinary Verbs: `cefr-card-reference.spec.js` 49/49 on Chromium — the verbs page loads `css/core.css` and reuses the `#view-flashcard` id, so this is the direct proof the `html[data-level]` scoping held; additionally `postfix-probe.log` measures the verbs toolbar buttons at their original 33.0 px inline-styled size with no new outline. Guided Verbs: not re-run (no verbs or guided file changed; covered transitively by the unchanged reference spec suite).
- Phrases / Conversation / navigation / favorites / SRS: `srs.spec.js` + `activity-streak.spec.js` + `favorites-filters.spec.js` + `phrases-conversations.spec.js` = 17 passed / 1 pre-existing desktop skip; phrase flashcards still flip through the legacy card and phrase grading still routes to the phrase fields (SC3-REG groups green in the full-spec runs).
- Legacy storage / refresh / level and account isolation: no storage key, schema, write-path, or ID touched (CSS + tests only); the full-spec storage assertions (`SC3-A1-SRS-WRITE`, `SC3-A1-FAV`, `SC3-B2-ID-PERSISTENCE`) are green on both projects.
- Actual audio adapter text AND language: not applicable to this package's change (no audio path touched); the unchanged speech assertions inside the full spec (`SC3-A1-SECRECY-AUDIO`, `SC3-B2-SYNTH-VOICE`, `SC3-INT-BACK-CONTROLS`) are green on both projects.
- Browser/module startup and console errors: `SC3-CONSOLE` green on both projects (zero console/page errors with the corrected CSS).
- Desktop/mobile/themes/keyboard/reduced-motion: desktop Chromium + Mobile Chrome (Pixel 5 emulation) both green for the corrected tests and the complete spec; the keyboard walk uses real Tab keypresses on both projects; `SC3-THEME` and `SC3-A11Y-REDUCED-MOTION` remain green; `SC3-OVERFLOW` (no horizontal overflow, front + revealed mixed back) remains green on both projects — the taller toolbar rows introduce no overflow. Visual QA: the probes capture the exact computed dimensions and indicator styles rather than screenshots (consistent with the SHARED-CARD-003 approach); the change is a pure CSS size/outline delta verified by those computed-style measurements.
- Untested/blocked: real-device/Safari (Mobile Chrome is emulation); no human screenshot inspection (per the established computed-style evidence pattern).

## 7. Test-quality and fault-probe evidence

| Probe | Risk/contract | Production target + exact mutation | Detecting test | Baseline / syntax result | Actual failure | Classification | Integrity proof |
|---|---|---|---|---|---|---|---|
| 1 | 44×44 sizing rule disabled / AC-09, owner item 9 | `css/core.css`: the `html[data-level] #view-flashcard .controls-row .btn` selector line removed from the min-width/min-height rule (rule reverts to `.fc-nav .btn` only — 1-line diff, `git diff --stat` = 1 insertion / 2 deletions) | `SC3-A11Y-TARGETS` (primary) + `SC3-B2-A11Y-TOOLBAR` (sizing assertions) | worktree baseline 3/3 passing; valid CSS (the rule block remains syntactically complete) | `back-to-list height` / `height`: Expected ≥ 44, Received 37 in both tests; `SC3-A11Y-FOCUS` correctly still passes (the focus rule is intact — the failure isolates to the sizing family) | **KILLED** | `css/core.css` sha256 `31d8d773…` before → `841a6427…` mutated → `31d8d773…` after restore (byte-identical); tests sha256 `70f62ddd…` never changed |
| 2 | focus-visible rule disabled / AC-09, owner item 9 | `css/core.css`: the `html[data-level] #view-flashcard .controls-row .btn:focus-visible` selector line removed from the outline rule | `SC3-A11Y-FOCUS` (primary) + `SC3-B2-A11Y-TOOLBAR` (indicator assertion) | worktree baseline 3/3 passing; valid CSS | `#view-flashcard .controls-row > .btn:first-child must show a keyboard-focus indicator`: Expected true, Received false in both tests; `SC3-A11Y-TARGETS` correctly still passes (the sizing rule is intact) | **KILLED** | `css/core.css` sha256 `31d8d773…` before → `a01a1f71…` mutated → `31d8d773…` after restore (byte-identical); tests sha256 `70f62ddd…` never changed |

Probe selection: exactly the two families the owner mandated (sizing, focus), one probe each, each disabling precisely the selector this package added and nothing else; production-only mutations inside a disposable Git worktree at `222819c` (tests, fixtures, runner, and Playwright config untouched — TS-MUT-002/007); the worktree's own unmodified baseline was captured first (`probe-baseline-worktree.log`, 3/3) so each KILLED verdict rests on a passing baseline; both probes' failures land on the intended assertions and the third test isolates the mutation to exactly one rule family per probe (TS-MUT-004). The RED run at base is the additional test-quality proof: the corrected tests failed at these same assertions before the fix existed. Test-quality review: no conditional assertions, no blanket skips, no snapshot regeneration, no timeout increases, no sleeps were introduced; the expected values (44 CSS px, indicator presence) are independently authored from the contract, not computed from the implementation; programmatic `.focus()` is never used as focus evidence — every focus assertion comes from a real Tab keypress walk (the pre-existing `tabWalk` pattern).

## 8. Dead-code and dependency inventory

Search scope: `rg` across `*.html`, `css/*.css`, `js/**/*.js`, `tests/**` for every selector and symbol this package touched (DC-002).

| Candidate symbol/path | Searches + dynamic caller checks | Classification | Disposition | Regression proof |
|---|---|---|---|---|
| `.controls-row` (class) | level.html ×5 rows (glossary, filters, flashcard ×2, quiz), verbs.html ×4 rows, app.js ×1 generated row, core.css base + mobile rules, reference spec locator, the new scoped selectors | DYNAMICALLY_USED (live layout class across all pages; this package only adds level-page-scoped consumers) | Kept | full spec + reference spec green |
| `html[data-level]` (attribute scope) | set only by level.html `<head>` script (synchronous); read by app.js via `script[data-level]`; now also consumed by the two new core.css selectors | DYNAMICALLY_USED (new CSS consumer of an existing runtime attribute; level.html always sets it before first paint) | Kept | corrected tests green on both level pages |
| `#view-flashcard .controls-row .btn` (new selectors) | match exactly the seven owner-listed level-page controls (six ids + the structural Back-to-List selector `> .btn:first-child`, unique because the second row's first child is a `.btn-group` div) | DYNAMICALLY_USED | Kept | corrected tests + probes |
| Removed/changed production symbols | none — no declaration was removed; the only production change is two added selector lines and the comment in one existing block | N/A (nothing orphaned by this change) | N/A | diff inspection |

No new dependency, lockfile, Playwright configuration, or framework change; no IDs, content, or storage shapes touched (`git diff e3d3c22..222819c` = `css/core.css` + `tests/e2e/cefr-cards.spec.js`). No dead code was created or removed by this change; the duplication noted in SHARED-CARD-003 §8 (phrase-card presentation, flip CSS in two places) is unchanged and remains assigned to a future REFACTOR package.

## 9. Findings, limitations, and handoff

| Finding ID | Severity/impact | Reproduction and evidence | Disposition | Owner decision needed |
|---|---|---|---|---|
| SC3-C1-FINDING-001 (the owner finding): seven visible `#view-flashcard` controls were 37–38 px tall with no visible keyboard-focus indicator; the existing SC3-A11Y tests omitted them | medium (AC-09 accessibility gap on the primary study surface) | `baseline-probe.log` (37.0 px, zero focus style changes, 7/7 Tab-reachable) + `baseline-omission-chromium.log` (old tests 2/2 at base) + `red-a11y-chromium.log` (corrected tests fail at the intended assertions) | **fixed** — scoped CSS correction + corrected tests + B2 variant test; probes 1 and 2 prove both rule families are guarded | none (reviewer confirmation) |
| verbs.html reuses the `#view-flashcard` id (scope hazard discovered during planning) | low (would have been an out-of-scope visual change on the reference page if the correction had been scoped only by the id) | `verbs.html` lines 1252–1274; `postfix-probe.log` (verbs toolbar unchanged at 33.0 px, no new outline); `final-reference-chromium.log` 49/49 | **mitigated by design** — `html[data-level]` scoping; documented in the CSS comment | none (informational) |
| Pre-existing words-audio failure and R1 SC-INFRA-01 flake (declared in SHARED-CARD-003-01 §9) | medium/low (unchanged, out of scope) | referenced SHARED-CARD-003 evidence; no JavaScript, HTML, or runner file changed in this package, so the prior classifications remain the governing evidence | **open (pre-existing, inherited)** — not re-measured here because this package touches CSS + one test file only | unchanged owner waiver/triage decisions |

- Remaining product risks: none new — the change is a scoped CSS size/outline delta with no state, storage, or behavioral surface; the only coupling is the shared `#view-flashcard` id between the two pages, which the `html[data-level]` scope resolves and the tests/probes pin.
- Environment restrictions and untested requirements: Mobile Chrome is emulation (no real-device/Safari certification); visual verification is by computed-style measurement rather than human screenshot inspection (the established pattern for this codebase); the first baseline attempt hit the reprovisioned sandbox's missing browser cache (setup note in the INDEX) and was resolved with one pinned browser install.
- Final diff/status: `git diff e3d3c22..222819c` touches exactly the two permitted code/test files (+74/−10 tests, +12/−5 CSS); `git diff --check` and `git diff --cached --check` clean at every commit; no unintended ID/content/storage/dependency change (diff inspection).
- GitHub delivery: branch `codex/glm-shared-card-003-c1-01`; incremental checkpoints pushed after the tests commit (`3f82255`) and the fix commit (`222819c`), then the docs commit (final SHA returned in the handoff message); every push a single non-forced attempt via the documented transient credential procedure (chmod-600 store file outside the repo, one-shot `git -c credential.helper='store --file=…'`, redacted output, immediate deletion verified; the owner-pasted session PAT was reused for all three pushes as the task mandates pushing and provides no other credential — rotation recommended).
- Next proposed WP: none started — AUDIO-001/REVIEW-001 and later packages remain untouched pending owner assignment.
- The executor cannot approve its own work: this report claims READY_FOR_REVIEW only; acceptance is the owner's decision.

## 10. Owner/reviewer disposition — not executor approval

- Reviewer and reviewed exact revision: pending review of code `222819ca48cd602a28d09fc7f82e205a28be4dde` (tests commit `3f82255…` + fix commit `222819c…`) and this delivery branch tip (docs).
- Verdict: pending
- Criteria/findings accepted or declined; explicit waivers and reason: for the reviewer — (1) the CSS scoping approach (`html[data-level]` under `#view-flashcard` rather than a bare id scope) is presented for confirmation because it was chosen to protect verbs.html from the shared-id collision; (2) the pre-existing words-audio failure and R1 flake remain under their existing SHARED-CARD-003 waiver/triage decisions (not re-measured here — no relevant file changed); (3) the corrected tests keep their stable `SC3-A11Y-FOCUS`/`SC3-A11Y-TARGETS` IDs with extended coverage plus the new `SC3-B2-A11Y-TOOLBAR` test. This report does not approve itself.
