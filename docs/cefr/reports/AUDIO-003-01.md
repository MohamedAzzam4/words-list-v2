# AUDIO-003 — Attempt 01 Delivery Report

## 1. Identity and status

- Status: READY_FOR_REVIEW
- Assigned goal and task type: Level autoplay and end-to-end language correctness for ordinary CEFR levels (A1 + B2). Primary type `FEATURE` (level autoplay parity with the applicable Verbs controls through the AUDIO-001 planner and AUDIO-002/C1 queue lifecycle) with secondary `BUGFIX` (the words-audio Stop highlight-clearing defect, now in this package's authorized scope, and the speakText explicit-language forwarding gap).
- Owner assignment reference: owner task message "Execute AUDIO-003 — Level Autoplay and End-to-End Language Correctness" (this session), accepted base 742553c1ff4da7f5e03afb8f427d368204807019.
- GitHub task branch; base commit; final tested code commit: `codex/glm-audio-003-01`; base `742553c1ff4da7f5e03afb8f427d368204807019` (AUDIO-002-C1 accepted delivery); tested implementation commit `89b9ead` with the strengthened detector test at `b07be82` (production files byte-identical between the two; probes ran against 89b9ead's production files).
- Evidence index: `docs/cefr/evidence/AUDIO-003/01/INDEX.md`
- Final delivered SHA is returned after commit/push in the handoff message, not embedded into its own commit.
- Executor/model and sandbox OS/runtime versions: GLM (Super Z executor); Debian Linux, Node v24.19.0, npm 11.17.0, Python 3.12.14, Playwright 1.61.x (chromium build 1228), git 2.47.3.
- Start/end time; work directory: 2026-09-02 (session); `/home/z/my-project/words-list-v2` on branch `codex/glm-audio-003-01`.
- Dependency acceptance references: AUDIO-001 planner (speech-plan.mjs, accepted C1 revision, unchanged here), AUDIO-002 + AUDIO-002-C1 SpeechQueue lifecycle (tts.js as delivered at 742553c), SHARED-CARD-003 shared card and first-example behavior (unchanged here).

## 2. Required reading and pre-edit plan

- Paths fully read: AGENTS.md; contracts/LEVEL_FLASHCARD_STANDARD.md, CHANGE_MANAGEMENT.md, TESTING_AND_SUCCESS.md, DEAD_CODE_AND_REFACTORING.md, DELIVERY_REPORTING.md, CODE_FINGERPRINT.md, PORTABLE_AGENT_EXECUTION.md; docs/cefr/WORK_PACKAGES.md (AUDIO-003 section and catalog), ACCEPTANCE_MATRIX.md, GITHUB_DELIVERY.md, GLM_HANDOFF.md; js/core/app.js, tts.js, glossary.js, flashcards.js, speech-plan.mjs, level-data-validator.mjs, nav-service.js, content-parser.js; js/levels/a1.config.js and b2.config.js (parsers and raw formats); level.html; css/core.css (relevant rules); tests/unit/speech-plan.test.mjs, verbs-audio.test.mjs, level-data.test.mjs, flashcards.test.mjs; tests/e2e/verbs-audio.spec.js, words-audio.spec.js, phrases-conversations.spec.js, favorites-filters.spec.js, srs.spec.js; playwright.config.js, package.json; verbs.html autoplay controls (parity reference). No conflicts found; LF-AUDIO/AC-03/AC-11/AC-12 are the governing rows.
- Before behavior (see the pre-edit report issued in-session before editing): words autoplay scraped German text from the glossary DOM with no repeat/examples/translation/start-at/pause/progress, no language handling beyond binary de/en, no Arabic tag or voice, no cancellation on view/mode/filter/hide-German changes, no highlight clearing on stop, and `speakText(t)` dropped the explicit language flashcards passed (English card audio was spoken with a German voice/tag).
- Approved after behavior: planned autoplay from normalized cards with the full control set, queue scope bound to the current level/unit/filter, explicit cancellation on every context change, per-step language tags (de-DE / en-US / ar) with Arabic never sent to an English or German voice, and truthful empty/missing-text handling. Explicit non-goals: no Verbs controller copy, no new queue implementation, no planner redesign, no vocabulary/content/ID changes, no review/favorites/SRS redesign, no phrases/conversation playback change.
- Exact write paths used: `js/core/cefr-audio.mjs` (new pure adapter), `js/core/app.js`, `js/core/tts.js`, `js/core/glossary.js`, `level.html`, `css/core.css`, `tests/unit/cefr-audio.test.mjs` (new), `tests/e2e/cefr-audio.spec.js` (new), `tests/e2e/words-audio.spec.js` (extension), plus this report and evidence. `js/core/flashcards.js` is in the WP write family but needed no change (its speak contract is already language-explicit; LEVEL-DATA-012/013/014 remain binding and green). `js/core/nav-service.js` is NOT in the write family and was not edited; the view/mode cancellations are implemented in the `window.app` wrappers that own those entry points.
- Risk: high (owner designation). Fault families: wrong-language speech, queue scope/ownership leaks, stale-callback resurrection, highlight/control state disagreement, empty-scope handling.
- Baseline tests and known failures: `tests/e2e/words-audio.spec.js` failed 1/1 on both projects at line 77 (highlight not cleared after Stop) — reproduced on the pristine base this session before any edit (evidence: baseline-words-audio-defect.txt). No other baseline failures in the affected suites.

## 3. Changes and rationale

| File | Change/purpose | Contract/WP criterion | Compatibility impact |
|---|---|---|---|
| `js/core/cefr-audio.mjs` (new) | Pure adapter: `matchesVocabularyFilter` (the one shared vocabulary-filter rule) and `mapCefrSpeechStepsToQueueItems` (planned steps → SpeechQueue records with word identity, per-step language, labels). | FP-DESIGN-010, LF-AUDIO, AC-11 | None; new module, no ordering logic duplicated (ordering stays in planSpeechSequence). |
| `js/core/app.js` | `playAllWords()` rebuilt: cards from `engines.glossary.getFilteredWords()` intersected with the historic hide-and-guess row guard; options (repeat/example mode/include-translation/start-at with clamping) read from the new controls; plan via `planSpeechSequence`; queue records via the adapter; visible progress; empty-scope and empty-plan early returns stop any previous session (AUDIO-002-C1 lesson); warning report for skipped steps. `stopAudioQueue()` now clears `.highlighted-speech` (the defect fix) and resets pause/progress/start-at. New `togglePauseWordsAudio`, `toggleAudioSettingsDrawer`, `_populateStartWordSelect`, `_isRowSpeakable`. Cancellations added to `switchView`, `switchMode`, `setTypeFilter`, `hideTableColumn('de'/'mixed')`; `switchUnit` repopulates Start-At. `speakText(t, lang='de')` forwards the caller's language. | LF-AUDIO, AC-12, LF-NAV, CM-FIX-003/004 | Button/flow contracts preserved (Play All toggle semantics, phrases code paths untouched); flashcard English audio now actually English (intended fix). |
| `js/core/tts.js` | `'ar'` language tag at the utterance boundary in `speak()` and `SpeechQueue._speakCurrent()`; Arabic voice slot selected like German/English; Arabic never assigned the English or German voice; level ownership hook in `speak()` (`window.app.stopAudioQueue`) so a single pronunciation replaces a running level queue (mirrors the existing Verbs hook). | LF-AUDIO, AC-03 | Existing de/en paths byte-equivalent in behavior; phrases (no `.lang`) unchanged. |
| `js/core/glossary.js` | Imports the shared matcher; render and the new `getFilteredWords()` both use it — table and queue scope cannot disagree. | LF-AUDIO, DC-008 | Filter semantics identical to the previous inline predicate (unit-tested). |
| `level.html` | Words-tab autoplay controls: pause button, gear + settings drawer (repeat 1/2/3, example mode first/all/none, include-translation, Start-At), progress pill. | LF-AUDIO, AC-11 | Additive markup; existing controls unchanged. |
| `css/core.css` | `#glossary-tbody tr.highlighted-speech` (reduced-motion aware) and `.words-audio-progress` styles. | LF-AUDIO, FP-UI-005 | Additive rules. |
| `tests/unit/cefr-audio.test.mjs` (new) | Adapter unit tests: matcher semantics/purity; mapping with hand-written steps and expected records; real A1/B2 parser cards through the real planner; Arabic-only, mixed, multi-example (synthetic), repeat ordering, mute card. | TS-PRE-001, TS-TEST-005 | New. |
| `tests/e2e/cefr-audio.spec.js` (new) | The assignment's browser cases 1–22 through the real level pages with a deterministic speech double (manual completion driving, no sleeps), plus the no-synthesis fallback describe. | AC-03/AC-11/AC-12, TS-TEST-004 | New. |
| `tests/e2e/words-audio.spec.js` | Existing defect test kept byte-for-byte; added coverage for highlight clearing after unit navigation, filter change, rapid queue replacement, and natural completion. | CM-FIX-004 | Existing assertions not weakened (verified: only additions after the original test). |

Root cause of the fixed defect: `app.js stopAudioQueue()` reset button labels but never removed `.highlighted-speech` from glossary rows (the Verbs controller does); natural completion routes through the same method, so the stale highlight survived both paths. Fix: the same clearing rule as the Verbs controller, plus control resets for the new controls.

Scope deviations/approvals: none beyond the disclosed design decisions in the pre-edit report (hide-German toggle also cancels autoplay — the answer-secrecy extension of "no hidden content may continue speaking"; no all-unit scope exists in the level UI, so the queue follows the current unit, documented as N/A).

Tests or assertions superseded: none. The former words-audio:48 failure is fixed in behavior, not by weakening the test.

## 4. Acceptance-to-evidence mapping

| Criterion / AC row / contract ID | Test path + exact name | Result | Log/artifact | Tested revision |
|---|---|---|---|---|
| LF-AUDIO autoplay controls (repeat, example mode, include-translation, start-at, progress, pause, resume, stop, completion, replacement, empty) | tests/e2e/cefr-audio.spec.js — "A1 autoplay speaks the exact planned text and language sequence with visible progress", "repeat count orders…", "example modes none, first, and all…", "include-translation on and off…", "start-at beginning, middle, and end…", "pause and resume retain the current position…", "stop clears future speech…", "rapid queue replacement…" | PASS (both projects) | final-cefr-audio-chromium.log / final-cefr-audio-mobile.log | b07be82 |
| LF-AUDIO queue scope: current level/unit/filter; no hidden unit speech | "unit navigation cancels the old queue and the new unit speaks only its own words", "the active vocabulary filter scopes the queue and cancels on change" | PASS (both projects) | same | b07be82 |
| LF-AUDIO empty result (idle + while active) | "the active vocabulary filter scopes the queue and cancels on change" (favourites filter with no favourites) | PASS (both projects) | same | b07be82 |
| Stale callbacks cannot advance/restart/alter | "rapid queue replacement owns the speaker and stale callbacks cannot disturb it" (strengthened atomic cursor check) | PASS (both projects) | same | b07be82 |
| AC-03 language at the real utterance boundary (German/English tags; Arabic tag; mixed → separate steps; Arabic never an English voice; missing English never substituted) | "B2 mixed cards speak separately tagged English and Arabic steps", "a B2 Arabic-only translation speaks Arabic and never an English voice" (route-injected synthetic card, disclosed), "an available Arabic voice is selected for Arabic steps" | PASS (both projects) | same | b07be82 |
| Speech error advances; no-synthesis fallback truthful | "a genuine speech error advances the queue"; no-synthesis describe | PASS (chromium + Mobile Chrome for the main describe; no-synthesis run in both full suites) | same + final-full-*.log | b07be82 |
| Normalized fields honored (translationLanguage, speechText, examples, identity) | tests/unit/cefr-audio.test.mjs — real-parser integration tests | PASS 11/11 | final-units.log | b07be82 |
| First example stays on the card; autoplay queues examples; no term duplication | "the first example and translation remain visible on the flashcard" + unit multi-example test | PASS | same | b07be82 |
| A1 special tabs preserved; phrase/conversation unchanged | tests/e2e/phrases-conversations.spec.js (23 passed, 1 paired skip) + "tab changes cancel or replace queue ownership between words and phrases" | PASS | ladder7 + final logs | 89b9ead / b07be82 |
| Favorites/SRS/storage/shared-card regressions | tests/e2e/favorites-filters.spec.js, tests/e2e/srs.spec.js, full suite incl. cefr-cards/verbs suites | PASS | ladder8 + final-full-*.log | 89b9ead / b07be82 |
| Known in-scope defect (words-audio:48) | tests/e2e/words-audio.spec.js — all 5 tests, both projects | PASS 10/10 | ladder6-words-audio-both.log | 89b9ead |
| AC-21 test quality | RED evidence + six fault probes | PASS | red-*.log, probe-*.log | 742553c / 89b9ead |

Case-23/24 phrase/conversation and favorites/SRS/shared-card regression coverage is provided by the tracked suites in the ladder (deliberately not duplicated in the new spec).

## 5. Complete command ledger

Environment/setup events (full detail in evidence setup-events.txt): chromium build 1228 re-download before the first baseline (launch failed before any test logic); one RED command killed at the 500s sandbox window (canceled, re-run with `--timeout=8000`); the both-projects full-suite command killed at the 600s window (canceled) and a failed nohup background attempt (empty log) — the full suite was then run per project; probe round 1 had a broken restore (no `.git` in the git-archive copy) — fixed and fully re-run (round 1 superseded, disclosed).

| Command (exact) | Phase + revision | Duration | Exit | Passed/failed/skipped | Artifact | Interpretation |
|---|---|---|---|---|---|---|
| `npx playwright test tests/e2e/words-audio.spec.js --project=chromium` | baseline, 742553c | ~25s | 1 | 0/1/0 | baseline-words-audio-defect.txt | Known defect reproduced (RED) |
| same `--project="Mobile Chrome"` | baseline, 742553c | ~25s | 1 | 0/1/0 | baseline-words-audio-defect.txt | Same on Mobile (RED) |
| `node --test tests/unit/cefr-audio.test.mjs` | RED, 742553c+tests | <1s | 1 | 0/1/0 | (console; module missing) | Unit RED |
| `npx playwright test tests/e2e/cefr-audio.spec.js --project=chromium --timeout=8000` | RED, 742553c+tests | ~2.5m | 1 | 0/18/0 | red-cefr-audio-chromium.log | Suite RED |
| same `--project="Mobile Chrome"` | RED, 742553c+tests | ~3m | 1 | 0/18/0 | red-cefr-audio-mobile.log | Suite RED |
| `npx playwright test tests/e2e/words-audio.spec.js --project=chromium --timeout=8000` | RED, 742553c+tests | ~19s | 1 | 1/4/0 | red-words-audio-chromium.log | 4 new-case failures; unit-navigation case green on base (characterization, disclosed) |
| same `--project="Mobile Chrome"` | RED | ~23s | 1 | 1/4/0 | red-words-audio-mobile.log | Same pattern |
| `node --check` / `node --input-type=module --check` (7 changed modules/tests) | ladder 1, 89b9ead | <1s | 0 | n/a | (console: all OK) | Syntax/import checks |
| `node --test tests/unit/cefr-audio.test.mjs` | ladder 2, 89b9ead | <1s | 0 | 11/0/0 | (console) | Focused adapter tests |
| `npm run test:units` | ladder 3, 89b9ead | ~1s | 0 | 155/0/0 | ladder3-units.log | Full units |
| `npx playwright test tests/e2e/cefr-audio.spec.js --project=chromium` | ladder 4, 89b9ead | ~40s | 0 | 18/0/0 | ladder4-cefr-audio-chromium.log | New suite green |
| same `--project="Mobile Chrome"` | ladder 5, 89b9ead | ~42s | 0 | 18/0/0 | ladder5-cefr-audio-mobile.log | After mobile click-latency harness fix |
| `npx playwright test tests/e2e/words-audio.spec.js` | ladder 6, 89b9ead | ~12s | 0 | 10/0/0 | ladder6-words-audio-both.log | Defect fixed, both projects |
| `npx playwright test tests/e2e/phrases-conversations.spec.js` | ladder 7, 89b9ead | ~1.1m | 0 | 23/0/1 | ladder7-phrases-conversations.log | Paired mobile-only skip (by design) |
| `npx playwright test tests/e2e/favorites-filters.spec.js tests/e2e/srs.spec.js` | ladder 8, 89b9ead | ~7s | 0 | 4/0/0 | ladder8-favorites-srs.log | Regressions |
| `npx playwright test tests/e2e/verbs-audio.spec.js` | ladder 9, 89b9ead | ~50s | 0 | 32/0/0 | ladder9-verbs-audio.log | Verbs audio regressions |
| `npx playwright test --project=chromium` | ladder 10, 89b9ead | ~5.2m | 0 | 182/0/1 | ladder10-full-chromium.log | Full suite chromium |
| `npx playwright test --project="Mobile Chrome"` | ladder 10, 89b9ead | ~5.4m | 1 | 182/1/0 | ladder10-full-mobile.log | verbs.spec.js:33 Mobile flake (known family) |
| `npx playwright test tests/e2e/verbs.spec.js --project="Mobile Chrome" --grep "custom Auto-Play Audio sequence"` | isolation, 89b9ead | 1.8s | 0 | 1/0/0 | ladder10-isolate-verbs-mobile.log | Flake passes isolated |
| `npx playwright test tests/e2e/verbs.spec.js --project="Mobile Chrome"` | rerun, 89b9ead | 4.6s | 0 | 5/0/0 | ladder10-verbs-mobile-rerun.log | File-level rerun green |
| `npx playwright test --project="Mobile Chrome"` | rerun, 89b9ead | ~5.3m | 0 | 183/0/0 | ladder10-full-mobile-rerun.log | Full Mobile suite green; flake not reproduced |
| probe baselines/mutants (6 probes × 2 runs + strengthened P5 pair) | probes, 89b9ead | ~4m total | per log | per log | probe-*.log | See section 7 |
| `npm run test:units` | final, b07be82 | ~1s | 0 | 155/0/0 | final-units.log | Final units |
| `npx playwright test tests/e2e/cefr-audio.spec.js` (chromium, Mobile Chrome) | final, b07be82 | ~40s each | 0 | 18/0/0 each | final-cefr-audio-*.log | Final new suite, both projects |
| `npx playwright test --project=chromium` | final, b07be82 | ~5.2m | 0 | 182/0/1 | final-full-chromium.log | Final full suite |
| `npx playwright test --project="Mobile Chrome"` | final, b07be82 | ~5.2m | 0 | 183/0/0 | final-full-mobile.log | Final full suite |
| `git diff --check`; `git status --short` | ladder 11–12, all revisions | <1s | 0 | n/a | (console) | Clean; only the assigned files changed |

Reason for each broad rerun: the Mobile full-suite rerun isolated the single first-run failure; the final full re-runs (units, cefr-audio both projects, both full-suite projects) were required because the strengthened detector test (b07be82) is a material test edit after the ladder runs (TS-LOOP-007). No duplicate concurrent Playwright processes were ever launched (sequential only; the killed commands left no orphans — verified).

## 6. Regression and integration

- A1 / B2: full new autoplay suite green on both projects with exact text/lang/voice assertions from real data (A1 unit 1 card 1-0 sequence; B2 mixed cards; B2 en-only via unit tests on real dataset cards).
- Ordinary Verbs / Guided Verbs: verbs-audio 32/32; verbs.spec.js + verb-guided-challenge green in both full suites (one transient Mobile flake in the first run, proven transient).
- Phrases / Conversation / navigation / favorites / SRS: phrases-conversations 23+1 paired skip; favorites-filters + srs 4/4; favorites/SRS/shared-card suites green in both full suites; storage identity untouched (no schema/ID changes — `git diff` shows no data files).
- Legacy storage / refresh / level and account isolation: not modified; level change test proves the queue cannot survive a level page load (fresh context, zero speech).
- Actual audio adapter text AND language: asserted at the real SpeechSynthesisUtterance boundary (text, lang, voice) in every cefr-audio language test; Arabic steps carry `lang: 'ar'` and either the Arabic mock voice or no explicit voice — never the English/German voice.
- Browser/module startup and console errors: full suites run with the tracked suites' console-error checks green; no new console errors from the changed modules.
- Desktop/mobile/themes/keyboard/reduced-motion/screenshots: both configured projects green; reduced-motion rule added for the new row highlight; touch targets use the existing `.btn` styles. No screenshots were captured this attempt (no new visual layout beyond existing control patterns; the CSS additions are additive) — noted as a limitation for reviewer visual QA.
- Inapplicable/untested: real-device voice output and real Arabic/German/English hardware voices (headless mock only — the report separates deterministic queue proof from voice availability, TS-DONE-006); "Mobile Chrome" is emulation; example mode "all" beyond "first" has no real-data instance (real cards carry ≤1 example) — pinned by unit tests with a synthetic multi-example card and asserted equal-to-first on real data.

## 7. Test-quality and fault-probe evidence

| Probe | Risk/contract | Production target + patch | Detecting test | Baseline / syntax | Actual failure | Classification | Integrity proof |
|---|---|---|---|---|---|---|---|
| P1 | Mislabel Arabic as English (LF-AUDIO/AC-03) | tts.js `_speakCurrent`: ar mapped to `en-US` + englishVoice | cefr-audio "B2 mixed cards speak separately tagged English and Arabic steps" | pass / parse OK | lang+voice assertions failed | KILLED | restore hash-verified |
| P2 | Remove current-unit scope (LF-AUDIO) | app.js: cards := `levelConfig.vocabulary.flat()` | cefr-audio "unit navigation cancels the old queue…" | pass / parse OK | restart on unit 2 spoke unit 1's word | KILLED | restore hash-verified |
| P3 | Remove tab/unit navigation cancellation (AC-12) | app.js: switchUnitTab leading stop removed | cefr-audio "tab changes cancel or replace queue ownership…" | pass / parse OK | queue not cancelled on tab change | KILLED | restore hash-verified |
| P4 | Remove active-filter scope (LF-AUDIO) | app.js: cards := `levelConfig.vocabulary[state.unit]` (filter ignored) | cefr-audio "the active vocabulary filter scopes the queue…" | pass / parse OK | first utterance not a verb under the verbs filter | KILLED | restore hash-verified |
| P5 | Remove stale-callback ownership protection (AC-12) | tts.js: two-layer onend guard removed | cefr-audio "rapid queue replacement…" (strengthened) | pass / parse OK | atomic cursor check failed on mutant | KILLED | restore hash-verified |
| P6 | Remove the stopAudioQueue highlight clear (the fixed defect) | app.js: `.highlighted-speech` clear removed | words-audio "Play all words highlights rows and respects filters" (line 77) | pass / parse OK | line-77 assertion failed | KILLED | restore hash-verified |

Probe mechanics: production-only mutations in a disposable git-archive copy of 89b9ead with hard-linked node_modules (symlinks are blocked in this sandbox); every mutant parse-checked; every detector run against the pristine copy first (all baselines passed); the owner's working tree never mutated; each restore is byte-verified against the pristine hash. Full TS-MUT-005 cycle retained: the original P5 detector let the mutant survive (the 250ms delayed-speak window hid the early advance); the detector was strengthened to fire the stale onend and read the replacement queue's cursor state atomically, verified green on unmodified code on both projects (commit b07be82), and the mutant is now killed. Probe round 1's broken restore (git-checkout in a copy without .git) is disclosed in setup-events.txt; the entire round was re-run cleanly after the fix.

Conditional assertions/weak mocks review: no conditional assertions; the speech double mocks only the browser platform (text/lang/voice recorded, completion driven manually); the route-injected synthetic Arabic-only card and the temporary out-of-range select option are disclosed synthetic edge cases (real B2 data has no Arabic-only card; a real select cannot hold a value with no option).

## 8. Dead-code and dependency inventory

| Candidate symbol/path | Searches + dynamic caller checks | Classification | Disposition |
|---|---|---|---|
| `GlossaryEngine.speakAll()` (js/core/glossary.js) | rg repository-wide: only an old audit document references it; no static or `data-action`/inline-handler callers; the new autoplay path does not use it | PRE-EXISTING orphan (not created by this change) | Reported for owner triage; NOT removed (DC-004: removal belongs to an authorized cleanup package) |
| Old DOM-scrape logic in `playAllWords` | The only caller was rewritten by this change; replaced by the data-driven planner path | Removed by this change (superseded) | Covered by the new suite |
| `auto-include-en` / `auto-start-word` etc. on verbs.html vs level.html | IDs deliberately mirror the Verbs page (separate documents; no conflict) | Duplicated markup by design (parity) | Live on both pages |

Search boundary: changed symbols and their direct callers, HTML inline handlers, `window.app` exports, delegated `data-action` strings, dynamic imports, and level configuration (DC-002). No dependency/manifest/lockfile changes.

## 9. Findings, limitations, and handoff

| Finding ID | Severity/impact | Reproduction and evidence | Disposition | Owner decision needed |
|---|---|---|---|---|
| F-A003-1 | Medium (product) | Real B2 data has no Arabic-only cards (3028 mixed / 3 en / 0 ar — data recon). The Arabic-only path is proven with a route-injected synthetic card and real-parser unit fixtures. | Documented; no data change (out of scope) | Whether future content should include Arabic-only translations |
| F-A003-2 | Low (test env) | Mobile Chrome full-suite flake at verbs.spec.js:33 (first ladder-10 run only; passes isolated, at file level, and in the full-suite rerun; matches the known F-A002C1-2/SC-INFRA-01 family from the AUDIO-002-C1 delivery) | Not reproduced with proof; disclosed | Owner triage of the flake family (pre-existing) |
| F-A003-3 | Low (UX, disclosed) | The main Play All button remains a toggle (stop while playing) as before; a paused session restarts from the beginning when Play is pressed (same as the phrases behavior and the Verbs restart semantics). | Documented behavior decision | Optional future UX refinement |
| F-A003-4 | Low (a11y/presentation) | The words row highlight previously had NO CSS rule on level pages; this change adds one (reduced-motion safe). | Fixed within scope | Visual QA welcome |
| F-A003-5 | Info | `GlossaryEngine.speakAll()` is a pre-existing orphan (see dead-code table). | Reported, not removed | Owner decision on removal in a cleanup package |

- Remaining product risks / environment restrictions: headless mocks cannot prove real hardware voice availability (German/English/Arabic) on end-user devices; Mobile Chrome is emulation; the no-voice fallback is proven deterministically but real-device voice selection depends on the platform. Example mode "all" beyond "first" is unit-pinned only (real data has single examples). No visual screenshots captured this attempt.
- Final diff/status: only the assigned files changed (7 implementation files + 3 test files + docs/evidence); no IDs, content counts, storage, dependencies, or contracts changed; `git diff --check` clean at every commit.
- GitHub delivery branch and report/evidence paths: `codex/glm-audio-003-01`; this report; `docs/cefr/evidence/AUDIO-003/01/INDEX.md`. Remote-SHA verification returned in the final message.
- Next proposed WP: REVIEW-001 (not started; awaits owner assignment).

## 10. Owner/reviewer disposition — not executor approval

- Reviewer and reviewed exact revision: pending (branch `codex/glm-audio-003-01`, final delivered SHA in the handoff message).
- Verdict: pending.
- Criteria/findings accepted or declined; explicit waivers and reason: none recorded yet.
