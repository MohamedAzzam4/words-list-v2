# SHARED-CARD-003 — Attempt 01 Delivery Report

## 1. Identity and status

- Status: READY_FOR_REVIEW
- Assigned goal and task type: adopt the accepted shared-card presentation (the SHARED-CARD-002-C1 revision of `js/core/shared-card.js`) in ordinary A1 and B2 vocabulary flashcards — FEATURE (primary: new presentation for ordinary vocabulary) + MODIFICATION (secondary: replaces the legacy words-card presentation; introduces an explicit words/phrases source boundary and fixes the stale-source persistence desync it exposed). CM gates FEAT + MOD applied: behavior before/after documented (§2), superseded assertions listed (§3), failing-target RED evidence captured before any production edit (§5).
- Owner assignment reference: owner task message "TASK: Execute SHARED-CARD-003 — adopt the accepted shared-card presentation in ordinary A1 and B2 vocabulary flashcards" with the required base, permitted production/test/delivery paths, UX requirements, Stage A/B plan, mandatory coverage, fault-probe families, verification ladder, and delivery rules.
- GitHub task branch: `codex/glm-shared-card-003-01`; base commit: `e55debd4d30ff9e6122c4ff93756ca38732c621a` (accepted SHARED-CARD-002-C1 revision); stage commits: A1 `9b31bd58c5482dc24b71b094e2fffb43728a466e`, B2/final tested code `9d9eb61717500062929a4f83e311a428e9163666` (all code evidence in `docs/cefr/evidence/SHARED-CARD-003/01/` ran against exactly this tree; the report/evidence commit is docs-only and does not move the tested code revision).
- Evidence index: `docs/cefr/evidence/SHARED-CARD-003/01/INDEX.md`
- Final delivered SHA is returned after commit/push in the handoff message, not embedded into its own commit.
- Executor/model and sandbox OS/runtime versions: Super Z (GLM executor); Debian sandbox, Node v24.19.0, npm 11.17.0, Playwright 1.61.1 (lockfile; chromium + chromium-headless-shell v1228 already present from the C1 session).
- Start/end time; work directory: 2026-08-30 ~02:00–~04:00 UTC (sandbox clock); `/home/z/my-project/words-list-v2` (repo-relative paths in this report).
- Dependency acceptance references: SHARED-CARD-002-C1 attempt 01 accepted by owner review (base of this package); SHARED-CARD-001/002 evidence lineage for the pre-existing words-audio baseline failure.
- Language: English only, per the owner's mandatory language requirement.

## 2. Required reading and pre-edit plan

- Paths fully read, in the owner's order: `AGENTS.md`; `contracts/README.md`; `contracts/PORTABLE_AGENT_EXECUTION.md`; `contracts/CHANGE_MANAGEMENT.md`; `contracts/CODE_FINGERPRINT.md`; `contracts/TESTING_AND_SUCCESS.md`; `contracts/DEAD_CODE_AND_REFACTORING.md`; `contracts/DELIVERY_REPORTING.md`; `contracts/LEVEL_FLASHCARD_STANDARD.md`; `docs/cefr/BASELINE.md`; `docs/cefr/WORK_PACKAGES.md` (SHARED-CARD-003 section); `docs/cefr/ACCEPTANCE_MATRIX.md` (AC-04..AC-10); `Refactoring documentation/plans/CEFR_LEVEL_FLASHCARD_STANDARD_AND_REVIEW_CENTER_ROADMAP.md`; `docs/cefr/reports/SHARED-CARD-002-C1-01.md`; plus the complete current implementations and relevant tests: `js/core/flashcards.js`, `js/core/app.js`, `js/core/nav-service.js`, `js/core/stats-service.js` (source-flag reader; read-only), `js/core/storage.js`, `js/core/tts.js`, `js/core/firebase.js`, `js/core/content-parser.js`, `js/core/glossary.js`, `js/core/shared-card.js`, `js/core/verbs-engine.js` (ordinary adapter, flipCard, bindEvents), `js/levels/a1.config.js` + `js/levels/b2.config.js` (parsers + raw data), `level.html`, `verbs.html` (card CSS), `css/core.css` + `css/shared-card.css`, `tests/unit/flashcards.test.mjs`, `tests/unit/shared-card.test.mjs`, `tests/e2e/cefr-card-reference.spec.js` (complete), `tests/e2e/favorites-filters.spec.js`, `tests/e2e/srs.spec.js`, `tests/e2e/words-audio.spec.js`, `tests/e2e/activity-streak.spec.js`, `tests/e2e/phrases-conversations.spec.js`, `playwright.config.js`, `package.json`. No conflicts found.
- Before behavior (characterized at `e55debd`): the ordinary words flashcard on the level pages was the legacy static DOM (`#active-flashcard` / `.flashcard-inner` with `#fc-de`/`#fc-en`/`#fc-ex-de`/`#fc-ex-en`) rendered by `FlashcardEngine.render()` — pointer-only activation (inline `onclick`, no tabindex/role/keyboard), the answer and example parked in the DOM before reveal, and the "example" filled from the word/meaning (`w.context ? w.de : ''`) instead of the real example (BASELINE gap 1). The same engine served phrase flashcards through `NavigationService` with the source tracked only in `state.flashcardSource`, which `switchUnit` never resynced: grading a word after switching units from the phrase-flashcard view wrote word progress into `knownPhrases`/`favoritePhrases`/`phraseErrors` (corruption; proven by the RED run of `SC3-REG-SOURCE-BOUNDARY`).
- Approved after behavior: ordinary A1/B2 word cards render through the accepted shared renderer — shared shell and flip interaction, topbar speak/favorite affordances with descriptive accessible names and truthful `aria-pressed`, lazy answer side (secrecy before reveal), first real German example with its actual translation and truthful direction/language metadata (en ltr, ar rtl, mixed/unknown auto with no single-language label), pointer + Enter + Space activation with duplicate-event guards, inert inactive faces, 44×44 targets, visible focus, reduced motion, light/dark, no horizontal overflow — while the level-owned controls (filters, shuffle, face modes, prev/next, counter, Known/Still Learning, session completion) and all SRS/favorites persistence to the exact original stable IDs keep working. Phrase flashcards retain their existing legacy card and behavior verbatim; Conversation is untouched; the words/phrases source is explicit at every `loadUnit` call site and the unit-switch desync is fixed.
- Explicit non-goals: Review Center, autoplay redesign (AUDIO-001..003 — including the `window.app.speakText` language-forwarding gap, which remains AUDIO-003's scope), vocabulary content or ID changes, German Verbs scheduling/grading/latency/persistence, `verb-challenge-engine.js`, Guided Challenge behavior, storage schema, dependencies/config, unrelated refactoring.
- Exact allowed write paths (all writes stayed inside them): `js/core/flashcards.js`, `js/core/nav-service.js`, `level.html`, `css/core.css`, new `tests/e2e/cefr-cards.spec.js`, `tests/unit/flashcards.test.mjs`, report + evidence paths. `js/core/app.js`, `js/core/shared-card.js` and `css/shared-card.css` were NOT modified (permitted but not needed — the shared renderer's API already covers the level-card needs). `git diff --name-only e55debd..9d9eb61` = exactly the six permitted files.
- Risk: HIGH (shared SRS/favorites wiring + the phrase boundary + both levels' primary study surface). Fault families assigned: example-source substitution, secrecy removal, wrong-unit persistence, duplicate-event guard, phrase/source isolation, direction/language mislabeling — six probes executed (§7).
- Affected boundaries: presentation only. No storage schema, key, write-path, ID, content, scheduler, TTS adapter signature, or public export changed; `window.app` API surface unchanged.
- Baseline tests and known failures: units 73/73; affected Chromium suites green (srs + activity-streak + favorites-filters 6/6; phrases-conversations 11 pass/1 pre-existing skip); words-audio:48 failing at base — the mandatory measurement — and byte-identical at the final revision (pre-existing; §6).

## 3. Changes and rationale

| File | Change/purpose | Contract/WP criterion | Compatibility impact |
|---|---|---|---|
| `js/core/flashcards.js` | Words-source cards render through the shared renderer (`renderSharedCard`/`renderCardFront`/`renderCardAffordances`/`renderExampleBlock`): front = type badge + SRS row + question term (German with `lang="de"` and `deContext`, or the translation with `dir`/`lang` from `translationDisplayAttrs`); revealed back = answer + FIRST real example block (language-neutral fields from `examples[0]`); answer markup exists only after reveal; `flip()` mirrors the verbs adapter (class toggle + inert both faces + lazy back fill); mount-scoped delegated click + keydown (Enter/Space, repeat guard, button exclusion); `render()` preserves the revealed state (a favorite toggle from the back never hides the answer); `setFilter`/`setFace`/`toggleShuffle` reset to the front. New pure exports `firstExample(word)` and `translationDisplayAttrs(language)`. Explicit `source` ('words' default / 'phrases') on constructor + `loadUnit`; the legacy phrases render path and its observable behavior (including the re-render flip reset) are preserved verbatim via a one-time snapshot/restore of the static card markup. | LF-CARD / LF-NAV; AC-04..AC-10; FP-DESIGN-009; FP-UI-004/005/006 | Public engine methods unchanged; constructor/loadUnit gained optional trailing params only (the VM unit loader with 5 args still works); phrases path byte-compatible |
| `js/core/nav-service.js` | All three `loadUnit` call sites pass the explicit card source (`switchMode` phrases/words branches, `switchUnit` words); `switchUnit` additionally resyncs `state.flashcardSource = 'words'` — the explicit source boundary that fixes the stale-source desync (word grades after a unit switch from the phrase-flashcard view previously corrupted `knownPhrases`). | Task architecture-risk clause; AC-17 analog; FP-DESIGN-007 | `state.flashcardSource` is deliberately kept (read by `stats-service.js:20`, which is outside this package's permitted paths) and is now set at every source transition |
| `level.html` | Single card slot `#fc-card-mount` wrapping the untouched legacy phrase card (the words render replaces it in the slot; phrase mode restores the snapshot verbatim — exactly one card in the DOM at any time, so no duplicate ids); `css/shared-card.css` linked; B2's translation-front control relabeled "🌐 Translation" (B2 display translations are mixed EN/AR or Arabic-only — never labeled English; A1 keeps its truthful "🇬🇧 English" because its data is English). | LF-CARD / LF-NAV; AC-10; task UX requirement 7 | Phrases markup unchanged inside the slot; no other view touched |
| `css/core.css` | Card-shell styles scoped under `#fc-card-mount` (flip mechanics, faces, topbar, center content, tap hint, term/label/context, SRS row, example box, favorite button base, mobile media query) mirroring the accepted verbs card; `#view-flashcard .fc-btn:focus-visible` / `.fc-nav .btn:focus-visible` outlines and the `.fc-nav .btn` 44×44 minimum for the level-owned controls. | LF-CARD / AC-09 | Scoped selectors cannot affect `verbs.html` (no `#fc-card-mount` there); the verbs page's inline card styles are untouched |
| `tests/e2e/cefr-cards.spec.js` (new) | 45 tests: A1 real data (shell/no-deck, real first example + translation, faces, DOM + AX-tree secrecy, audio secrecy, favorites immediate + refreshed + no flip/grade, exact-ID SRS writes with wrong-unit negatives, filters/shuffle/nav/counter, session completion + restart), A1 synthetic edges (no-example incl. stale-content negatives, multiple-example first-only, example-without-translation), B2 real data (mixed translation + mixed example translation with dir=auto/no-lang, real English-only card 45-18 ltr/en, real no-example unit 69, real sentence-example-without-translation 69-80, secrecy sweep, exact B2 ID under the B2 storage key, unit switching), B2 synthetic language edges (Arabic-only rtl/ar, mixed multiple-example, no-example en, missing-translation truthful placeholder, voice safety — Arabic/mixed never spoken), interaction/a11y (pointer, Enter, Space, control-keyboard isolation, grade double-fire protection, back controls never flip/grade/advance, inert faces + AX-tree + flip-back restore, accessible names + aria-pressed, focus-visible indicators, 44×44, reduced motion, light/dark, overflow, clean console), regression (phrases tab, phrase flashcards legacy behavior + phrase-counter persistence, conversation, B2 missing-content states, source boundary, mode swap with no duplicate ids, A1 unit switch). | Mandatory test coverage list; TS-TEST-001..008 | New file; no existing test touched |
| `tests/unit/flashcards.test.mjs` | Two new unit cases for the pure helpers (`translationDisplayAttrs` metadata table incl. mixed/unknown; `firstExample` first-only/no-example/legacy-null). | TS-PRE-001; FP-TEST-001 | Existing 3 cases untouched and green |

Approach rationale: the words card reuses the accepted shared renderer with zero changes to `js/core/shared-card.js` / `css/shared-card.css` — the adapter (`FlashcardEngine`) supplies level-owned content (type badge, SRS row, question/answer terms, example view-model) exactly as `verbs-engine.js` does, so both consumers now share one presentation implementation (AC-04). The single-slot + snapshot/restore design keeps the phrase card's static markup and inline handlers byte-identical while guaranteeing that exactly one card (and one of each compatibility id: `#active-flashcard`, `#fc-de`, `#fc-en`, `#fc-srs-dots`) exists at any time — which is why every pre-existing E2E assertion in `srs.spec.js`, `activity-streak.spec.js` and `phrases-conversations.spec.js` stayed green UNCHANGED (no superseded assertions; see the superseded-test ledger below for the corrections made to this package's own new tests during stabilization). Scope deviations: none.

Superseded-test ledger: **no existing test or assertion was superseded, weakened, or regenerated.** During stabilization, four expectations in this package's OWN new tests were corrected against real behavior (documented for transparency): (1) `SC3-REG-MODE-SWAP` originally expected one `#fc-en` pre-reveal — corrected to zero (the lazy answer side renders no answer element at all before reveal, which is the approved secrecy behavior) and one after the flip; (2) `SC3-REG-A1-UNIT-SWITCH` expected unit 2 to hold 30 words — corrected to the real 43; (3) the "Back to List" locator needed scoping to `#view-flashcard` (a second identical button exists in the article-quiz view); (4) `SC3-B2-SYNTH-MISSING-TR` originally asserted the placeholder on the revealed back of a translation-front card — corrected: in translation-front mode the revealed back correctly shows the German answer (`lang="de"`); the placeholder belongs on the revealed back of a German-front card and on the translation-front question side, which the corrected test now asserts in both directions.

## 4. Acceptance-to-evidence mapping

| Criterion / AC row | Test path + exact name | Result | Log/artifact | Tested revision |
|---|---|---|---|---|
| AC-04 / LF-CARD — one generic card presentation for A1 and B2, level-owned controls, no verb-only sections | E2E `SC3-A1-SHELL` (shared shell, topbar affordances, level controls, no `.flashcard-inner` alongside, no verbs deck strip) + `SC3-B2-MIXED`/`SC3-B2-UNIT-SWITCH` (same renderer path for B2) + code inspection (`js/core/shared-card.js` unchanged; both adapters call it) | PASS | `final-cefr-cards-chromium.log`, `final-cefr-cards-mobile.log` | 9d9eb61 |
| AC-05 / LF-CARD — revealed back shows first German example AND its actual translation, no term-as-example, no second example, no stale example | E2E `SC3-A1-EXAMPLE` (real A1 card 1-0: exact sentence + exact translation `(Hello, I am Anna.)`), `SC3-A1-SYNTH-NOEX` (no box + stale negatives), `SC3-A1-SYNTH-MULTI` (first only), `SC3-A1-SYNTH-EX-WO-TR` (sentence only, no empty line), `SC3-B2-MIXED` (real mixed example translation), `SC3-B2-EN-ONLY`, `SC3-B2-NOEX`, `SC3-B2-EX-NO-TR`, `SC3-B2-SYNTH-MIXED` | PASS | same | 9d9eb61 |
| AC-06 / LF-CARD — unrevealed card exposes no answer/example/audio via DOM, attributes, or accessible text | E2E `SC3-A1-SECRECY-DOM` (complete subtree sweep + real AX tree), `SC3-A1-SECRECY-AUDIO` (front speak never speaks the hidden German answer; positive post-reveal contrast), `SC3-B2-SECRECY` (B2 sweep + AX tree + post-reveal positive), `SC3-B2-SYNTH-MISSING-TR` (no placeholder leak of the German answer) | PASS | same | 9d9eb61 |
| AC-07 / LF-CARD — pointer, Enter and Space each reveal; duplicate input never double-grades | E2E `SC3-INT-POINTER`, `SC3-INT-ENTER` (exactly one class mutation per press), `SC3-INT-SPACE`, `SC3-INT-CONTROLS` (Enter on a control: control only, zero transitions), `SC3-INT-GRADE-DOUBLE` (Enter on Known: exactly one SRS write, one advance) | PASS | same | 9d9eb61 |
| AC-08 / LF-CARD — audio, favorite and back details cannot flip, grade, or advance | E2E `SC3-INT-BACK-CONTROLS` (back speak, back favorite, example-speech: state-compare + exact expected side effects), `SC3-A1-FAV` (front favorite: no flip/grade/advance), `SC3-A1-SECRECY-AUDIO` | PASS | same | 9d9eb61 |
| AC-09 / LF-CARD — mobile without horizontal overflow, visible focus, 44×44, reduced motion, themes | E2E `SC3-A11Y-FOCUS`, `SC3-A11Y-TARGETS`, `SC3-A11Y-REDUCED-MOTION`, `SC3-THEME` (light/dark surface change + flip still works), `SC3-OVERFLOW` (front + revealed mixed back, runs on both projects incl. Mobile Chrome) | PASS | both final cefr-cards logs | 9d9eb61 |
| AC-10 / LF-NAV — units authoritative, no verbs deck strip, Phrases/Conversation preserved by configuration | E2E `SC3-A1-SHELL` (no deck strip), `SC3-REG-PHRASES-TAB`, `SC3-REG-PHRASE-FLASHCARDS` (legacy card + phrase-counter persistence), `SC3-REG-CONVERSATION`, `SC3-REG-B2-MISSING` (configured empty states), `SC3-REG-A1-UNIT-SWITCH`, `SC3-REG-B2-UNIT-SWITCH`, `SC3-REG-MODE-SWAP`, `SC3-REG-SOURCE-BOUNDARY` | PASS | same | 9d9eb61 |
| Language truthfulness — never label Arabic/mixed as English; never speak Arabic/mixed display text | unit `translationDisplayAttrs` case; E2E `SC3-B2-SYNTH-AR` (rtl + ar front/back/example line; B2 face control never says "English"), `SC3-B2-SYNTH-VOICE` (no Arabic in any captured utterance; mixed speaks English part only; example speaks de-DE), `SC3-B2-MIXED`/`SC3-B2-EN-ONLY` (dir/lang metadata on real data) | PASS | `final-units.log` + both cefr-cards logs | 9d9eb61 |
| Storage/ID compatibility — exact original IDs, original unit persistence, favorites/SRS immediate + refreshed | E2E `SC3-A1-SRS-WRITE` (keys exactly `['1-0','1-1']`, wrong-unit negatives), `SC3-A1-FAV` (favorites `['1-0']` immediate + after reload), `SC3-B2-ID-PERSISTENCE` (exact `45-18` under the B2 key only), `SC3-REG-A1-UNIT-SWITCH` (`2-0`), plus unchanged `srs.spec.js`/`favorites-filters.spec.js`/`activity-streak.spec.js` green | PASS | `final-affected-existing-chromium.log` + cefr-cards logs | 9d9eb61 |
| Explicit words/phrases source boundary (architecture risk clause) | E2E `SC3-REG-SOURCE-BOUNDARY` (word grade after unit switch from phrase view routes to word storage; `knownPhrases`/`favoritePhrases`/`phraseErrors` stay empty), `SC3-REG-PHRASE-FLASHCARDS` (phrase grades route to phrase fields), `SC3-REG-MODE-SWAP` (one card, no duplicate ids); fault probe 5 | PASS | same + `probe5.log` | 9d9eb61 |
| German Verbs shared-card reference unchanged | complete `tests/e2e/cefr-card-reference.spec.js` both projects | PASS 49/49 + 49/49 | `final-reference-chromium.log`, `final-reference-mobile.log` | 9d9eb61 |
| Mandatory RED evidence before production edits | 41 new E2E tests failing at intended assertions + 2 unit cases failing at base | PASS | `red-e2e-chromium.log`, `red-unit.log` | e55debd |
| Fault probes — six assigned families | §7 (5 KILLED + 1 equivalent-mutant SURVIVED with a revised KILLED probe) | PASS | `probe1–6` logs | 9d9eb61 (disposable worktree) |
| Complete tracked suite with honest failure classification | 284 passed / 3 failed / 1 skipped — words-audio both projects (pre-existing, identical at base and final) + verb-guided R1 chromium (SC-INFRA-01 flake, passes isolated) | PASS with declared baseline failures | `final-full-tracked-suite.log`, `final-words-audio-isolated.log`, `final-gc-r1-isolated.log` | 9d9eb61 |

## 5. Complete command ledger

Every test command attempted during this package, in execution order. Working directory: the repo root. `exit` is the command's own exit code; artifacts live in `docs/cefr/evidence/SHARED-CARD-003/01/`.

| Command (exact) | Phase + revision | Duration | Exit | Passed/failed/skipped | Artifact | Interpretation |
|---|---|---|---|---|---|---|
| `node /home/z/my-project/scripts/sc003-data-scan.mjs` | characterization, base `e55debd` | <1s | 0 | — | `data-characterization.log` | Real-dataset language/example edge-case inventory used to plan real-vs-synthetic coverage |
| `npx playwright test tests/e2e/words-audio.spec.js --project=chromium` | mandatory baseline measurement, base `e55debd` | ~30s | 1 | 0/1 | `baseline-words-audio-chromium.log` | words-audio:48 fails at base (line 77 highlight-clear timing) — the known pre-existing failure measured before edits |
| `npx playwright test tests/e2e/srs.spec.js tests/e2e/activity-streak.spec.js tests/e2e/favorites-filters.spec.js --project=chromium` | baseline, base `e55debd` | 6.3s | 0 | 6/0 | `baseline-affected-chromium.log` | Protected behavior green before edits |
| `npx playwright test tests/e2e/phrases-conversations.spec.js --project=chromium` | baseline, base `e55debd` | 29.3s | 0 | 11/0/1 skip | `baseline-phrases-chromium.log` | Phrase/conversation behavior green before edits (skip = pre-existing desktop skip of the mobile-only test) |
| `npm run test:units` | baseline, base `e55debd` | ~3s | 0 | 73/0 | `baseline-units.log` | Units green before edits |
| `node --test tests/unit/flashcards.test.mjs` | RED, base `e55debd` | <1s | 1 | 3/2 | `red-unit.log` | Both new unit cases fail at their intended targets (helpers absent); 3 pre-existing pass |
| `npx playwright test tests/e2e/cefr-cards.spec.js --project=chromium --timeout=9000` | RED, base `e55debd` | 6.4m | 1 | 4/41 | `red-e2e-chromium.log` | 41 new tests fail at intended assertions (predominantly the shared-card shell visibility); the 4 passes are exactly the protected legacy behaviors. Reduced per-test timeout used for the RED run only, so absent locators fail fast; the intended failing assertions are unaffected |
| `node --input-type=module --check < js/core/{flashcards,nav-service}.js` | syntax gate, working tree | <1s | 0 | — | (inline) | TS-LOOP-001 module-mode parse; repeated after every edit |
| `node --test tests/unit/flashcards.test.mjs` | iterate, working tree | <1s | 1→0 | 0/5 → 5/0 | (overwritten) | First post-edit run: the VM loader strips single-line `import` statements, so flashcards.js's multi-line shared-card import broke the sandbox — fixed by matching the file's original single-line import style (production fix, not a test change); then 5/5 after the VM-prototype comparison fix in the new unit case (values copied into host objects before `deepEqual`) |
| `npx playwright test tests/e2e/cefr-cards.spec.js --project=chromium --grep "A1 ordinary card"` | iterate ×3, working tree | ~30s each | 1→0 | 4/8 → 12/0 | (overwritten) | Three stabilization findings, each fixed at its root: (1) missing `#fc-de` compatibility id on the words front term (production fix); (2) `installSpeechCapture` was not awaited in `prepareLevelPage`, letting `goto` race the init-script registration (test-infra fix in this package's spec; the same latent pattern exists in the reference spec where it has not manifested); (3) no test-expectation changes in this group |
| `npx playwright test tests/e2e/cefr-cards.spec.js --project=chromium --grep "B2 ordinary card"` | iterate ×2, working tree | ~30s each | 1→0 | 11/1 → 12/0 | (overwritten) | `SC3-B2-SYNTH-MISSING-TR` corrected (translation-front back correctly shows the German answer; see §3 ledger) |
| `npx playwright test tests/e2e/cefr-cards.spec.js --project=chromium --grep "interaction and accessibility"` | iterate, working tree | 15.6s | 0 | 14/0 | (overwritten) | Green on first run after the fixes above |
| `npx playwright test tests/e2e/cefr-cards.spec.js --project=chromium --grep "regression"` | iterate ×3, working tree | ~10s each | 1→0 | 5/2 → 7/0 | (overwritten) | Back-to-List locator scoping, unit-2 count 43, pre-reveal `#fc-en` count 0 (see §3 ledger) |
| `npx playwright test tests/e2e/cefr-cards.spec.js --project=chromium` | Stage A verification (33-test Stage A spec; B2 groups + B2 label excluded) | 49.6s | 0 | 33/0 | (overwritten by the full-spec runs) | Stage A stabilized and committed as `9b31bd5` |
| `npm run test:units` | Stage A verification | ~3s | 0 | 75/0 | (same content as `final-units.log`) | Stage A green |
| `npx playwright test tests/e2e/cefr-cards.spec.js --project=chromium --grep "B2 ordinary card"` | Stage B verification, restored full spec | 29.4s | 0 | 12/0 | (overwritten by full runs) | Stage B stabilized and committed as `9d9eb61` |
| `npx playwright test tests/e2e/srs.spec.js tests/e2e/activity-streak.spec.js tests/e2e/favorites-filters.spec.js tests/e2e/phrases-conversations.spec.js --project=chromium` | affected regression, working tree = `9d9eb61` content | 37.5s | 0 | 17/0/1 skip | (same content as final affected log) | Every existing affected suite green UNCHANGED |
| `npm run test:units` | final ladder, `9d9eb61` | ~3s | 0 | 75/0 | `final-units.log` | All tracked units incl. 2 new cases |
| `npx playwright test tests/e2e/cefr-cards.spec.js --project=chromium` | final ladder | 1.3m | 0 | 45/0 | `final-cefr-cards-chromium.log` | Complete new spec, Chromium |
| `npx playwright test tests/e2e/cefr-cards.spec.js --project="Mobile Chrome"` | final ladder | 1.1m | 0 | 45/0 | `final-cefr-cards-mobile.log` | Complete new spec, Mobile Chrome |
| `npx playwright test tests/e2e/cefr-card-reference.spec.js --project=chromium` | final ladder | 1.2m | 0 | 49/0 | `final-reference-chromium.log` | German Verbs shared-card reference regression |
| `npx playwright test tests/e2e/cefr-card-reference.spec.js --project="Mobile Chrome"` | final ladder | 1.2m | 0 | 49/0 | `final-reference-mobile.log` | Same, Mobile Chrome |
| `npx playwright test tests/e2e/srs.spec.js tests/e2e/activity-streak.spec.js tests/e2e/favorites-filters.spec.js tests/e2e/phrases-conversations.spec.js tests/e2e/words-audio.spec.js --project=chromium` | final ladder (affected existing incl. words-audio) | 40.7s | 1 | 17/1/1 skip | `final-affected-existing-chromium.log` | Only words-audio:48 fails — byte-identical to the base measurement |
| `npx playwright test tests/e2e/words-audio.spec.js:48 --project=chromium` | failure protocol: single isolation rerun | ~30s | 1 | 0/1 | `final-words-audio-isolated.log` | Consistent in isolation → pre-existing (same class declared at `7747d3c` and `3af8219`); NOT counted as a pass |
| `npx playwright test` | complete tracked suite, once after stabilization, `9d9eb61` | 8.5m | 1 | 284/3/1 skip | `final-full-tracked-suite.log` | 284 = 194 previously passing + all 90 new tests. Failures classified below; no rerun of the broad set was needed (no correction occurred) |
| `npx playwright test "tests/e2e/verb-guided-challenge.spec.js:1036" --project=chromium` | failure protocol: single isolation rerun | 2.5s | 0 | 1/0 | `final-gc-r1-isolated.log` | R1 passes isolated → SC-INFRA-01 parallel-suite flake class (verbs/guided production untouched by this package) |
| probe runs 1–6 (§7; disposable worktree at `9d9eb61`, tests unmodified) | fault probes | ~10–35s each | 1 (except probe 4a: 0) | as recorded | `probe1.log` … `probe6-e2e.log` | 5 KILLED + 1 equivalent-mutant SURVIVED (revised probe KILLED); byte-identical restores verified after every probe |
| `git diff --check` / `git diff --cached --check` / `git status --short` | gates at every commit and at the end | <1s | 0 | — | INDEX notes | Clean at every stage; final tree clean at `9d9eb61` |

Background/duplicate runs: none (TS-LOOP-006/PX-008) — every command above is a single sequential execution; the documented `--grep` iterations were distinct diagnostic runs, not reruns of a broad suite. Zero-selection checks: every `--grep` was verified non-zero by its own pass/fail counts. Final evidence freshness: no source/test/fixture/config edit occurred after `9d9eb61` (the only later commits are docs-only), so all final logs remain valid for the delivered code revision.

## 6. Regression and integration

- Ordinary A1 words: complete new spec green on both projects — shell, real first example + translation, faces, secrecy (DOM + AX tree + audio), favorites (immediate + refreshed), exact-ID SRS writes with wrong-unit negatives, filters/shuffle/nav/counter, completion + restart, unit switching.
- Ordinary B2 words: real mixed/en-only/no-example/example-without-translation coverage with exact dir/lang metadata, exact B2 ID persistence under `german_app_progress_german-b2-app` only, unit switching, missing-translation placeholder, voice safety.
- Ordinary Verbs (reference experience): `cefr-card-reference.spec.js` 49/49 on Chromium and Mobile Chrome — the shared renderer and the verbs page are unchanged (`git diff e55debd..9d9eb61` touches no verbs file).
- Guided Verbs: covered by the complete tracked suite (only the R1 SC-INFRA-01 parallel flake, isolated pass, no product change).
- Phrases / Conversation: `SC3-REG-PHRASES-TAB`, `SC3-REG-PHRASE-FLASHCARDS` (legacy card flips via `.flashcard-inner`, grades write `P-a1-0-*` ids into `knownPhrases` with `known` empty), `SC3-REG-CONVERSATION`, `SC3-REG-B2-MISSING` (configured 404 empty states), `SC3-REG-MODE-SWAP`, `SC3-REG-SOURCE-BOUNDARY`, plus the full unchanged `phrases-conversations.spec.js` (11 pass/1 skip) — behavior preserved exactly.
- Legacy storage / refresh / level and account isolation: no storage key, schema, or write-path change; the A1 and B2 storage keys remain distinct and asserted (`SC3-B2-ID-PERSISTENCE` verifies no cross-level write); refresh persistence proven by `SC3-A1-FAV` (favorite survives reload) and the unchanged `srs.spec.js` SRS progression/time-travel flow.
- Actual audio adapter text AND language: captured at the browser adapter boundary. German example sentences submit the exact sentence with `de-DE` (`SC3-INT-BACK-CONTROLS`, `SC3-B2-SYNTH-VOICE`); the front/back speak action submits only the visible side's safe text and never Arabic/mixed display text (`SC3-A1-SECRECY-AUDIO`, `SC3-B2-SYNTH-VOICE`, unit LEVEL-DATA-012/013/014). Known limitation inherited from BASELINE gap 2 (AUDIO-003 scope): `window.app.speakText` still drops its language argument, so English prompt text is spoken through the German-default voice — the wrapper receives the correct safe text and language tag, and the utterance-language integration gap remains assigned to AUDIO-003 (not silently waived, not fixed without authorization).
- Browser/module startup and console errors: `SC3-CONSOLE` asserts zero console errors/page errors on both level pages with the shared card; `phrases-conversations.spec.js` console-error assertions green.
- Desktop/mobile/themes/keyboard/reduced-motion: `SC3-A11Y-FOCUS` (real Tab walks + `:focus-visible` + technique-agnostic indicator comparison), `SC3-A11Y-TARGETS` (44×44 incl. the nav buttons via the new scoped minimum), `SC3-A11Y-REDUCED-MOTION` (transition 0s + rule present + flip still works), `SC3-THEME` (dark surface change + flip works + `darkMode` persisted), `SC3-OVERFLOW` (no horizontal overflow front + revealed mixed back), all duplicated on Mobile Chrome by the full-spec run.
- Accessibility tree: real CDP `Accessibility.getFullAXTree` evidence — pre-reveal translation-front trees expose no German answer (`SC3-A1-SECRECY-DOM`, `SC3-B2-SECRECY`); post-reveal, exactly one "Speak word" button lives on the displayed back while the inert front is excluded (`SC3-A11Y-INERT`).
- Untested/blocked: real-device/Safari (Mobile Chrome is emulation); hardware voice output (deterministic speechSynthesis double only); the real B2 example-without-translation sentence cards exist only at deep queue positions (69-80 reached through the app's own `nextCard` navigation API — documented in the test); the words-audio glossary highlight-clear failure is out of scope (below).

## 7. Test-quality and fault-probe evidence

| Probe | Risk/contract | Production target + exact mutation | Detecting test | Baseline / syntax result | Actual failure | Classification | Integrity proof |
|---|---|---|---|---|---|---|---|
| 1 | Example-source substitution / AC-05 | `flashcards.js` `_buildWordsBackHtml`: `sourceText: example.de` → `sourceText: w.de` | E2E `SC3-A1-EXAMPLE` | passing; syntax OK | `toHaveText('💬 Hallo, ich bin Anna.')` — the card showed the vocabulary term instead of the real example | **KILLED** | sha256 restored byte-identical |
| 2 | Secrecy removal / AC-06 | `flashcards.js` `_renderWordsCard`: `backHtml` built unconditionally (answer rendered before reveal) | E2E `SC3-A1-SECRECY-DOM` | passing; syntax OK | subtree sweep found 19 leak carriers (`Hallo!` + `Hallo, ich bin Anna.` in text and `attr:[data-text]`) | **KILLED** | restored byte-identical |
| 3 | Wrong-level/unit persistence / AC-17 analog | `flashcards.js` `mark()`: srsData written under `'9-99'` instead of `w.id` | E2E `SC3-A1-SRS-WRITE` | passing; syntax OK | exact-ID `toEqual(['1-0'])` failed (wrong-unit key present) | **KILLED** | restored byte-identical |
| 4a | Duplicate-event guard (click path) / AC-07 | `flashcards.js` mount click `flip` branch: `if (!e.target.closest('button'))` guard removed | `SC3-A1-SECRECY-AUDIO` | passing; syntax OK | no failure | **SURVIVED — equivalent mutant**: control clicks already resolve to the control's own `data-action` through `closest('[data-action]')`, so the guard is redundant defense-in-depth; investigated per TS-MUT-005, no coverage gap exists (the load-bearing guard is probed in 4b); no expected result was rewritten |
| 4b | Duplicate-event guard (keyboard path) / AC-07 | `flashcards.js` keydown handler: `if (target.closest('button, a, select, input, textarea')) return;` removed | E2E `SC3-INT-CONTROLS` | passing; syntax OK | `expect(calls).toHaveLength(1)` / `__flipMutations === 0` failed — one Enter on a focused control no longer ran only that control | **KILLED** | restored byte-identical |
| 5 | Phrase/word source isolation / architecture-risk clause | `nav-service.js` `switchUnit`: `state.flashcardSource = 'words'` sync removed (legacy desync restored) | E2E `SC3-REG-SOURCE-BOUNDARY` | passing; syntax OK | `waitForFunction` for `data.known.length === 1` timed out — the word grade was routed into `knownPhrases` | **KILLED** | `nav-service.js` restored byte-identical |
| 6 | Direction/language mislabeling / AC-03 + LF-DATA | `flashcards.js` `translationDisplayAttrs`: always `{ dir: 'ltr', lang: 'en' }` | unit `translationDisplayAttrs` case AND E2E `SC3-B2-SYNTH-AR` | passing; syntax OK | unit: ar→rtl deepEqual failed; E2E: `toHaveAttribute('dir', 'rtl')` failed | **KILLED** | restored byte-identical |

Probe integrity: all mutations applied only to production files inside a disposable Git worktree at `9d9eb61` (tests, fixtures, config, runner untouched — TS-MUT-002/007); `js/core/flashcards.js` = `b2a9de3b…3add` and `js/core/nav-service.js` = `ce448a83…1cf7e` recorded before the probes and re-verified byte-identical after each restore; the worktree was removed afterwards and the main tree stayed clean. Six distinct failure families covered (TS-MUT-006): example source, secrecy, wrong-unit writes, duplicate events (both the redundant and the load-bearing guard investigated), source isolation, language metadata. RED evidence proves every new E2E group fails at its intended assertion against the legacy presentation before the change.

Additional test-quality notes: expected values are authored independently from the real datasets (exact sentences, ids, counts verified by the pre-edit characterization scan); no test uses the implementation as its own oracle; no conditional assertions, blanket skips, snapshot regeneration, timeout increases, or sleeps were introduced; the AX-tree evidence uses Chrome's real accessibility tree (CDP) because Playwright role selectors do not exclude `inert` subtrees (proven in the C1 package); the speech double captures utterance text and language exactly as `tts.js` submits them.

## 8. Dead-code and dependency inventory

Search scope: `rg` across `js/`, `css/`, `*.html`, `tests/` for every legacy card symbol, selector, and handler touched by this migration, plus the delegated-action inventory (DC-002).

| Candidate symbol/path | Searches + dynamic caller checks | Classification | Disposition | Regression proof |
|---|---|---|---|---|
| `#active-flashcard` | level.html (legacy phrase card), flashcards.js (legacy flip branch + words alias assignment), srs.spec.js/activity-streak.spec.js (words-mode clicks), cefr-cards.spec.js | DYNAMICALLY_USED (legacy phrase card id + compatibility alias on the words flip surface) | Kept | srs/activity-streak green unchanged |
| `.flashcard-inner`, `.flashcard`, `.flashcard-front/back`, `.fc-german`, `.fc-english`, `.fc-example` | core.css, level.html, flashcards.js legacy path, phrases-conversations.spec.js | DYNAMICALLY_USED (phrases presentation — deliberately preserved) | Kept | phrases-conversations 11/1 skip green; `SC3-REG-PHRASE-FLASHCARDS` |
| `#fc-ex-de`, `#fc-ex-en`, `#fc-type`, `#fc-type-back`, `#fc-fav-badge(-back)`, `#fc-srs-dots(-back)`, `#fc-speak(-back)` | level.html static markup + flashcards.js `_renderLegacyCard` writes | DYNAMICALLY_USED (phrase card only; the words card uses the shared affordances) | Kept | same |
| `state.flashcardSource` | app.js `_save`, nav-service (set at both source transitions + unit switch), stats-service.js `_getUnitProgress` (read-only file outside this package's scope) | DYNAMICALLY_USED | Kept and now synced at every transition; the engine's own `source` is the rendering boundary — both are set together at the same call sites | `SC3-REG-SOURCE-BOUNDARY`, probe 5 |
| `#fc-card-mount`, `_legacyCardHtml`, `firstExample`, `translationDisplayAttrs`, `.fc-card-*`, `.fc-srs-row` | all new symbols exercised by the new tests and production paths | DYNAMICALLY_USED | Kept | cefr-cards spec 45/45 both projects |
| `window.app.flipCard/speakCurrentCard/toggleCurrentCardFavorite` | app.js wrappers — `flipCard` still used by the phrase card's inline onclick; the other two remain public app API used by the legacy card's inline handlers and available to callers | DYNAMICALLY_USED | Kept | phrase flashcard test flips via the legacy surface |
| Removed legacy words-render code | the words branch of the old `render()` body was replaced by the shared path; every removed line's remaining consumers were checked (SRS dot markup regenerated by `_srsDotsInnerHtml` for both paths; example-slot fill `w.context ? w.de : ''` had no other consumer) | PROVEN_ORPHAN (created by this change — the words presentation's legacy internals) | Removed with the migration | complete new spec + all affected existing suites green |

No new dependency, lockfile, Playwright configuration, or framework change; no IDs, content, or storage shapes touched (`git diff e55debd..9d9eb61` = the six permitted files). Surviving duplication, reported for a future REFACTOR package: the legacy phrase-card presentation coexists with the shared card by explicit design (the task forbids changing phrase behavior); the flip-mechanics CSS exists both in `verbs.html` inline styles and, scoped to `#fc-card-mount`, in `css/core.css` (verbs.html is outside this package's permitted paths, so consolidation belongs to a later authorized package).

## 9. Findings, limitations, and handoff

| Finding ID | Severity/impact | Reproduction and evidence | Disposition | Owner decision needed |
|---|---|---|---|---|
| Legacy stale-source persistence desync (word grades after a unit switch from the phrase-flashcard view written into `knownPhrases`/`favoritePhrases`/`phraseErrors`) | high (data corruption; pre-existing) | reproduced by the RED run of `SC3-REG-SOURCE-BOUNDARY` at base `e55debd`; fixed by the explicit source boundary; guarded by the test and fault probe 5 | **fixed** | none (reviewer confirmation) |
| words-audio baseline failure | medium (evidence hygiene) | `baseline-words-audio-chromium.log` (base) vs `final-affected-existing-chromium.log` + `final-words-audio-isolated.log` (final): fails byte-identically at `words-audio.spec.js:77` on both revisions, consistent in isolation; same class already declared at `7747d3c` and `3af8219` | **open (pre-existing, proven at this package's base and final revision)** — words-page glossary audio highlight-clear timing, outside this package's permitted scope; never counted as a pass | owner waiver or separate fix assignment (AUDIO-003-adjacent) |
| verb-guided R1 chromium suite failure | low | failed only inside the 8.5m parallel tracked suite; passes isolated (`final-gc-r1-isolated.log`); same family as the GC-07 Mobile Chrome flake documented as SC-INFRA-01 in SHARED-CARD-002-C1-01 | **open (pre-existing infra flake)** | optional triage |
| `window.app.speakText` drops its language argument (English prompt text spoken through the German-default voice) | medium (known gap) | `SC3-A1-SECRECY-AUDIO` / probe logs: utterance `{"text":"Hello!","lang":"de-DE"}`; wrapper receives `('Hello!', 'en')` | **deferred** — BASELINE gap 2 explicitly assigned to AUDIO-003; safe text selection at the wrapper boundary is proven, wrong-language display text is never spoken | AUDIO-003 assignment |
| B2 unit-70 "examples" are grammar notes, not sentences | low (data property, not a defect) | `data-characterization.log`: unit 70 rows carry note text in the example slot | **deferred** — rendered truthfully as the data provides; content changes are forbidden without authorization | optional content triage |

- Storage/ID compatibility evidence: no schema, key, or write-path change; grading writes the exact original unit-position ids (`1-0`, `1-1`, `2-0`, `45-18`, `P-a1-0-*` for phrases) to the same per-app keys; favorites and SRS survive reload; A1 and B2 storage remain isolated (`SC3-B2-ID-PERSISTENCE` asserts the A1 key stays untouched); the unchanged `srs.spec.js` SRS progression and legacy `storage.test.mjs` units stay green.
- Phrase/Conversation regression evidence: §6 — phrase flashcards keep their legacy card and phrase-scoped persistence; conversation scenes render; B2's configured missing-content states render; the complete unchanged `phrases-conversations.spec.js` is green.
- Limitations and untested cases: Mobile Chrome is emulation (no real-device/Safari certification); speech assertions use a deterministic speechSynthesis double (no hardware voice); the B2 synthetic fixtures are route-intercepted modules (never edits to real vocabulary); the deep-queue real card 69-80 is reached through the app's own navigation API rather than 80 manual clicks; visual QA was performed through computed-style and structural assertions plus the existing theme/overflow checks, not human screenshots.
- Remaining risks: the compatibility-id aliasing (`#active-flashcard`, `#fc-de`, `#fc-en`, `#fc-srs-dots` shared between the two card presentations — exactly one card exists in the DOM at any time, asserted by `SC3-REG-MODE-SWAP`) couples the new card to legacy selectors until a later package migrates the phrase card; the coexisting flip-mechanics CSS (see §8) is deliberate consolidation debt.
- Final diff/status: implementation commits `9b31bd5` (Stage A: shared integration + A1/synthetic/interaction/regression tests + unit tests) and `9d9eb61` (Stage B: B2 test groups + the B2 truthful translation-face label) touch exactly the permitted files; `git diff --check` and `git diff --cached --check` clean at every commit; the report/evidence commit is docs-only.
- The executor cannot approve its own work: this report claims READY_FOR_REVIEW only; acceptance is the owner's decision.

## 10. Owner/reviewer disposition — not executor approval

- Reviewer and reviewed exact revision: pending review of code `9d9eb61717500062929a4f83e311a428e9163666` (Stage A `9b31bd5…` + Stage B `9d9eb6…`) and this delivery branch tip (docs).
- Verdict: pending
- Criteria/findings accepted or declined; explicit waivers and reason: for the reviewer — (1) the pre-existing words-audio failure is proven identical at this package's base and final revision and needs an explicit waiver or a separate fix assignment; (2) the R1 SC-INFRA-01 flake remains open as characterized; (3) the AUDIO-003 speakText language-forwarding gap is deferred by design; (4) the stale-source desync fix (a behavior correction inside the assigned boundary clause) and the B2 translation-face relabel are presented for confirmation. This report does not approve itself.
