# AUDIO-001-C1 — Attempt 01 Delivery Report

## 1. Identity and status

- Status: READY_FOR_REVIEW (all engineering work complete and evidenced; the final delivery push is executed after this docs-only commit — final status is returned in the handoff message)
- Assigned goal and task type: correct the delivered AUDIO-001 speech-sequence planner on exactly three owner review findings — (1) read the REAL normalized metadata `card.translationLanguage` / `speechText.{de,en,ar}` instead of the synthetic `language` property (examples already used `example.translationLanguage` and stay unchanged; no redundant `language` alias may be added to production level data); (2) preserve the existing Verbs autoplay repeat semantics (`js/core/verbs-engine.js`: outer loop = selected items, inner loop = repeats of the current item, each repetition carrying that item's term, requested translation, and requested examples before the next repetition or item); (3) stable source indexing (`itemIndex` = the item's ORIGINAL index in the input array; `startIndex` never re-bases indices; `itemId` unchanged). Task type: **BUGFIX** (correction of the delivered AUDIO-001 attempt; CM gates applied: CM-PRE-001..006, CM-FIX-001..005, CM-MOD-002/003 for superseded tests).
- Owner assignment reference: owner task message "TASK: AUDIO-001-C1 — Correct the normalized-data boundary, repeat semantics, and stable source indexing" with required correction base `9c925aca406f1b7465959872f5545f7a30e177eb`, assigned branch, checkpoint gate, authorized write scope, mandated test items 1–11, three mandated fault probes, verification order, and delivery rules; resume message re-supplied the GitHub PAT after an AUTHENTICATION_BLOCKED gate (recorded in the sandbox worklog).
- GitHub task branch: `codex/glm-audio-001-c1-01`; correction base commit: `9c925aca406f1b7465959872f5545f7a30e177eb` (delivered AUDIO-001 attempt 01); commits: tests `7794504` (RED regression tests, committed first per DR-007 — the planner in that commit is still the uncorrected base version), final tested code `2f51c07` (the corrected planner). This docs-only commit adds the report and evidence and does not move the tested code revision.
- Evidence index: `docs/cefr/evidence/AUDIO-001-C1/01/INDEX.md`
- Final delivered SHA is returned after commit/push in the handoff message, not embedded into its own commit.
- Executor/model and sandbox OS/runtime versions: GLM executor (Super Z), Debian sandbox, Node v24.19.0, npm 11.17.0. No Playwright, browser, Python server, or network was used (pure unit-level package, per the assignment).
- Start/end time: 2026-08-31 ~21:05–21:35 UTC (sandbox clock); work directory: repository root.
- Dependency acceptance references: the correction base is the delivered AUDIO-001 attempt 01 (`9c925ac`), itself based on accepted SHARED-CARD-003-C2 (`8e8d3f3`).

## 2. Required reading and pre-edit plan

- Paths fully read this attempt (in the AGENTS.md order): `AGENTS.md`; `contracts/README.md`; `contracts/PORTABLE_AGENT_EXECUTION.md`; `contracts/CHANGE_MANAGEMENT.md`; `contracts/CODE_FINGERPRINT.md`; `contracts/TESTING_AND_SUCCESS.md`; `contracts/DEAD_CODE_AND_REFACTORING.md`; `contracts/DELIVERY_REPORTING.md`; `contracts/LEVEL_FLASHCARD_STANDARD.md`; `docs/cefr/BASELINE.md`; `docs/cefr/WORK_PACKAGES.md` (AUDIO-001..003 + catalog); `docs/cefr/ACCEPTANCE_MATRIX.md` (AC-03/AC-11 rows and reviewer procedure); `docs/cefr/GLM_HANDOFF.md`; `docs/cefr/GITHUB_DELIVERY.md`. Read additionally: the prior attempt's `docs/cefr/reports/AUDIO-001-01.md` and `docs/cefr/evidence/AUDIO-001/01/INDEX.md` (finding context + format), and the implementation boundary (read-only): `js/core/speech-plan.mjs` (base version), `tests/unit/speech-plan.test.mjs` (base version), `js/core/verbs-engine.js` (the `playAllVerbsAudio` repeat semantics), `js/levels/a1.config.js` + `js/levels/b2.config.js` (the actual parsers and normalized card shapes), `js/core/level-data-validator.mjs` references via `tests/unit/level-data.test.mjs` (VM loader pattern + real dataset regression fixtures), `package.json`.
- Relevant contract IDs: FP-DESIGN-010 (speech-sequence planning is a pure calculation), FP-JS/FP-DIFF (surgical diff), LF-DATA (normalized card shape: `translationLanguage`, `speechText.{de,en,ar}`), LF-AUDIO (explicit language fields; never mixed text to a single-language voice; skip+report), AC-03 (slash alternatives; Arabic-only/mixed separation), AC-11 (deterministic steps with stable item identity), TS-PRE-003 (RED before GREEN), TS-TEST-005 (independent oracles), TS-MUT-001..007 (probe discipline), DR-007 (tests commit first), DR-011 (corrections are bounded and ID-linked), PX-005/PX-006 (authorized write scope).
- Observed conflicts: none. The three findings are consistent with the contracts; the first attempt's planner diverged from the real parser output shape.
- Before behavior (at the correction base): term translations followed the synthetic `card.language` property that NO real A1/B2 parser-produced card exposes (so all term-translation steps would be silently dropped on real data); the loops repeated the whole list (all items repeat 0, then all items repeat 1), contradicting the Verbs controller's item-major order; `itemIndex` was re-based to 0 after `startIndex` (`items.slice(startIndex)` + local index), losing the original source index that verbs-engine preserves via `verb.index`. Approved after behavior: the planner reads `card.translationLanguage`, keeps `example.translationLanguage` for examples, iterates items in the outer loop with repeats of the current item in the inner loop, and reports the item's original input-array index; `itemId` is unchanged throughout.
- Explicit non-goals: no browser controller rewrite, no changes to `app.js`/`verbs-engine.js`/`tts.js`/`flashcards.js`, no HTML/CSS, no level-configuration or content changes (in particular NO `language` alias added to production level data), no AUDIO-002 wiring, no new dependencies, no contract changes.
- Exact allowed write paths (all writes stayed inside them): `js/core/speech-plan.mjs`, `tests/unit/speech-plan.test.mjs`, `docs/cefr/reports/AUDIO-001-C1-01.md`, `docs/cefr/evidence/AUDIO-001-C1/01/**`. The pre-edit plan (base/branch/scope/fixture strategy/probes/ladder) was reported in-conversation before editing.
- Baseline tests and known failures: at the correction base, `npm run test:units` passes 105/105 (the delivered AUDIO-001 state); no pre-existing failures relate to this correction.

## 3. Changes and rationale

| File | Change/purpose | Contract/WP criterion | Compatibility impact |
|---|---|---|---|
| `js/core/speech-plan.mjs` (+29/−12 lines) | Term translation now reads `card.translationLanguage` (the field the real A1/B2 parsers expose); loop nesting swapped to items-outer/repeats-inner matching `verbs-engine.js` `playAllVerbsAudio`; `itemIndex` iterates original input-array indices from `startIndex` (no `slice`, no re-base); JSDoc and invariant comments updated (including the `items` shape: `translationLanguage` in {'en','ar','mixed',null}). | LF-DATA; LF-AUDIO; AC-11; C1 findings 1–3; FP-DESIGN-010 | None — the module still has zero production importers (AUDIO-002 is the assigned consumer); the option/warning/error surface is unchanged |
| `tests/unit/speech-plan.test.mjs` (rewritten, 660 lines, 40 tests) | Fixtures now mirror the real normalized card shape and never set a `language` property; `termCard()` uses `translationLanguage`; the actual A1/B2 parsers are loaded in a VM sandbox (the `level-data.test.mjs` pattern) and the tests exercise cards produced by those real parsers — synthetic raw rows AND real shipped dataset cards (`1-0` Hallo!, `19-74` die EDV with slash alternatives + mixed example); every expected steps array is a hand-written literal (parser-card expectations transcribe the raw source rows). | TS-TEST-005; owner test items 1–11; AC-03/AC-11 | The unit-suite total changes 105 → 115 (75 pre-existing + 40 corrected planner tests) |
| `docs/cefr/evidence/AUDIO-001-C1/01/**` + this report | Sanitized evidence and the delivery report | DR-001..008 | None |

Root cause: the first attempt invented a `language` property on cards because the planner was designed against hand-shaped fixtures instead of the parsers' actual output (`js/levels/a1.config.js` line 740 and `js/levels/b2.config.js` line 3135 set `translationLanguage`, never `language`); it also chose repeat-major ordering and index re-basing on its own instead of matching the existing Verbs autoplay controller that AUDIO-002 must adapt to. The correction aligns the planner with the real normalized boundary (`translationLanguage` values 'en'/'ar'/'mixed'/null map exactly onto the existing `emitTranslationSteps` branches), with `verbs-engine.js`'s documented outer-items/inner-repeats iteration (which also keeps `verb.index` stable — the planner now keeps `itemIndex` stable the same way), and with stable source indexing for UI highlighting.

Tests or old assertions intentionally superseded (CM-MOD-002/003): (a) old *repeatCount=2 with two items emits term-1, term-2, term-1, term-2 (outer repeat, inner item)* — replaced by the item-major Verbs-order test; (b) old *startIndex in the middle plays from that item to the end with itemIndex re-based at 0* — replaced by the original-indices test; (c) all fixtures built through the synthetic `language` property — replaced by `translationLanguage` fixtures; every other old test intent (zero/one/many, example modes, include-translation on/off, empty-translation warning, slash alternatives, Arabic/mixed term+example separation, out-of-range start, 7 invalid-option cases, immutability, determinism) is preserved with an `AUDIO-001-C1:` name prefix.

Scope deviations: none in production scope. One test-fixture correction during the RED cycle is disclosed: the real B2 dataset card was first looked up positionally (`vocabulary[18][0]`), which returned the wrong card because raw B2 rows are not unit-sorted; the lookup now finds the card by its display translation (`'IT / edv'`), the same pattern `level-data.test.mjs` uses, and the tests commit (still local and unpushed at that point) was amended to `7794504` before the implementation commit. The RED evidence was re-captured in full after the amendment (17 failed / 23 passed, unchanged counts — the corrected fixture fails for the intended missing-term-translation reason).

## 4. Acceptance-to-evidence mapping

| Criterion / AC row / contract ID | Test path + exact name | Result | Log/artifact | Tested revision |
|---|---|---|---|---|
| Finding 1: A1-shaped normalized card, `translationLanguage: 'en'` (owner item 1) | *normalized A1-shaped card with translationLanguage "en" emits term then English term-translation* | PASS | `04-green-focused.log` | 2f51c07 |
| Finding 1: B2 mixed card, `translationLanguage: 'mixed'` (owner item 2) | *normalized B2 mixed card with translationLanguage "mixed" emits term, en, then ar term-translations* | PASS | same | 2f51c07 |
| Finding 1: Arabic-only normalized card (owner item 3) | *Arabic-only normalized card with translationLanguage "ar" emits term then Arabic term-translation, never English* | PASS | same | 2f51c07 |
| Finding 1: actual A1 parser card (owner item 4) | *a card produced by the actual A1 parser plans term, en term-translation, example, and en example-translation steps* | PASS | same | 2f51c07 |
| Finding 1: actual B2 parser mixed + Arabic cards (owner item 5) | *a mixed card produced by the actual B2 parser…*; *an Arabic-only card produced by the actual B2 parser…* | PASS | same | 2f51c07 |
| Real shipped dataset cards (stronger form of owner items 4–5) | *a real A1 dataset card plans…* (`1-0`); *a real B2 dataset card keeps slash alternatives in one en step and splits its mixed example into en and ar steps* (`19-74`, AC-03) | PASS | same | 2f51c07 |
| Finding 1: `card.language` neither required nor introduced (owner item 6) | *a stale synthetic card.language property is ignored; term translation follows card.translationLanguage*; *card.language is neither required nor introduced on normalized cards* (hasOwnProperty checks on all parser cards + immutability) | PASS | same | 2f51c07 |
| Finding 2: two-item repeat ordering matches Verbs (owner item 7) | *two items with repeatCount 2 emit item 1 both repeats then item 2 both repeats (Verbs autoplay order)* | PASS | same | 2f51c07 |
| Finding 2: repeat ordering including term translation and examples (owner item 8) | *each item repetition contains its term, requested translation, and requested examples before the next repetition or item* (16-step literal) | PASS | same | 2f51c07 |
| Finding 3: startIndex in the middle retains original indices (owner item 9) | *startIndex in the middle retains the original item indices 1 and 2 without re-basing* | PASS | same | 2f51c07 |
| Finding 3: stable item IDs and original indices across repeats (owner item 10) | *item ids and original indices stay stable across repeats after a startIndex* | PASS | same | 2f51c07 |
| Existing coverage preserved (owner item 11): zero/one/many; example modes; includeTranslation on/off; missing-text (empty en translation warning + empty German term warning, never substituted); language separation (slash alternatives intact; mixed example → en + ar separate steps); out-of-range start; 7 invalid-option throws; purity (immutability, determinism) | the remaining 25 tests in `tests/unit/speech-plan.test.mjs` (names listed in `04-green-focused.log`) | PASS | same | 2f51c07 |
| RED before GREEN (TS-PRE-003 / CM-FIX-004) | full corrected test file against the uncorrected base planner in a disposable worktree at `7794504` | **17 failed / 23 passed, exit 1** — every corrected behavior fails before the fix | `01-red-tests-against-uncorrected-base.log` | 7794504 (planner = base) |
| Syntax checks (TS-LOOP-001, owner verification step 1) | `node --check` both files | PASS exit 0 | `02-syntax-module.log`, `03-syntax-test.log` | 2f51c07 |
| Focused AUDIO-001 tests (owner verification step 2) | `node --test tests/unit/speech-plan.test.mjs` | PASS — 40/40 | `04-green-focused.log` | 2f51c07 |
| Affected normalized-data regression (owner verification step 3) | `node --test tests/unit/level-data.test.mjs` | PASS — 13/13 | `05-affected-level-data.log` | 2f51c07 |
| Full units once after focused pass (owner verification step 4) | `npm run test:units` | PASS — 115/115/0 skipped | `06-full-units.log` | 2f51c07 |
| `git diff --check` (owner verification step 5) | `git diff --check && git diff --cached --check && git status --short` | PASS — clean | `07-diff-check.log` | 2f51c07 |
| Diff-scope and clean-status inspection (owner verification step 6) | `git diff --name-only 9c925ac…HEAD` | PASS — only `js/core/speech-plan.mjs` + `tests/unit/speech-plan.test.mjs` (+ this report/evidence directory in the docs commit); 0 files outside the authorized scope | §5 ledger | HEAD |
| Three fault probes (owner requirement; TS-MUT-001..007) | see §7 | PASS — all KILLED | `08-fault-probes.log` | 2f51c07 (mutated worktree) |
| Checkpoint requirement (items 1–7) | gate + unchanged initial non-forced push | PASS — remote SHA `9c925ac…` verified, 0-byte diff | evidence INDEX | 9c925ac |

## 5. Complete command ledger

Every command attempted during the package, including failures and retries, in execution order. Durations are omitted for fast unit commands (<1s) unless noted.

| Command (exact) | Phase + revision | Exit | Passed/failed | Artifact | Interpretation |
|---|---|---|---|---|---|
| `git fetch origin --prune`; `git rev-parse 9c925ac…^{commit}`; `git ls-remote --heads origin codex/glm-audio-001-c1-01`; `git status --porcelain` | checkpoint gate | 0 | — | — | base exists; assigned branch absent; tree clean |
| `git checkout -b codex/glm-audio-001-c1-01 9c925ac…` | branch creation | 0 | — | — | branch created exactly from the correction base |
| transient-credential push: `git -c credential.helper="!f() { … password=$(cat <chmod-600 file outside repo>); }; f" push -u origin codex/glm-audio-001-c1-01` | initial checkpoint @ 9c925ac | 0 | — | — | unchanged branch pushed non-forced; remote SHA verified `9c925ac…`; fetched byte-identity 0-byte diff; credential file deleted immediately and verified absent; token never printed/committed/URL-embedded |
| (reading all mandated contracts/docs + implementation boundary — no commands) | pre-edit | — | — | — | 14 mandated docs + prior report/evidence + 6 implementation files read; plan reported |
| `node --check tests/unit/speech-plan.test.mjs` | tests syntax (pre-fix) | 0 | — | — | test module parses |
| `node --test tests/unit/speech-plan.test.mjs` | **RED** against uncorrected planner (working tree: tests rewritten, planner untouched) | 1 | 23 passed / 17 failed | superseded by the worktree RED log below | first RED capture — one fixture defect found (see next row) |
| inspect failure diff of the real B2 dataset card test | correction of the test fixture | — | — | — | the positional lookup `vocabulary[18][0]` returned the wrong card (`19-0` Absprache) because raw B2 rows are not unit-sorted; replaced with the `level-data.test.mjs` find-by-translation lookup (`'IT / edv'`, card `19-74`); expected itemId corrected |
| `git add tests/unit/speech-plan.test.mjs` + commit `--amend` | tests commit (local, unpushed at that point) | 0 | — | — | `7794504` — RED tests with correct fixtures (DR-007 tests-first; the planner in this commit is still the uncorrected base version) |
| `git worktree add /home/z/my-project/audio001c1-red-wt 7794504` + `node --test tests/unit/speech-plan.test.mjs` | **RED** (disposable worktree: corrected tests + uncorrected base planner; `card.language` at line 170, `playList` slice at line 243) | 1 | **23 passed / 17 failed** | `01-red-tests-against-uncorrected-base.log` | failing-acceptance evidence: all 17 failures map to the three findings; the B2 dataset test fails because the term-translation step is missing (real cards have no `language` property); worktree removed after capture |
| edits: planner reads `card.translationLanguage`; loops swapped to items-outer/repeats-inner; `itemIndex` iterates original indices; JSDoc updated | implementation | — | — | — | the three corrections |
| `node --check js/core/speech-plan.mjs`; `node --check tests/unit/speech-plan.test.mjs` | syntax (owner ladder step 1) | 0 | — | `02-syntax-module.log`, `03-syntax-test.log` | both parse cleanly |
| `node --test tests/unit/speech-plan.test.mjs` | focused (owner ladder step 2) — one intermediate run 39/1 before the fixture fix is already covered above; this is the final run | 0 | **40 passed / 0 failed** | `04-green-focused.log` | all corrected cases pass (124.7 ms) |
| `node --test tests/unit/level-data.test.mjs` | affected regression (owner ladder step 3) | 0 | 13 passed / 0 failed | `05-affected-level-data.log` | normalized-data suite unaffected |
| `npm run test:units` | full units once (owner ladder step 4) | 0 | **115 passed / 0 failed / 0 skipped** | `06-full-units.log` | 75 pre-existing + 40 corrected (565 ms) |
| `git diff --check && git diff --cached --check && git status --short` | hygiene (owner ladder step 5) | 0 | — | `07-diff-check.log` | clean |
| `git diff --stat 9c925ac..HEAD`; `git diff --name-only 9c925ac..HEAD` | scope inspection (owner ladder step 6) | 0 | — | — | only the two authorized code files (+ report/evidence in the docs commit); 0 files outside scope |
| `git add js/core/speech-plan.mjs` + commit | implementation commit | 0 | — | — | `2f51c07` (final tested code) |
| `sha256sum js/core/speech-plan.mjs` | integrity baseline | 0 | — | — | `1c2f26ab…` recorded for the probes |
| `python3 /home/z/my-project/scripts/audio001c1-probes.py` (worktree add at `2f51c07` → baseline → 3 probes with per-mutant `node --check` → restore + sha256 verify → worktree remove) | fault probes @ 2f51c07 copy | 0 | baseline 40/40; probe 1: 26/14; probe 2: 36/4; probe 3: 37/3 — all failing runs exit 1 at the intended tests | `08-fault-probes.log` | language-access KILLED; sequence-nesting KILLED; index-rebase KILLED; module sha256 byte-identical after every restore |
| `python3 /home/z/my-project/scripts/audio001c1-sanitize.py` | evidence publication | 0 | — | 8 files | all logs sanitized; credential scan zero hits |

No background processes were left running; no browser or server was ever started. The full unit suite ran once after the focused suite passed. Final evidence freshness: every published GREEN/probe log was captured against `2f51c07` after the last source edit; the RED log was captured in a disposable worktree at `7794504`; only docs changed afterwards.

## 6. Regression and integration

- A1 / B2 / ordinary Verbs / Guided Verbs: not applicable as browser behavior — no production file that any browser page loads was changed except the planner module, which has zero importers (verified again this attempt: repository-wide `speech-plan` search finds only the module, its test, and docs). A1/B2 normalized-shape fidelity is now proven directly: the planner tests consume cards produced by the actual A1/B2 parsers (synthetic rows and real dataset cards), and the level-data suite passes 13/13.
- Phrases / Conversation / navigation / favorites / SRS: not applicable — no shared surface touched; the full unit suite (storage/SRS/scheduler/flashcards/shared-card units included) passes 115/115.
- Legacy storage / refresh / level and account isolation: not applicable — the planner performs no I/O and touches no storage.
- Actual audio adapter text AND language: the planner produces per-language text and a language tag; no speech was played (no browser, per the assignment). Real utterance text/language through the adapter remains AUDIO-002/AUDIO-003 scope (BASELINE.md gap #2).
- Browser/module startup and console errors: not applicable — no browser run in this package (explicitly forbidden by the assignment: "Do not run Playwright; this remains a pure unit package"); `node --check` confirms both modules parse.
- Desktop/mobile/themes/keyboard/reduced-motion/screenshots: not applicable — no UI.
- Untested or blocked items: none within this package's scope. The module's browser-import integrity is deliberately deferred to AUDIO-002/003, where a browser consumer exists.

## 7. Test-quality and fault-probe evidence

| Probe | Risk/contract | Production target + exact patch artifact | Detecting test | Baseline / syntax result | Actual failure | Classification | Integrity proof |
|---|---|---|---|---|---|---|---|
| 1. language-access | LF-AUDIO / LF-DATA / C1 finding 1: the planner must read the real normalized metadata | disposable copy of `js/core/speech-plan.mjs`: the term-translation call passes `card.language` instead of `card.translationLanguage` | 14 tests fail: every normalized-metadata test — A1-shaped `en`, B2 `mixed`, Arabic-only, stale-`language` ignore, actual A1 parser card, actual B2 mixed + Arabic parser cards, real A1 dataset card, real B2 dataset card, full-block repetition, stable ids/indices with startIndex, empty-en warning, slash alternatives, deterministic ordering | worktree baseline 40/40 pass; mutation parses (exit 0) | 26/40 pass — real parser/dataset cards carry no `language` property, so term-translation steps disappear or (in the stale-`language` fixture) use the wrong language | **KILLED** | `1c2f26ab…` sha256 re-verified after restore |
| 2. sequence-nesting | AC-11 / C1 finding 2: Verbs item-major repeat order | disposable copy: the item/repeat loop nesting swapped back to whole-list repetition (outer = repeat, inner = item) | 4 tests fail: *two items with repeatCount 2 emit item 1 both repeats then item 2 both repeats (Verbs autoplay order)*; *each item repetition contains its term, requested translation, and requested examples…*; *item ids and original indices stay stable across repeats after a startIndex*; *deterministic item-major ordering…* | baseline 40/40; mutation parses | 36/40 pass — expected `[i1-r0, i1-r1, i2-r0, i2-r1]` vs mutant `[i1-r0, i2-r0, i1-r1, i2-r1]` | **KILLED** | sha256 re-verified after restore |
| 3. index-rebase | AC-11 / C1 finding 3: stable source indexing | disposable copy: `const itemIndex = i - normalized.startIndex;` — indices re-based to 0 after applying `startIndex` | 3 tests fail: *startIndex in the middle retains the original item indices 1 and 2 without re-basing*; *startIndex at the last item plays exactly that item with its original index*; *item ids and original indices stay stable across repeats after a startIndex* | baseline 40/40; mutation parses | 37/40 pass — expected indices 1/2 vs mutant 0/1 (and 2 vs 0) | **KILLED** | sha256 re-verified after restore |

Probe selection/count: the owner mandated exactly these three probes — one per corrected risk family (metadata boundary, ordering, indexing), matching TS-MUT-006's distinct-failure-family rule. All three mutants were behavioral (each passed `node --check`; the unmutated worktree baseline passed 40/40 before each). The full 17-test RED set against the uncorrected base is the strongest test-the-tests evidence: before the fix, the suite fails on exactly the behaviors the review found wrong. No conditional assertions, mocks, ignored errors, or snapshot changes exist in this package: the tests use only `assert.deepEqual`/`assert.throws`/`assert.equal` on literal expectations. The one fixture correction during the RED cycle (positional → find-by-translation dataset lookup) is disclosed in §3/§5; no assertion was weakened — the expected steps were already correct for the intended EDV card and became binding once the lookup selected the right card.

## 8. Dead-code and dependency inventory

| Candidate symbol/path | Searches + dynamic caller checks | Classification | Disposition | Regression proof |
|---|---|---|---|---|
| `js/core/speech-plan.mjs` (whole module) | `rg -ln "speech-plan" --glob '!node_modules'` → only the module itself, its test, and docs; no HTML, no `js/core/*.js` importer, no dynamic `import()` anywhere | **intentionally unwired pure module** (AUDIO-002 is the assigned consumer) | keep — unchanged from the first attempt | `06-full-units.log` 115/115; no broken import (nothing imports it) |
| `card.language` read (removed) | `rg -n "card\.language" js/ tests/` → zero remaining production or test references after the correction | PROVEN_ORPHAN (created by the first attempt, removed by this correction) | removed — exactly the finding-1 fix | probe 1 (re-introducing it is KILLED by 14 tests) |
| `planSpeechSequence` / `InvalidSpeechPlanOptionError` | `rg -n` → definitions + test call sites only | DYNAMICALLY_USED (live via tests; production consumer arrives in AUDIO-002) | keep | `04-green-focused.log` 40/40 |

Search boundary: repository-wide text search excluding `node_modules` and `.git`, plus inspection of HTML inline handlers and `window.*` export patterns (no reference exists). No dependency manifest or lockfile changed. No claim is made about dead code elsewhere in the repository (DC-001 scope is this package's changed symbols).

## 9. Findings, limitations, and handoff

| Finding ID | Severity/impact | Reproduction and evidence | Disposition | Owner decision needed |
|---|---|---|---|---|
| A001C1-F1: the corrected planner is still unwired (no production importer) | informational | DC search: zero production references; AUDIO-002 is the assigned consumer | by design — same as the first attempt | none |
| A001C1-F2: browser-import integrity not proven by unit tests | low (evidence limitation) | TS-LOOP-001 note; this package has no browser consumer by assignment | deferred to AUDIO-002/003 | none now |
| A001C1-F3 (carried over from A001-F4): `repeatCount = 0` still throws rather than "play nothing" | low (semantic decision) | option validation unchanged from the accepted first attempt's strict policy | documented decision — the adapter can default 0→1 before calling, as `verbs-engine.js` does today | optional: confirm or change to empty-sequence semantics |
| A001C1-F4: B2 rows are not unit-sorted, so positional dataset lookups are unsafe | low (test-engineering note, already handled) | the RED-cycle fixture defect in §3/§5; fixed via the `level-data.test.mjs` find-by-translation pattern | fixed in this attempt | none |

- Remaining product risks: none introduced — the module has no callers yet; the correction aligns it with the real parser shape and the Verbs controller's ordering before AUDIO-002 consumes it. Environment restrictions: none encountered (pure Node unit package; PAT channel worked).
- Final diff/status vs base: `js/core/speech-plan.mjs` (+29/−12), `tests/unit/speech-plan.test.mjs` (rewritten, 660 lines), `docs/cefr/reports/AUDIO-001-C1-01.md`, `docs/cefr/evidence/AUDIO-001-C1/01/**` (8 sanitized logs + `INDEX.md`). No unintended ID, content, storage, dependency, contract, vocabulary, or snapshot changes: `git status` clean at every commit; level configuration, browser controllers, TTS, HTML/CSS, contracts, dependencies, and unrelated tests are untouched (0 files outside the authorized scope).
- GitHub delivery branch and report/evidence paths: branch `codex/glm-audio-001-c1-01`; report `docs/cefr/reports/AUDIO-001-C1-01.md`; evidence `docs/cefr/evidence/AUDIO-001-C1/01/` (8 sanitized logs + `INDEX.md`). The initial unchanged checkpoint was pushed at `9c925ac` before any edit (remote SHA verified). Remote-SHA and byte-identity verification of the final delivery is returned in the handoff message.
- Next proposed WP: AUDIO-002 (Verbs autoplay adapter compatibility) — the first consumer of the corrected planner. NOT started; owner assigns.

## 10. Owner/reviewer disposition — not executor approval

- Reviewer and reviewed exact revision: owner review of `codex/glm-audio-001-c1-01` at tested code `2f51c07` (tests `7794504`, docs commit after this report).
- Verdict: pending.
- Criteria/findings accepted or declined; explicit waivers and reason: awaiting owner review. Suggested acceptance checks: (a) confirm the three probes in `08-fault-probes.log` match the mandated probe set (language access killed by the real parser-card tests; nesting swap killed by the repeat-order tests; index re-base killed by the stable-index tests); (b) confirm the RED log shows the 17 pre-fix failures mapped to the three review findings; (c) confirm `git diff 9c925ac..HEAD --stat` shows only the four authorized path families; (d) confirm the amended tests commit (disclosed in §3/§5) is acceptable — it was local and unpushed when amended, and all RED evidence was re-captured against the amended commit.
