# SHARED-CARD-001 — Attempt 01 Delivery Report

## 1. Identity and status

- Status: READY_FOR_REVIEW
- Assigned goal and task type: SHARED-CARD-001 — CHARACTERIZATION/TEST package (primary type per CM: DOCUMENTATION-adjacent test work; no runtime change). Characterize the published German Verbs card reference (ordinary and Guided Challenge cards) before any shared-card extraction; capture actual DOM/interaction boundaries; compare with the approved LF-CARD / GC-UI targets; expose pre-existing contract failures as stable findings instead of hiding them in green snapshots. No production edits.
- Owner assignment reference: task instruction for SHARED-CARD-001 (repository `MohamedAzzam4/words-list-v2`; starting commit `d8fdfa55476a1fad96fd42ac749b60b0e3a711e7`; assigned branch `codex/glm-shared-card-001-01`). Accepted dependencies stated by the owner: BASELINE-001 attempt 02 ACCEPTED at `1e1e5dea71ba06baba3e1a4845cf98ebf4498ce9`; LEVEL-STD-001 and LEVEL-DATA-001..003 accepted as existing foundation at `d2f4c79909c4c89e5efbdbeb5d93ebac2b206986`; **BL-03 (Guided Challenge scheduler mismatch) acknowledged and deferred — characterization of Guided card PRESENTATION only; the scheduler and persistence were neither modified nor test-driven.**
- Branch; base commit; final tested code commit; report commit:
  - Branch: `codex/glm-shared-card-001-01` (created directly from the starting commit; the only pushed ref)
  - Base commit: `d8fdfa55476a1fad96fd42ac749b60b0e3a711e7` — verified exact at branch creation; clean tree (`git status --short` empty before the new files were authored)
  - Final tested code commit: `d8fdfa55476a1fad96fd42ac749b60b0e3a711e7` — **unchanged**; this package adds tests/fixtures/report/evidence only, so per DR-007 no code revision moved
  - Report commit: the publication commit at the branch tip containing this report, the new spec, the fixture, and the evidence directory; full SHA returned in the delivery response (the report does not embed its own commit's hash)
- Executor/model and sandbox OS/runtime versions: same executor sandbox as BASELINE-001 — Debian GNU/Linux 13 (trixie); Node v24.19.0; npm 11.17.0; Playwright 1.61.1 (lockfile, `@playwright/test` 1.61.1); Chromium 149.0.7827.55 (chromium + headless shell, build v1228); Mobile Chrome = Pixel 5 device emulation (not a real device). `npx playwright install --with-deps` remains unavailable without root (unchanged environment restriction from BASELINE-001); plain Chromium install with a verified headless launch was used and did not block any check.
- Start/end time; work directory: 2026-08-29T00:40Z–01:25Z (UTC; document reading from ~00:40Z, test execution window 01:03Z–01:21Z); work directory `/home/z/my-project/words-list-v2` (same clone used for BASELINE-001; branch switched to the new task branch at the exact starting commit).
- Dependency acceptance references: none newly accepted — this package consumes the owner-accepted foundations listed above and adds no dependency, script, or lockfile change (`npm ci` not re-run; the BASELINE-001 `node_modules` from the unchanged lockfile was reused; Playwright version re-verified against the lockfile at 1.61.1).

## 2. Required reading and pre-edit plan

- Paths fully read (in the order required by `AGENTS.md`), relevant contract IDs, observed conflicts:
  - `AGENTS.md` (incl. the new portable-delivery rules); `contracts/README.md`; `contracts/PORTABLE_AGENT_EXECUTION.md` (PX-001..010); `contracts/CHANGE_MANAGEMENT.md` (CM); `contracts/CODE_FINGERPRINT.md` (FP, esp. FP-DESIGN-009, FP-UI-005/006, FP-TEST-001..005); `contracts/TESTING_AND_SUCCESS.md` (TS, esp. TS-TEST-001..008, TS-LOOP-001..008, the card-rendering regression-matrix row); `contracts/DEAD_CODE_AND_REFACTORING.md` (DC-005: characterize before extraction); `contracts/DELIVERY_REPORTING.md` (DR-001..013); `contracts/LEVEL_FLASHCARD_STANDARD.md` (LF-CARD — the approved card targets); `contracts/GUIDED_CHALLENGE.md` (GC-UI-001..009 — the guided card targets; the superseded-Acquisition section noted as deferred BL-03); `docs/cefr/BASELINE.md`; `docs/cefr/WORK_PACKAGES.md` (SHARED-CARD-001 section); `docs/cefr/ACCEPTANCE_MATRIX.md` (AC-04..AC-09 rows owned partly by SHARED-CARD-001); `docs/cefr/GITHUB_DELIVERY.md` (publication procedure, evidence rules, credential-helper requirement).
  - Implementation/test files read for the characterization: `verbs.html` (card markup/CSS incl. flip mechanics, pointer-events rules, control sizes), `js/core/verbs-engine.js` (full card render + delegation + guided render paths), `js/core/verb-challenge-engine.js` (presentation/direction model only), `js/core/tts.js` (utterance language contract), `tests/e2e/verbs.spec.js`, `tests/e2e/verb-guided-challenge.spec.js` (established stub/drive patterns), `tests/e2e/words-audio.spec.js` (speech mock pattern), `playwright.config.js`, `content/generated/verbs/top_verbs_2000.json` (data shape; deck 36 and deck 1 samples).
  - Observed conflicts: none between documents. Code-vs-contract divergences are the SUBJECT of this package and are reported as findings SC-01..SC-07 (section 9), not silently resolved. The BL-03 scheduler mismatch was neither tested nor redesigned here.
- Before behavior; approved after behavior; explicit non-goals:
  - Before: repository exactly as published at `d8fdfa55476a1fad96fd42ac749b60b0e3a711e7`.
  - After: identical code; added `tests/e2e/cefr-card-reference.spec.js` (35 characterization tests), `tests/fixtures/cefr/verbs-card-reference.json` (synthetic 8-verb edge-case deck), this report, and the evidence directory.
  - Non-goals: no production JS/HTML/CSS/content/contract/dependency/lockfile/workflow/enforcement/hook/CI changes; no scheduler or persistence changes or test-drives (BL-03 deferral); no fixes for the exposed findings (they are inputs to SHARED-CARD-002/003); no full-repository E2E suite run; no merge/deploy/push to shared branches; no token in chat/commands/URLs/files/reports.
- Pre-edit plan (presented and recorded before any file was authored): (1) exact starting commit + clean tree verified; (2) proposed file list: the spec, the fixture, the report, the evidence directory — nothing else; (3) every required behavior mapped to a named test (section 4); (4) every check classified as current-behavior characterization (CHAR), approved target assertion the reference already meets (TARGET), or expected failure representing an existing product finding (`test.fail` finding tests + CHAR pins); (5) no existing test altered — `verbs.spec.js` and `verb-guided-challenge.spec.js` remain untouched regression anchors.
- Risk: low-medium — additive test package; the risk families are (a) characterizing the wrong behavior (mitigated by exact positive/negative assertions against independently authored fixture values and the real dataset), (b) expected-failure mechanism that could silently pass (mitigated by `test.fail` semantics — an unexpected pass fails the run — plus a per-result JSON status verification), (c) flaky baseline noise misread as regression (mitigated by isolated diagnostic reruns, both preserved in evidence), (d) fixture unrealism (mitigated by mirroring the real verb shape field-for-field; the real dataset also exercised in CHAR-20).
- Affected callers/UI/state/storage/audio boundaries: none modified. Inspected boundaries: `js/core/verbs-engine.js` (`renderCard`, `renderChallengeIntro/Recall`, `flipCard`, `toggleFavorite`, `speakCurrentCard`, body click delegation), `js/core/tts.js` (`speak` text+lang), `verbs.html` (card CSS: sizes, focus, transition, pointer-events), `js/core/verb-challenge-engine.js` (presentation model read-only).
- Baseline tests and known failures: existing relevant Chromium baseline run once at the start (25 passed / 2 environment-flaky `page.goto` timeouts; section 6); known gaps BL-01..BL-08 from BASELINE-001 remain as recorded there.

## 3. Changes and rationale

| File | Change/purpose | Contract/WP criterion | Compatibility impact |
|---|---|---|---|
| `tests/e2e/cefr-card-reference.spec.js` | New: 35-test characterization suite for the ordinary Verbs card and the Guided Challenge card (shell, front/back boundaries, examples, no-example safety, favorite/audio controls, activation inputs, non-grading back actions, hidden-answer leakage, focus/motion/target-size/overflow, module startup) | SHARED-CARD-001 "Write: … new `tests/e2e/cefr-card-reference.spec.js`"; LF-CARD; GC-UI-001..009; AC-04..AC-09 evidence | None — test-only; no runtime file touched |
| `tests/fixtures/cefr/verbs-card-reference.json` | New: synthetic 8-verb deck mirroring the real verb shape field-for-field; supplies zero-example, single-example, German-only-example, separable-prefix, and multi-example verbs that the real dataset (every verb has exactly 3 examples) cannot provide | SHARED-CARD-001 "synthetic fixtures"; AC-02/AC-05 no-example case; TS-TEST-007 synthetic seeding | None — served only via `page.route` interception inside this spec's browser contexts; the real dataset is untouched |
| `docs/cefr/reports/SHARED-CARD-001-01.md` | New: this delivery report | DR-001 | None |
| `docs/cefr/evidence/SHARED-CARD-001/01/**` | New: INDEX.md, 11 retained command logs, 8 inspected screenshots, 6 visual-inspection transcripts | DR-006; `GITHUB_DELIVERY.md` | None |

No application source, test (existing), contract, content, dependency, lockfile, or workflow file was modified. `git status --short` before staging showed exactly the two new test paths; `git diff --check` exit 0. No bug fixes were made, so no root-cause subsection applies; the characterization findings SC-01..SC-08 are recorded for owner triage in section 9.

Root cause of the two test iterations (test-authoring, transparently recorded): attempt 1's 10 failures comprised three expectation defects in the new spec (unscoped `.ex-sentence-span` selector matching the hidden glossary table; the favorite assertion missing the published canonical-id-plus-infinitive-alias storage pair; the intro-controls-inside-card expectation inverted) and one Chrome computed-style nuance (outline-width reports '3px' — its computed 'medium' — even under `outline: none`, so invisibility must be asserted via outline-style). Each fix was evidence-based; no product behavior was changed to make tests pass. All attempts are retained in the evidence directory.

## 4. Acceptance-to-evidence mapping

Classification key: **CHAR** = current-behavior characterization (passes now; fails when behavior changes, forcing a conscious update). **TARGET** = approved LF/GC assertion the published reference already meets. **EXPECTED-FAIL** = approved target assertion that currently fails, wrapped in `test.fail` with the finding ID in the title; the test runs, fails for real, and an unexpected pass would be reported by Playwright as a run failure (the finding cannot silently heal).

| Criterion / AC row / contract ID | Test path + exact name (chromium & Mobile Chrome) | Class | Result | Log/artifact | Tested revision |
|---|---|---|---|---|---|
| WP: ordinary + guided card shells before extraction; AC-04 / GC-UI-001 | `CHAR-01: ordinary card shell…`; `CHAR-21 (finding SC-07): guided card is a separate shell…`; `SC-07 TARGET: guided card uses the ordinary card shell…` | CHAR + EXPECTED-FAIL | CHAR PASS (ordinary shell `.verb-flashcard[data-action=flip]` + front/back faces + below-card grade/nav controls; guided renders `.guided-card` in `#guided-challenge-root`, ordinary shell absent there and not visible; intro controls inside the guided card, recall controls outside); TARGET fails as expected → **SC-07** | `new-spec-chromium-final.log`; `new-spec-mobile-chrome.log` | d8fdfa5 |
| WP: front/back structure and content boundaries; LF-CARD | `CHAR-01`, `CHAR-02` (pointer flip both ways; back fields incl. participle/auxiliary), `CHAR-09` (en-to-de front exact English text; no visible German), `CHAR-23`/`CHAR-24` (guided front labels and exact prompt/answer text) | CHAR | PASS | same logs | d8fdfa5 |
| WP: first example and its translation; AC-05 / LF-CARD | `CHAR-03 (finding SC-02)` (all examples inline; translations behind EN chip); `CHAR-04` (single example exact pair); `CHAR-07` (example-direction priority box: first example + full translation); `SC-02 TARGET` | CHAR + EXPECTED-FAIL | CHAR PASS (pins current behavior); TARGET fails → **SC-02** (LF-CARD wants only the first example with its translation always visible) | same logs | d8fdfa5 |
| WP: safe no-example behavior; AC-02 | `CHAR-05` (zero-example: no example box/chip/span, no stale previous-card content, back otherwise safe); `CHAR-08` (explicit absence message in example direction); `CHAR-27` (guided intro of every fixture verb incl. the zero-example verb with no example lines) | CHAR (meets target) | PASS | same logs | d8fdfa5 |
| WP: favorite and audio controls; LF-CARD | `CHAR-10` (front favorite: star toggle, persistence of `['v_ref_machen','machen']`, no flip/grade/advance, exact un-toggle); `CHAR-11` (back favorite keeps reveal); `CHAR-12` (back speak + example-sentence click produce exact adapter utterances `machen`/`Ich mache die Hausaufgaben.` at `de-DE`); `CHAR-25` (guided Listen speaks the served infinitive `de-DE`, no grade/advance) | CHAR + TARGET | PASS | same logs | d8fdfa5 |
| WP: pointer, Enter and Space activation independently; AC-07 / LF-CARD | `CHAR-02` (pointer flips front→back and back→front — flip-back surface is the back face because the flipped front is `pointer-events:none`); `CHAR-14 (finding SC-06)` (no tabindex/role/focusability; Enter/Space/`Space` key presses do not flip); `CHAR-26` (guided reveal button: Enter and Space each activate — native button semantics); `SC-06 TARGET` | CHAR + EXPECTED-FAIL | pointer PASS; keyboard on the ordinary card: TARGET fails → **SC-06**; guided keyboard PASS | same logs | d8fdfa5 |
| WP: back controls must not flip, grade, or advance; AC-08 / LF-CARD | `CHAR-12 (TARGET, AC-08)`: back speak, example-sentence click, both accordions, and the EN chip each leave `isFlipped=true`, `currentIndex=0`, `knownVerbIds=[]`, favorites unchanged, counter `1 / 8`, persisted storage unchanged | TARGET | **PASS — the published reference meets AC-08** (finding SC-08, positive) | same logs | d8fdfa5 |
| WP: hidden-answer leakage through text, attributes, titles, accessible content; AC-06 / GC-UI-005 / LF-CARD secrecy | `CHAR-09 (finding SC-01)` (ordinary en-to-de: full German answer in pre-reveal DOM on the rotated back; partial answer in the hidden hint box; front speak button speaks the German infinitive `de-DE` before reveal); `CHAR-23`/`CHAR-24 (TARGET, AC-06)` (guided: entire `#guided-challenge-root` subtree sweep — no element text, no attribute value (title/aria/data/onclick), no root text contains the German infinitive or the German example pre-reveal; German audio control absent until reveal); `SC-01 TARGET` | CHAR + EXPECTED-FAIL + TARGET | guided TARGET **PASS** (meets GC-UI-005/AC-06); ordinary EXPECTED-FAIL fails → **SC-01** | same logs | d8fdfa5 |
| WP: focus visibility, reduced motion, 44×44 targets, mobile overflow; AC-09 / LF-CARD | `CHAR-16 (finding SC-04)` (focused card controls compute `outline-style: none`); `CHAR-17 (finding SC-05)` (reduced-motion: 0.6 s transform transition unchanged, zero `prefers-reduced-motion` rules in CSSOM); `CHAR-15 (finding SC-03)` (favorite 28×28, speak <44, hint <44; grade buttons ≥44); `CHAR-18`/`CHAR-28` (no horizontal overflow at the current viewport incl. content-heavy back and guided view); `SC-03/SC-04/SC-05 TARGET` | CHAR + EXPECTED-FAIL (+TARGET for overflow) | overflow TARGET PASS (desktop + Pixel 5); focus/motion/target-size TARGET fail → **SC-03, SC-04, SC-05** | same logs + screenshots | d8fdfa5 |
| WP: real browser module startup and console errors; TS-TEST-006 | `CHAR-19 (TARGET)` (real module boot through `verbs.html`'s ESM import chain with only the firebase auth adapter stubbed: `window.verbsEngine` live, dataset loaded, zero console errors / pageerrors) | TARGET | PASS | same logs | d8fdfa5 |
| WP: desktop/mobile screenshots inspected; TS-TEST-008 | 8 screenshots captured at the tested revision and visually inspected by the executor (vision-model transcripts published); findings consistent with the DOM assertions | inspection | PASS — no horizontal overflow, correct stacking, no visible German on the en-to-de front, guided front shows no answer; one transient trophy toast observed overlapping bottom controls in 3 desktop captures (noted, not actioned) | `screenshots/*`, `vlm-inspections/*` | d8fdfa5 |
| WP: existing green behavior stays protected | Baseline run before editing (step 2) and regression run after stabilization (step 6) of the untouched `verbs.spec.js` + `verb-guided-challenge.spec.js` | regression | 25/27 then 26/27 — each failed test passes in isolation (environment-flaky `page.goto`/state waits under serial suite load; pre-existing at this revision, unrelated to the additive change); finding **SC-INFRA-01** | baseline + regression logs | d8fdfa5 |
| WP: prerequisite mismatch → finding requiring owner triage | Findings SC-01..SC-08 + SC-INFRA-01 with file:line reproduction | findings | All recorded in section 9; none fixed | report §9 | d8fdfa5 |
| AC-21 / TS-MUT fault probes | N/A — no production code changed in this package, so there is no changed risk family to probe (TS-MUT-001 exempts documentation/test-only work). Test-quality evidence is instead supplied by: the expected-failure demonstrations (each `test.fail` test proves a real target assertion fails), the false-positive review (attempt 1 caught three authoring defects in the new spec itself — the suite demonstrably fails when its expectations are wrong), and the per-result status verification | N/A (justified) | section 7 | — |
| DC-001 dead-code inventory | N/A — no production symbol changed or removed; the new spec adds no production code. Scope note: the spec introduces no selectors that production code must maintain; fixture JSON is data-only | N/A (justified) | section 8 | — |
| AC-22 / PX, DR: honest complete report, no scope drift | This report; command ledger with failures and retries; evidence INDEX with hashes, redactions, missing-evidence declarations | report | READY_FOR_REVIEW (executor-supplied proof only) | this file | d8fdfa5 |

## 5. Complete command ledger

All commands ran in `/home/z/my-project/words-list-v2` unless noted; Playwright's own webServer (`python -m http.server 9012`, port verified free first) served every browser run from this checkout. Exit codes are actual terminal results. Every browser suite ran in the foreground, one at a time, awaited to completion; no duplicates were launched; no test file was edited mid-run.

| # | Command (exact) | Phase | Start/duration | Exit | Counts | Artifact | Interpretation |
|---|---|---|---|---|---|---|---|
| 1 | `git fetch origin` + `git checkout -b codex/glm-shared-card-001-01 d8fdfa55476a1fad96fd42ac749b60b0e3a711e7` | setup | 00:5xZ, <5 s | 0 | — | — | Branch created at the exact starting commit; clean tree verified (`git status --short` empty) |
| 2 | Document/source reading: 14 required documents + `verbs.html`, `verbs-engine.js`, `verb-challenge-engine.js`, `tts.js`, 3 existing E2E specs, `playwright.config.js`, dataset samples (`rg`/Read/`sed -n`/`python3 -c` shape inspection) | reading | 00:40Z–01:00Z | 0 | — | — | Required reading completed fully before editing; pre-edit plan issued |
| 3 | `npx playwright test tests/e2e/cefr-card-reference.spec.js --project=chromium --list` (×3 during development; first two hit an `import.meta`/path ENOENT error in the spec header, fixed) | inventory | 01:0xZ | 1, 1, 0 | 35 listed (final) | `inventory-list-chromium.log` (re-captured at 01:21Z from the final file — see INDEX missing-evidence note) | Development listings not retained; published inventory reflects the final spec |
| 4 | `npx playwright test tests/e2e/verbs.spec.js tests/e2e/verb-guided-challenge.spec.js --project=chromium --reporter=line` | **ladder step 2 — existing baseline, once** | 01:01Z, 2.1 m | 1 | **25 passed / 2 failed** | `baseline-existing-chromium.log` | 2 `page.goto` 30 s `load` timeouts (`daily review with due verbs…`, `T3: canonical-ID migration…`) |
| 5 | Same command + `--grep "daily review with due verbs serves recognition then production tracks\|T3: canonical-ID migration through real loading lifecycle"` | failure isolation (TS-LOOP-005) | 01:03Z, 4 s | 0 | **2 passed** | `baseline-failure-diagnostic-rerun.log` | Both pass in isolation → environment-flaky page-load timeouts under serial suite load (CDN-sensitive); pre-existing, not caused by this package (additive-only change) |
| 6 | `npx playwright test tests/e2e/cefr-card-reference.spec.js --project=chromium --reporter=line` | ladder step 4 — attempt 1 | 01:05Z, 2.2 m | 1 | 25 passed / 10 failed | `new-spec-chromium.log` | 3 test-authoring defects + 1 computed-style nuance + product-finding demonstrations; every failure diagnosed (see §3/§7) |
| 7 | Same command | ladder step 4 — attempt 2 | 01:12Z, 1.2 m | 1 | 34 passed / 1 failed | `new-spec-chromium-attempt2.log` | CHAR-21 expectation inverted (intro controls ARE inside the guided card — pinned actual behavior, contrast added) |
| 8 | Same command | ladder step 4 — final | 01:13Z, 1.1 m | 0 | **35 passed** | `new-spec-chromium-final.log` | 28 genuinely passing + 7 expectedly-failing finding demonstrations |
| 9 | Same command with `--reporter=json` (piped to a status extractor) | status verification | 01:15Z, ~1.1 m | 0 | 28×`passed` + 7×expectedly-`failed`, 0 unexpected | not retained (counts recorded) | Proves every `test.fail` test executed and failed as declared; no unexpected pass |
| 10 | `npx playwright test tests/e2e/cefr-card-reference.spec.js --project="Mobile Chrome" --reporter=line` | **ladder step 5** | 01:16Z, 1.0 m | 0 | **35 passed** | `new-spec-mobile-chrome.log` | Same composition on Pixel 5 emulation incl. overflow assertions |
| 11 | `npx playwright test tests/e2e/verbs.spec.js tests/e2e/verb-guided-challenge.spec.js --project=chromium --reporter=line` | **ladder step 6 — regression, once** | 01:17Z, 1.0 m | 1 | **26 passed / 1 failed** | `regression-verbs-guided-chromium.log` | `R2: Resume Daily Review…` failed (`#view-guided` hidden at an expect); a different test than step 4's failures |
| 12 | Same command + `--grep "R2: Resume Daily Review after refresh restores the exact production-phase state"` | failure isolation | 01:17Z, 3 s | 0 | **1 passed** | `regression-failure-diagnostic-rerun.log` | Passes in isolation → same environment-flake class (SC-INFRA-01) |
| 13 | `node /home/z/my-project/scripts/capture-shared-card-001-screens.js` (with `python3 -m http.server 9012` started and killed around it; script outside the repo) | screenshots | 01:19Z, ~20 s | 0 | 8 PNGs | `screenshots/*` | Reference states captured on desktop + Pixel 5 with the synthetic fixture |
| 14 | `z-ai vision -p "…" -i <png>` ×6 (desktop front/back/entode/guided-front/guided-revealed, mobile back/guided) | visual inspection | 01:19Z–01:21Z | 0 | 6 transcripts | `vlm-inspections/*` | Screenshots inspected per TS-TEST-008; observations consistent with DOM assertions |
| 15 | `rg -i` credential scan over all artifacts; `sha256sum` over all published evidence; `du -sh` budget check; trailing-whitespace strip (`sed -E 's/[ ]+$//'`) on the four published log copies whose raw Playwright output carried trailing spaces (28 lines total, documented per file in the INDEX sanitization statement) | sanitization | 01:2xZ | 0 | — | INDEX tables + sanitization statement | No credential material found; no content redaction required; published diff passes `git diff --check` (raw untouched captures remain executor-side) |
| 16 | `git diff --check` | ladder step 7 | 01:2xZ | 0 | — | — | No whitespace/conflict-marker errors |
| 17 | `git add …` + `git diff --cached --check` | pre-commit gate | before commit | 0 | — | — | Mandatory staged-diff check passed |

Background task IDs and terminal outcomes: none — every browser run was foregrounded and awaited; the only background process was the screenshot server, started and killed around command 13 with its exit collected. Reason for reruns: commands 7–8 iterate the new spec only (TS-LOOP-002/005: after diagnosing attempt 1's failures, only the affected file was rerun); commands 5 and 12 are single-test diagnostic isolations of pre-existing suite flakiness, preserved in full. No broad suite was launched during debugging. The complete repository E2E suite was NOT run (assignment forbids it); the other E2E files' pass/fail is unknown and not claimed.

## 6. Regression and integration

- Existing relevant baseline (before edits): `verbs.spec.js` + `verb-guided-challenge.spec.js` on Chromium — 25/27 passed; the 2 failures are `page.goto` `load`-event timeouts that pass in isolation (logs 4–5). Regression after stabilization: 26/27 with a different single flaky failure (`R2`, passes in isolation; logs 11–12). Net: **no existing test regressed due to this package** — the additive-only change cannot alter those suites, and both runs' failures are the pre-existing environment-flake class (finding SC-INFRA-01, section 9). The full repository E2E suite was not run (out of assignment scope); unit suite not re-run (no production code changed; BASELINE-001's 59/59 unit result remains the record for the unchanged code at `d2f4c79`; note this package's tested revision `d8fdfa5` differs from that baseline only by documentation commits, which touched no runtime file).
- Ordinary Verbs card: fully characterized on the synthetic edge-case deck (35-test suite) and spot-verified on the real published dataset (CHAR-20: deck 1 "werden", 3 real examples, EN chip). Pointer flip both directions; keyboard absent (SC-06); back controls non-grading (AC-08 met); pre-reveal DOM/audio leakage in en-to-de (SC-01).
- Guided Challenge card: presentation characterized (shell, reveal button, intro/recall content boundaries, Listen gating, keyboard operability of its real buttons). The scheduler was only DRIVEN (as published) to reach presentation states; no scheduler or persistence behavior was modified, judged, or test-driven (BL-03 deferral honored — see section 9 separation note).
- Audio: adapter-level utterance text AND language asserted through the real `tts.js` `speak` path (`machen`/`Ich mache die Hausaufgaben.` at `de-DE`); speech is synthetic (mock `speechSynthesis`) — real hardware voice behavior remains untested (TS-DONE-006).
- Browser/module startup: real ESM import chain booted in every test (only the firebase auth adapter stubbed, matching the repo's established test pattern); zero console errors/pageerrors in the startup check (CHAR-19) and no new console errors were observed in any characterization run.
- Mobile: Pixel 5 emulation — no horizontal overflow (geometric assertions CHAR-18/CHAR-28 pass on both projects; visually confirmed in the inspected screenshots). Mobile Chrome is emulation, not a real-device or Safari certification.
- Desktop/mobile/themes/keyboard/reduced-motion/screenshots: screenshots inspected (8 files, 6 transcripts published); keyboard characterized (absent on the ordinary card — SC-06; native on guided buttons); reduced-motion absent (SC-05); focus indication absent on card controls (SC-04); light/dark themes not exercised in this package (no theme assertions; the theme toggle exists on the page — recorded as untested, not as passing).
- For each inapplicable item: B2 level cards — out of scope for this Verbs-reference package (B2 E2E remains the mandatory SHARED-CARD-003/AUDIO-003 gap BL-05); Phrases/Conversation — untouched and not run (assignment scope); real-device/Safari — not available; hardware speech — not available.

## 7. Test-quality and fault-probe evidence

- Expected-failure mechanism (per the task's rule "name the finding ID, demonstrate the actual failure, and remain capable of detecting an unexpected pass"): each of the 7 approved-target failures is declared `test.fail('<SC-xx> TARGET: …')`; the test body asserts the real LF/GC target, RUNS, and fails against the published implementation (per-result status `failed`/expected — verified via the JSON status run, command 9). If the product ever meets the target, Playwright reports the unexpected pass as a failure, so a finding cannot silently disappear. In addition, the CHAR pin-tests assert the current defective behavior exactly (e.g., CHAR-09 pins the DOM/audio leak; CHAR-03 pins all-examples-inline + chip-hidden translations), so an unannounced behavior change breaks the suite and forces a conscious test update.
- No unconditional skips: zero `test.skip`/`fixme` in the suite; all 35 tests executed in both projects (listing ≠ pass — per-run counts and per-result statuses recorded).
- False-positive review (task rule: "Inspect every changed test for false positives"): performed and materially proven — the first run failed 10 tests, of which 4 were defects in the new spec's own expectations (unscoped selector; wrong favorite-array expectation; inverted controls-inside-card expectation; outline-width-vs-style nuance). Each was diagnosed from evidence and corrected in the test file only. The corrected suite then demonstrably separates product findings (7 expected failures) from passing characterization. No assertion was weakened to obtain green: every fix tightened scope or pinned actual published behavior.
- Assertion style: exact positive and negative assertions on fixture-authored values (no implementation-derived oracles for presentation outcomes); real user actions (`locator.click`, `keyboard.press`, `focus`); state compared before/after via the controller's own exported state and persisted localStorage; deterministic waits via `waitForFunction` on DOM state — **no arbitrary sleeps** (the only fixed delays are in the screenshot-capture script, which is not a test); no conditional assertions, try/catch, or optional-chained escapes in test bodies (guard loops bound iterations and end with hard assertions).
- No mutation/fault probes: **justified N/A** — TS-MUT-001 scopes implementation fault probes to changed domain logic; this package changed zero production code. The 7 expected-failure demonstrations are the package's test-quality evidence that the assertions can fail.
- Speech quality: the mock captures utterance `text` AND `lang` AND voice selection at the real adapter boundary (TS-TEST-004), covering the wrong-language family deterministically for the Verbs page (the level-page `app.js` wrapper gap remains BL-02/AUDIO-003 scope).

## 8. Dead-code and dependency inventory

| Candidate symbol/path | Searches + dynamic caller checks | Classification | Disposition | Regression proof |
|---|---|---|---|---|
| — | — | — | — | — |

**Justified N/A for removal inventory**: DC-001 scopes the examination to changed symbols and direct callers; this package changed no production symbol (additive test/fixture/docs only), so there are no changed symbols, no orphaned handlers, and no dependency deltas (`package.json`/lockfile untouched). Note for completeness: the new spec adds no production selector that app code must keep alive — it consumes existing DOM contracts (`.verb-flashcard`, `.verb-card-front/back`, `.guided-card`, `data-action` values), which is precisely the pre-extraction snapshot SHARED-CARD-002 will preserve.

## 9. Findings, limitations, and handoff

| Finding ID | Severity/impact | Reproduction and evidence | Disposition | Owner decision needed |
|---|---|---|---|---|
| SC-01 | High (answer secrecy, ordinary card) — en-to-de card exposes the hidden German answer pre-reveal through the DOM and audio | `js/core/verbs-engine.js:1296-1465` renders the full back face (German infinitive, meaning, examples, conjugations, participle) into the DOM of every direction mode; in `en-to-de` the answer is visually rotated away (`verbs.html:680-683`) but present in the DOM/accessible tree (CHAR-09). The hidden hint box carries `Verb Infinitive: mac…` (`verbs-engine.js:1255`). `speakCurrentCard()` (`verbs-engine.js:1670-1685`) speaks `verb.infinitive` in `de-DE` from the FRONT speak button in every non-example mode — German answer audio before reveal (CHAR-09 utterance capture). Violates LF-CARD secrecy ("must not expose German answer text, German answer audio, or equivalent accessible metadata") / AC-06 analog | Open — input to SHARED-CARD-002/003 (shared renderer must render the back only on reveal for hidden-answer directions, and gate front audio) | Confirm SC-01 handling inside SHARED-CARD-002 scope |
| SC-02 | Medium (LF divergence) — default-direction back shows ALL German examples inline and hides translations behind an extra EN-chip click | `verbs-engine.js:1420-1446`: every example sentence rendered inline (CHAR-03 count 3 on a 3-example verb), translations rendered inside `.ex-en-line.hidden` until the chip click. LF-CARD target: only the first example on the flashcard, its translation always visible with it. The example-direction modes already implement the target pattern (priority box, CHAR-07) but add extra examples behind an on-card toggle | Open — input to SHARED-CARD-002/003 | Choose whether the shared card adopts first-example-only + always-visible translation (LF target) for all directions |
| SC-03 | Medium (touch targets) — primary card affordances below the 44×44 minimum | `verbs.html:548-560` (`.fav-icon-btn` fixed 28×28), `css/core.css:402-410` (`.speak-btn` ≈27 px), `verbs.html:700-710` (`.hint-btn` <44); grade buttons compliant (CHAR-15 measurements) | Open — styling fix belongs to SHARED-CARD-002/003 CSS work | Confirm 44×44 applies to favorite/audio/hint on the shared card (LF-CARD says primary targets) |
| SC-04 | Medium (accessibility) — no visible keyboard focus on card controls | `css/core.css:84-90` global `button, select { outline: none }` with no `:focus-visible` replacement for card controls (only `.tab-btn` and `.form-input` have focus styles); CHAR-16 computed `outline-style: none` on all six sampled controls | Open — CSS fix in SHARED-CARD-002/003 | None beyond scope confirmation |
| SC-05 | Medium (accessibility) — no reduced-motion support | No `prefers-reduced-motion` rule anywhere in `css/core.css`, `css/verb-challenge.css`, or `verbs.html` (CSSOM scan, CHAR-17); the card flip is a 0.6 s transform transition (`verbs.html:639-648`) | Open — CSS fix in SHARED-CARD-002/003 | None beyond scope confirmation |
| SC-06 | High (accessibility) — the ordinary flip card has no keyboard activation | `.verb-flashcard` is a plain `<div data-action="flip">` (`verbs-engine.js:1297`) — no tabindex, no role, not focusable, and the controller registers no key handler anywhere (`rg keydown|tabindex|role=` → zero matches in engine/HTML); Enter/Space do nothing (CHAR-14). Guided controls are real `<button>`s and keyboard-operable (CHAR-26) | Open — interaction fix in SHARED-CARD-002 | Confirm the shared card becomes a focusable, Enter/Space-activatable control |
| SC-07 | Medium (structural, expected pre-extraction) — Guided card does not use the ordinary card shell or flip reveal | `renderChallengeRecall` (`verbs-engine.js:2102-2176`) renders `.guided-card` (separate implementation) and reveals via a dedicated button, not the ordinary flip; intro controls sit inside the guided card while recall controls sit outside (CHAR-21/22); card-body clicks do nothing (CHAR-22). Diverges from GC-UI-001/002 (shared shell + flip reveal) — the exact gap SHARED-CARD-002 closes | Open — primary input to SHARED-CARD-002 | None — this is the expected pre-extraction state; characterized, not judged |
| SC-08 | Positive verification — the published reference MEETS AC-08 (back controls cannot flip, grade, or advance) | CHAR-12: back speak, example-sentence click, both accordions, and the EN chip each leave flip/index/known/favorites/persisted storage byte-identical; only the grade buttons advance (CHAR-13) | Verified — protect as regression during extraction | None |
| SC-INFRA-01 | Medium (test infrastructure, pre-existing) — the tracked guided E2E suite contains load-sensitive flaky tests in this sandbox | Baseline run: 2 `page.goto` 30 s `load` timeouts (`daily review with due verbs…`, `T3…`); regression run: 1 different failure (`R2` `#view-guided` hidden); all three pass in isolation within seconds (diagnostic logs). Pattern: serial full-file runs + real-Firebase CDN page loads → occasional load-event/state-wait timeouts. Pre-existing at `d8fdfa5`; unrelated to this additive package | Open — owner/reviewer triage; candidate causes: CDN latency sensitivity of `page.goto` default `load` wait, or suite-load timing | Decide whether to harden these tests (e.g., `waitUntil` policy) in a dedicated test-quality package — executors must not silently alter them |
| OBS-01 | Info — transient trophy toast overlaps bottom controls | "Achievement Unlocked! Weekend Warrior" toast rendered over the grade/nav buttons in 3 desktop screenshots (the trophies engine fires on init; 2026-08-29 is a Saturday). Transient by design; observed only, no action | Noted | None |

BL-03 separation statement: this package characterizes Guided card PRESENTATION only (shell, content boundaries, controls, secrecy, keyboard). The published scheduler (`verb-challenge-engine.js`) was exclusively DRIVEN to reach presentation states (intro → recall → production), never modified, never redesigned, and no scheduler policy was asserted beyond what reaching a presentation state requires. The deferred BL-03 mismatch (single-recall Acquisition graduation vs GC v0.2 GC-ACQ-004..017) remains exactly as recorded in BASELINE-001 and is untouched by these tests.

- Remaining product risks, environment restrictions and untested requirements: (1) the other 98 E2E cases in the repository were not run (assignment scope) — their pass/fail is unknown and not claimed; (2) Mobile Chrome is emulation; no real-device or Safari certification; (3) all speech evidence is synthetic (mock `speechSynthesis`) — real voice availability/selection untested; (4) themes (light/dark) not exercised; (5) `playwright install --with-deps` still unavailable without root (non-blocking, unchanged); (6) SC-INFRA-01 flakiness may intermittently affect reviewer reruns of the tracked guided suite.
- Final diff/status and no unintended ID/content/storage/dependency changes: the final diff vs `d8fdfa55476a1fad96fd42ac749b60b0e3a711e7` contains exactly `tests/e2e/cefr-card-reference.spec.js`, `tests/fixtures/cefr/verbs-card-reference.json`, `docs/cefr/reports/SHARED-CARD-001-01.md`, and `docs/cefr/evidence/SHARED-CARD-001/01/**`. `git diff --check` and `git diff --cached --check` both exit 0. No production, contract, content, dependency, lockfile, or workflow file was touched; no IDs or stored progress changed (all test storage was synthetic, per-context).
- Branch/commit/patch retrieval: task branch `codex/glm-shared-card-001-01` (only pushed ref; no force-push; no merge). Tested code SHA `d8fdfa55476a1fad96fd42ac749b60b0e3a711e7` recorded here; the final delivery commit SHA is returned in the chat response per DR-007 (not embedded here). Evidence: `docs/cefr/evidence/SHARED-CARD-001/01/INDEX.md` with SHA-256 per file, redaction statement (none required), and missing-evidence declarations.
- Next proposed WP (do not start without assignment): **SHARED-CARD-002 — Extract shared presentation without shared grading**, consuming this characterization as its baseline; prerequisite owner decisions from this audit: (a) confirm SC-01 secrecy handling (render-on-reveal back + front-audio gating) is in SHARED-CARD-002 scope; (b) decide the SC-02 first-example/translation presentation for all directions; (c) bundle the SC-03/04/05/06 accessibility fixes into the shared-card extraction rather than piecemeal; (d) triage SC-INFRA-01 separately (test-quality package) so flaky guided anchors do not mask extraction regressions; (e) BL-03 remains deferred — SHARED-CARD-002 must still not touch the scheduler.

## 10. Owner/reviewer disposition — not executor approval

- Reviewer and reviewed exact revision: pending — code revision `d8fdfa55476a1fad96fd42ac749b60b0e3a711e7`; delivery revision = the branch-tip commit returned in the delivery response.
- Verdict: pending / CHANGES_REQUESTED / ACCEPTED
- Criteria/findings accepted or declined; explicit waivers and reason: *(left for reviewer)*
