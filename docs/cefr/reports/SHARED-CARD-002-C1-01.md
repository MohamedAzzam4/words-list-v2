# SHARED-CARD-002-C1 — Attempt 01 Delivery Report

## 1. Identity and status

- Status: READY_FOR_REVIEW
- Assigned goal and task type: Correct the four owner-review findings in the shared card foundation (SC2-C1-A11Y-001 meaningful accessible names; SC2-C1-A11Y-002 inactive face isolation; SC2-C1-A11Y-003 truthful Guided semantics; SC2-C1-DESIGN-001 language-neutral example API) — BUGFIX (primary; A11Y-001/002/003 restore intended accessible behavior) + MODIFICATION (secondary; DESIGN-001 consciously changes the shared example API/selector). CM gates FIX + MOD applied: failing-target reproductions recorded as RED evidence before any edit (§4/§5); superseded tests listed (§3); behavior before/after documented (§2/§3).
- Owner assignment reference: owner task message "TASK: Execute SHARED-CARD-002-C1 — correct the owner-review findings in the shared card foundation" with the four finding definitions, permitted paths, mandatory test-first requirements, verification ladder, four fault-probe families, and delivery rules.
- GitHub task branch: `codex/glm-shared-card-002-c1-01`; base commit: `3af821907cf034426603959b0107b2f024d4df2a` (accepted parent candidate); final tested code commit: `ebc90953c8f721d11faf6fbb91331277064614be` (implementation + tests; all code evidence in `docs/cefr/evidence/SHARED-CARD-002-C1/01/` ran against exactly this tree; the report/evidence commit is docs-only and does not move the tested code revision).
- Evidence index: `docs/cefr/evidence/SHARED-CARD-002-C1/01/INDEX.md`
- Final delivered SHA is returned after commit/push in the handoff message, not embedded into its own commit.
- Executor/model and sandbox OS/runtime versions: Super Z (GLM executor); Debian sandbox, Node v24.19.0, npm 11.17.0, Playwright 1.61.1 (lockfile; chromium-headless-shell v1228 installed during this session — the sandbox reprovision on 2026-08-30 removed the previously pinned browser cache, so `npx playwright install chromium` was run once before any browser test).
- Start/end time; work directory: 2026-08-30 ~00:55–~02:10 UTC (sandbox clock); `/home/z/my-project/words-list-v2` (repo-relative paths in this report).
- Dependency acceptance references: SHARED-CARD-002 attempt 01 at `3af821907cf034426603959b0107b2f024d4df2a` (review of that attempt produced the four findings corrected here).
- Language: English only, per the owner's mandatory language requirement.

## 2. Required reading and pre-edit plan

- Paths fully read: `AGENTS.md`, `contracts/CHANGE_MANAGEMENT.md`, `contracts/CODE_FINGERPRINT.md`, `contracts/TESTING_AND_SUCCESS.md`, `contracts/DEAD_CODE_AND_REFACTORING.md`, `contracts/DELIVERY_REPORTING.md`, `contracts/LEVEL_FLASHCARD_STANDARD.md`, `docs/cefr/WORK_PACKAGES.md` (SHARED-CARD-002 section), `docs/cefr/reports/SHARED-CARD-002-01.md` (complete), `docs/cefr/evidence/SHARED-CARD-002/01/INDEX.md` (complete), plus the implementation surfaces for this correction: `js/core/shared-card.js`, `js/core/verbs-engine.js` (rendering/flip/guided sections), `css/shared-card.css`, `css/verb-challenge.css`, `verbs.html` (flip mechanics/CSS), `tests/unit/shared-card.test.mjs`, `tests/e2e/cefr-card-reference.spec.js` (complete), `tests/fixtures/cefr/verbs-card-reference.json`, `playwright.config.js`.
- Before behavior (characterized at `3af8219`): icon-only Speak exposed the accessible name "🔊" and Favorite "☆"/"⭐" (text-content names; titles never contribute because content is non-empty) — SC2-C1-A11Y-001. After a 3D flip, the visually hidden front face and its hint/speak/favorite controls remained in the Tab order and the accessibility tree (only `pointer-events: none` CSS applied; the pre-reveal empty back face was likewise never isolated) — SC2-C1-A11Y-002. After a Guided reveal, the card retained `data-action="flip"`, `tabindex="0"`, `role="button"`, `aria-label="Guided challenge card: activate to reveal the answer"` and a pointer cursor, advertising a no-op activation — SC2-C1-A11Y-003. The shared example API was English-specific (`renderExampleBlock({ de, en })`, `.ex-en-line`) with no language/direction metadata — SC2-C1-DESIGN-001.
- Approved after behavior: icon-only affordances carry stable descriptive `aria-label` names (Speak Verb / Toggle Favorite), favorite state conveyed by `aria-pressed` with an unchanging name; only the displayed face is keyboard-focusable and exposed to assistive technology (`inert` on the hidden face in both flip directions, deterministic at render and on every flip, restored on flip-back, secrecy preserved); a revealed Guided card exposes no activation affordance at all (no flip action, button role, focus target, reveal name, or pointer affordance) while grading, Listen and Continue controls stay independently accessible; `renderExampleBlock({ sourceText, sourceLang, translation, translationLang, label })` renders `.ex-translation-line` with `dir`+`lang` metadata (en→ltr/lang, ar→rtl/lang, mixed or unknown→auto/no lang, missing translation→no line) and the German sentence keeps `lang="de"`.
- Explicit non-goals: challenge grading, scheduler, latency, persistence and token-guard semantics (untouched); vocabulary content (never invented or translated); BL-03 (still deferred); SC-INFRA-01 test-infrastructure flakiness (not authorization to rewrite unrelated infra); A1/B2 integration (SHARED-CARD-003); autoplay redesign (AUDIO-*).
- Exact allowed write paths (all writes stayed inside them): `js/core/shared-card.js`, `js/core/verbs-engine.js`, `css/shared-card.css`, `tests/unit/shared-card.test.mjs`, `tests/e2e/cefr-card-reference.spec.js`, report + evidence paths. No other file was edited.
- Risk: medium-high (shared presentation core used by both card modes). Fault families assigned: accessible-label removal, inactive-face isolation removal, stale Guided reveal role restoration, Arabic/mixed direction loss — four distinct probes executed (§7).
- Affected boundaries: presentation only. No storage schema, IDs, content, scheduler inputs/outputs, TTS adapter signatures, or public exports changed (`window.verbsEngine` API preserved; the delegated `data-action` surface unchanged except that a revealed Guided card no longer carries `data-action="flip"` — that removal IS the correction).
- Baseline tests and known failures: RED run of the new unit cases (10 pass / 4 fail) and the complete reference spec with new tests at base (44 pass / 5 fail, each at its intended target assertion); known pre-existing words-audio baseline failure re-verified at this package's base in isolation (§6); SC-INFRA-01 Mobile Chrome parallel flakes unchanged (§6).
- Empirical pre-work recorded for reviewer transparency: a disposable Playwright probe confirmed (a) Playwright 1.61.1's `getByRole` does NOT exclude `inert` subtrees, and (b) Chrome's real accessibility tree (CDP `Accessibility.getFullAXTree`) DOES exclude them, and that AX nodes resolve to DOM faces via `DOM.resolveNode`. The a11y-tree evidence in C1-A11Y-002 therefore uses the CDP accessibility tree, not role selectors.

## 3. Changes and rationale

| File | Change/purpose | Contract/finding | Compatibility impact |
|---|---|---|---|
| `js/core/shared-card.js` | `renderCardAffordances`: speak and favorite buttons carry explicit `aria-label`s (speak mirrors its adapter-supplied title, default 'Speak verb'; favorite is the constant 'Toggle Favorite') while favorite keeps `aria-pressed`. `renderCardFront`: new `inert` flag renders the front face inert on demand. `renderExampleBlock`: language-neutral API (`sourceText`/`sourceLang`/`translation`/`translationLang` + `label`) with `translationDirection()` helper (ar→rtl, en/de→ltr, else auto), `dir`+`lang` metadata on the translation line, `lang` on the German sentence button. `renderSharedCard`: new `activatable` option (default = `flippable`) gates the flip action/role/tabindex/aria-label; the hidden back face renders `inert` until flipped. | SC2-C1-A11Y-001/002/003; SC2-C1-DESIGN-001; FP-DESIGN-009; LF-CARD | Pure module; no new dependencies; existing exported names unchanged |
| `js/core/verbs-engine.js` | Ordinary adapter: `_buildOrdinaryFrontHtml` passes `inert: this.isFlipped`; `_buildOrdinaryBackHtml` passes the language-neutral view-model (`translationLang: 'en'` — the Verbs dataset's translations ARE English, declared rather than assumed); `flipCard` toggles `inert` on both faces via `toggleAttribute` alongside the existing surgical class toggle and lazy back fill. Guided adapter: `renderChallengeRecall` passes `inert: revealed` on the shared front, the language-neutral example view-model, and `activatable: !revealed` to the shared shell. | SC2-C1-A11Y-002/003; SC2-C1-DESIGN-001; GC-UI-001/002/005 | Public exports preserved; scheduler/grading/latency/persistence/token guards untouched; `flipCard` still a class toggle (focus + transition counting preserved); guided full re-render unchanged |
| `css/shared-card.css` | `.ex-en-line` renamed `.ex-translation-line` with `text-align: start` so alignment follows the rendered direction (identical LTR appearance for English). | SC2-C1-DESIGN-001 | Scoped under `.shared-card-block`; glossary styles untouched |
| `tests/unit/shared-card.test.mjs` | 4 new C1 cases (accessible names + aria-pressed stability; inert face emission incl. `renderCardFront` inert support; non-activatable shell without action affordances; language-neutral en/ar/mixed/unknown/missing + escaping). Superseded updates: the 4 example-block cases moved to the new API/selector; the unrevealed-shell case asserts the inert empty back face. | TS-PRE-001/003; FP-TEST-001 | None |
| `tests/e2e/cefr-card-reference.spec.js` | New `axTreeButtonFaces` CDP helper (real accessibility-tree evidence) and 5 new C1 tests (§4). Superseded updates: 17 `.ex-en-line` selector references → `.ex-translation-line`; CHAR-22's post-reveal no-op click moved from the hidden (now inert) front prompt to the displayed back face — same proof intent (post-reveal card-body click neither hides the answer nor grades), truthful click surface. | CM-MOD-002/003; TS-TEST-001/008 | Fixture unchanged (byte-identical) |

Approach rationale: all four corrections live in the shared presentation module with the adapters passing explicit state (`inert`, `activatable`, `translationLang`) — the renderer stays pure (proven again by the unchanged source-scan unit test), and the ordinary flip remains a surgical class toggle so the keyboard-focus and per-flip transition-counting proofs from SHARED-CARD-002 survive. `inert` was chosen over aria-hidden/tabindex surgery because it deterministically removes the subtree from focus, Tab order, pointer hit-testing AND the accessibility tree in one standard attribute (Chromium 102+). Scope deviations: none. Superseded test ledger (old → new, all re-pinned to approved targets): unit example-block cases `{de,en}` → language-neutral fields; unit `.ex-en-line` → `.ex-translation-line` (with dir/lang); unit unrevealed-shell empty-back assertion → empty AND inert back; E2E CHAR-03/04/06/07/09b/12/20/24 + SC-02 TARGET `.ex-en-line` → `.ex-translation-line`; CHAR-22 post-reveal no-op click target `.guided-prompt-main` (hidden front) → `.guided-answer` (displayed back) — forced by the approved inert isolation, which makes the hidden front physically unclickable; CHAR-06's absence assertion now targets `.ex-translation-line` while the new C1-DESIGN-001 test additionally pins the retired selector's permanent absence.

## 4. Acceptance-to-evidence mapping

| Criterion / finding | Test path + exact name | Result | Log/artifact | Tested revision |
|---|---|---|---|---|
| SC2-C1-A11Y-001 — descriptive accessible names for icon-only Speak/Favorite | E2E `C1-A11Y-001 (SC2-C1-A11Y-001)`; unit `SHARED-CARD-002-C1 unit: icon-only Speak and Favorite…` | PASS | `c1-targeted-chromium.log`, `c1-targeted-mobile-chrome.log`, `test-units.log` | `ebc9095` |
| SC2-C1-A11Y-001 — Favorite aria-pressed correct and name stable across toggle | E2E `C1-A11Y-001` (false→true→false with role lookups and glyph-name negatives at every step) | PASS | same | `ebc9095` |
| SC2-C1-A11Y-002 — before reveal only the front face is exposed | E2E `C1-A11Y-002 (ordinary)` pre-reveal block (front active, back inert, front controls Tab-reachable) | PASS | same | `ebc9095` |
| SC2-C1-A11Y-002 — after ordinary reveal hidden-front controls unfocusable and absent from the accessible tree | E2E `C1-A11Y-002 (ordinary)` post-reveal block: `inert` property on both faces, full 70-Tab traversal never focuses a front control while back controls are reached, and Chrome's real accessibility tree (CDP `Accessibility.getFullAXTree` + `DOM.resolveNode` face resolution) contains exactly one "Speak Verb" button — on the back | PASS | same | `ebc9095` |
| SC2-C1-A11Y-002 — flipping back restores front-face accessibility | E2E `C1-A11Y-002 (ordinary)` flip-back block (inert swapped back, front control Tab-reachable again, answer side emptied — secrecy preserved) | PASS | same | `ebc9095` |
| SC2-C1-A11Y-002 — Guided revealed back exposes controls while hidden front isolated | E2E `C1-A11Y-002 (guided)` | PASS | same | `ebc9095` |
| SC2-C1-A11Y-003 — pre-reveal Guided card keyboard reachable, Enter reveals exactly once | E2E `C1-A11Y-003` pre-reveal block (+ existing SC2-KEY-ENTER/SPACE, CHAR-26a/b) | PASS | same | `ebc9095` |
| SC2-C1-A11Y-003 — post-reveal card advertises no actionable reveal (no flip action / button role / keyboard target / reveal label / pointer affordance) | E2E `C1-A11Y-003` post-reveal block (`not.toHaveAttribute` on data-action/role/tabindex/aria-label, `[aria-label*="reveal" i]` count 0, computed `cursor: default`) | PASS | same | `ebc9095` |
| SC2-C1-A11Y-003 — revealed grading/Listen controls independently accessible | E2E `C1-A11Y-003` (role/name lookups for Listen / I knew it / I forgot + real Tab reachability); spacer/transition Continue controls share the same adapter-owned actionsHtml path below the card (proven construction + guided regression suite) | PASS | same + `regression-verbs-guided-chromium.log` | `ebc9095` |
| SC2-C1-DESIGN-001 — language-neutral API/selector with direction metadata | unit `SHARED-CARD-002-C1 unit: example translations are language-neutral…` (en ltr/lang, ar rtl/lang, mixed auto/no-lang, unknown auto/no-lang, missing → no line, German `lang="de"`, escaping) | PASS | `test-units.log` | `ebc9095` |
| SC2-C1-DESIGN-001 — existing German Verbs rendering visually and behaviorally compatible | E2E `C1-DESIGN-001` (dir/lang/en text on the real page, retired selector absent, sentence click still speaks `de-DE` through the real adapter) + all carried-forward example CHARS on both projects (unchanged expected texts, new selector) | PASS | `full-spec-chromium.log`, `full-spec-mobile-chrome.log` | `ebc9095` |
| No vocabulary content invented or translated | Adapter passes only `firstPair.de`/`firstPair.en` values that already exist in the dataset; the renderer never generates text (unit source-scan + escaping tests) | PASS | `test-units.log` | `ebc9095` |
| Mandatory RED evidence before production edits | `red-unit.log` (10 pass / 4 fail) + `red-e2e-full-spec-chromium.log` (44 pass / 5 fail, each at its intended target assertion) at base `3af8219` | PASS | both logs | `3af8219` |
| Accessibility-tree AND real keyboard evidence (report requirement) | CDP accessibility-tree face resolution + real Tab traversals inside `C1-A11Y-002`/`C1-A11Y-003` (no programmatic focus anywhere; `tabWalk`/`tabUntilFocused` press real keys) | PASS | `c1-targeted-*.log` | `ebc9095` |
| Fault probes — four assigned families | §7 (4/4 KILLED) | PASS | `probe1–4` logs | `ebc9095` (disposable worktree) |
| Regression: complete reference spec both projects | 49/49 Chromium + 49/49 Mobile Chrome | PASS | `full-spec-*.log` | `ebc9095` |
| Regression: units + affected Verbs/Guided + complete tracked suite | 73/73 units; 31/31 affected; 194/3/1 complete suite with full classification (§6) | PASS with declared baseline failures | respective logs | `ebc9095` / base `3af8219` |

## 5. Complete command ledger

| Command (exact) | Phase + revision | Duration | Exit | Passed/failed/skipped | Artifact | Interpretation |
|---|---|---|---|---|---|---|
| `npx playwright install chromium` | environment restore (reprovisioned sandbox lost the pinned browser cache) | ~40s | 0 | — | (not a test; noted for reproducibility) | chromium-headless-shell v1228 installed |
| `node --test tests/unit/shared-card.test.mjs` | RED, base `3af8219` | <1s | 1 | 10/4 | `red-unit.log` | All 4 new C1 unit cases fail at intended assertions; 10 pre-existing pass |
| `npx playwright test tests/e2e/cefr-card-reference.spec.js --project=chromium` | RED, base `3af8219` | 1.5m | 1 | 44/5 | `red-e2e-full-spec-chromium.log` | All 44 pre-existing tests pass; all 5 new C1 tests fail at intended target assertions (details in INDEX) |
| `node --input-type=module --check < js/core/{shared-card,verbs-engine}.js` | syntax gate, working tree | <1s | 0 | — | (trivial) | TS-LOOP-001 module-mode parse |
| `node --test tests/unit/shared-card.test.mjs` | unit, working tree (= `ebc9095` content) | <1s | 0 | 14/0 | `test-units.log` (full-units run) | Targeted unit layer green |
| `npx playwright test tests/e2e/cefr-card-reference.spec.js --project=chromium --grep "C1-"` | targeted correction, Chromium | 7.2s | 0 | 5/0 | `c1-targeted-chromium.log` | All four findings corrected |
| same, `--project="Mobile Chrome"` | targeted correction, Mobile Chrome | 5.8s | 0 | 5/0 | `c1-targeted-mobile-chrome.log` | Same |
| `npx playwright test tests/e2e/cefr-card-reference.spec.js --project=chromium` | complete spec, attempt 1 | 1.7m | 1 | 48/1 | (overwritten by the rerun — see INDEX note) | CHAR-22 post-reveal click timed out on the now-inert hidden front; base worktree rerun of CHAR-22 passed → intended consequence of the correction, superseded (§3) |
| `npx playwright test tests/e2e/cefr-card-reference.spec.js --project=chromium --grep "CHAR-22"` | supersession verification | 2.4s | 0 | 1/0 | (inline; same test in final run) | Corrected CHAR-22 green |
| `npx playwright test tests/e2e/cefr-card-reference.spec.js --project=chromium` | complete spec, final | 1.2m | 0 | **49/0** | `full-spec-chromium.log` | Complete corrected spec, Chromium |
| same, `--project="Mobile Chrome"` | complete spec, final | 1.1m | 0 | **49/0** | `full-spec-mobile-chrome.log` | Complete corrected spec, Mobile Chrome |
| `npm run test:units` | units (task said `npm.cmd` — Windows launcher; Linux sandbox uses `npm`) | ~3s | 0 | **73/0** | `test-units.log` | All tracked units incl. 4 new C1 cases |
| `npx playwright test tests/e2e/verbs.spec.js tests/e2e/verb-guided-challenge.spec.js tests/e2e/verbs-dashboard-trophies.spec.js --project=chromium` | affected regression | 1.2m | 0 | **31/0** | `regression-verbs-guided-chromium.log` | Verbs/Guided/dashboard suites green |
| `npx playwright test` | complete tracked suite (once, after stabilization) | 6.5m | 1 | **194/3/1** | `full-tracked-suite.log` | Broad gate; failures classified below |
| `npx playwright test tests/e2e/words-audio.spec.js:48 --project=chromium` | isolation | ~30s | 1 | 0/1 | `isolated-words-audio-chromium.log` | Consistent failure |
| `npx playwright test tests/e2e/words-audio.spec.js:48 --project="Mobile Chrome"` | isolation | ~25s | 1 | 0/1 | `isolated-words-audio-mobile.log` | Consistent on both projects |
| same words-audio test, `--project=chromium`, disposable worktree at base `3af8219` | isolation at base | ~20s | 1 | 0/1 | `isolated-words-audio-BASE.log` | **Pre-existing at this package's base** — declared for owner waiver |
| `npx playwright test tests/e2e/verb-guided-challenge.spec.js:736 --project="Mobile Chrome"` | isolation | 2.3s | 0 | **1/0** | `isolated-gc07-mobile.log` | GC-07 passes isolated → SC-INFRA-01 flake class |
| probe runs 1–4 (§7; disposable worktree at `ebc9095`, tests unmodified) | fault probes | ~5–30s each | 1 | as recorded | `probe1–4` logs | 4/4 KILLED; byte-identical restores |
| `git diff --check` / `git diff --cached --check` | gates | <1s | 0 | — | INDEX notes | Clean at every stage |

No background/duplicate runs were launched (TS-LOOP-006/PX-008): each command above is a single sequential execution; the one complete-spec rerun is the documented, diagnosed CHAR-22 supersession. Zero-selection checks: every `--grep` used was verified non-zero (RED 5, C1 5, CHAR-22 1, probes 1 test each).

## 6. Regression and integration

- Ordinary Verbs: full 49-test reference spec green on both projects — card/grade/nav/favorite/audio flows, secrecy (SC-01 sweep), 44×44 targets, focus visibility, reduced motion, keyboard activation all preserved (CHAR-01..20, SC-0x TARGETs, SC2-*).
- Guided Verbs: 21/21 guided spec tests green under Chromium (GC double-event protections, T3 migration, review flows) + 31/31 affected regression incl. dashboard trophies; the token guards and delegated actions are unchanged (a revealed recall card simply no longer carries `data-action="flip"`, which is the correction).
- Complete tracked suite: 194 passed / 3 failed / 1 skipped. Classification: `words-audio.spec.js:48` failed on both projects — consistent in isolation and **reproduced identically at the unmodified base `3af8219`** (words page highlight-clear timing; outside this WP's permitted scope; same pre-existing failure already declared at `7747d3c` in the SHARED-CARD-002/01 evidence — awaiting the same owner waiver; never counted as a pass). `GC-07` Mobile Chrome failed only in the parallel suite and passes isolated → SC-INFRA-01 pre-existing flake class. The 1 skip is the pre-existing words-audio voice-dependency skip, unchanged.
- Legacy storage / refresh / isolation: untouched (no schema, key or write-path change; `git diff --name-only 3af8219..ebc9095` = the five permitted code/test files).
- Audio adapter text AND language: `C1-DESIGN-001` (sentence click → `Ich mache die Hausaufgaben.` / `de-DE`); all carried-forward audio assertions (CHAR-12, CHAR-25, SC-01 audio, CHAR-14a/b) green.
- Browser/module startup and console errors: CHAR-19 + guided console-error tests green.
- Desktop/mobile/keyboard/accessibility-tree: both projects; real Tab navigation only (no programmatic focus as focus-visibility proof); CDP accessibility-tree evidence for inert exclusion; Mobile Chrome is emulation (no real-device/Safari certification).
- Untested/blocked: real-device/Safari; hardware voice output; Arabic/mixed examples on the real Verbs page (the dataset's translations are English — the renderer's language-neutral behavior is proven at unit level; A1/B2/Arabic UI adoption belongs to SHARED-CARD-003 per LF-DATA).

## 7. Test-quality and fault-probe evidence

| Probe | Risk/finding | Production target + exact mutation | Detecting test | Baseline / syntax result | Actual failure | Classification | Integrity proof |
|---|---|---|---|---|---|---|---|
| 1 | Accessible label removal / SC2-C1-A11Y-001 | `shared-card.js` favorite button loses `aria-label="Toggle Favorite"` | E2E `C1-A11Y-001` | passing; syntax OK | `toHaveAttribute('aria-label', 'Toggle Favorite')` — Expected `"Toggle Favorite"`, Received `""` | **KILLED** | `shared-card.js` sha256 restored byte-identical |
| 2 | Inactive-face isolation removal / SC2-C1-A11Y-002 | `verbs-engine.js` `flipCard`: both `toggleAttribute('inert', …)` lines removed | E2E `C1-A11Y-002 (ordinary)` | passing; syntax OK | `expect(faces.front).toBe(true)` — received `false` (post-reveal front not inert) | **KILLED** | `verbs-engine.js` restored byte-identical |
| 3 | Stale Guided reveal role/label restoration / SC2-C1-A11Y-003 | `verbs-engine.js` `renderChallengeRecall`: `activatable: !revealed` → `activatable: true` | E2E `C1-A11Y-003` | passing; syntax OK | `not.toHaveAttribute('data-action')` — revealed card still advertises the flip action | **KILLED** | restored byte-identical |
| 4 | Arabic/mixed direction loss / SC2-C1-DESIGN-001 | `shared-card.js` `translationDirection` always returns `'ltr'` | unit `SHARED-CARD-002-C1 … language-neutral` | passing; syntax OK | Arabic case — expected `dir="rtl" lang="ar"`, mutant rendered `dir="ltr" lang="ar"` (13/14, one failure) | **KILLED** | restored byte-identical |

Probe selection: the four distinct assigned families exactly (TS-MUT-006); mutations applied only to production files in a disposable worktree at `ebc9095`; tests, fixtures, config and runner untouched (TS-MUT-002/007). Integrity: `js/core/shared-card.js` = `17a515f8…29cf39a` and `js/core/verbs-engine.js` = `c3b194c7…ab2986` recorded before the probes and re-verified byte-identical after each restore. No SURVIVED, INVALID_MUTATION, BASELINE_INVALID or INFRASTRUCTURE_FAILURE outcomes; no expected results were rewritten; no conditional assertions, weak mocks, ignored errors or snapshot changes introduced. Additional test-quality notes: the a11y-tree assertion uses Chrome's real accessibility tree (CDP) rather than Playwright role selectors because a pre-work probe proved role selectors do not exclude `inert` subtrees in Playwright 1.61.1 — the evidence mechanism was chosen for truthfulness, not convenience; the RED run proves every new C1 test fails at its intended target assertion before the fix.

## 8. Dead-code and dependency inventory

| Candidate symbol/path | Searches + dynamic caller checks | Classification | Disposition | Regression proof |
|---|---|---|---|---|
| `.ex-en-line` CSS rule (`css/shared-card.css`) | `rg` across `verbs.html css js tests`: after the rename, the only production reference was the rule itself; markup emitters all use `.ex-translation-line` | PROVEN_ORPHAN (created by this change) | Removed (renamed rule carries the styles forward) | 49/49 both projects; `C1-DESIGN-001` pins the retired selector's absence |
| `.ex-en-line` selector references in tests | `rg`: 17 superseded references updated; the single remaining reference is the C1-DESIGN-001 retirement probe (intentional) | DYNAMICALLY_USED (as a negative probe only) | Kept (documented) | `C1-DESIGN-001` |
| `renderExampleBlock` old `{de, en}` signature | `rg` across `js/ tests/`: no caller passes `de`/`en` anymore (both adapters + all tests use the language-neutral fields) | PROVEN_ORPHAN (superseded signature) | Removed with the API change | unit 14/14; full spec green |
| `renderCardAffordances` `speak.title` default ('Speak' → 'Speak verb') | both adapters always pass `title: 'Speak Verb'`; the default only guards direct renderer use | DYNAMICALLY_USED (defensive default) | Kept | unit accessible-names case |
| `inert` / `activatable` / `translationDirection` / `axTreeButtonFaces` | all new symbols exercised by the new tests; `translationDirection` internal-only by design | DYNAMICALLY_USED | Kept | C1 suites + units |
| Remaining inline `onclick` in `verbs-engine.js` (email-auth modal, glossary rows) | pre-existing, outside card rendering — unchanged from the SHARED-CARD-002 inventory | DUPLICATE_BUT_LIVE (pre-existing) | Untouched, reported | glossary/audio tests green |

Search boundary: `rg` over `verbs.html`, `css/`, `js/`, `tests/` for every removed/renamed symbol and selector, plus the delegated `data-action` inventory in `bindEvents`. No new dependency, lockfile or config change; no IDs, content or storage shapes touched (`git diff --name-only 3af8219..ebc9095` = `css/shared-card.css`, `js/core/shared-card.js`, `js/core/verbs-engine.js`, `tests/e2e/cefr-card-reference.spec.js`, `tests/unit/shared-card.test.mjs`).

## 9. Findings, limitations, and handoff

| Finding ID | Severity/impact | Reproduction and evidence | Disposition | Owner decision needed |
|---|---|---|---|---|
| SC2-C1-A11Y-001 — accessible names | medium (a11y) | RED `red-*.log` → `c1-targeted-*.log` (aria-labels, role lookups, glyph-name negatives, aria-pressed across toggles) | **fixed** | none |
| SC2-C1-A11Y-002 — inactive face isolation | high (a11y) | `C1-A11Y-002` ordinary + guided (inert both directions, 70-Tab traversal, CDP accessibility-tree exclusion, flip-back restore, back emptied) | **fixed** | none |
| SC2-C1-A11Y-003 — truthful Guided semantics | high (a11y) | `C1-A11Y-003` (pre-reveal reachability + exactly-one reveal; post-reveal: no action/role/tabindex/aria-label/reveal-name/pointer affordance; Listen + grading reachable by name and keyboard) | **fixed** | none |
| SC2-C1-DESIGN-001 — language-neutral example API | medium (design/future i18n) | unit language-neutral case (en/ar/mixed/unknown/missing + escaping) + `C1-DESIGN-001` on the real page; German Verbs rendering visually and behaviorally compatible (same styles via renamed rule, same speak behavior, all carried-forward CHARS green) | **fixed** | none |
| CHAR-22 supersession (post-reveal no-op click) | low (test truthfulness) | first complete-spec run 48/1 (timeout: hidden front inert → `verb-card-back` intercepts pointer events); base worktree rerun passed → intended consequence; assertion superseded to the displayed back face with unchanged proof intent | **corrected + documented** (§3 ledger) | reviewer confirmation |
| words-audio baseline failure | medium (evidence hygiene) | `isolated-words-audio-{chromium,mobile}.log` + `isolated-words-audio-BASE.log`: fails identically at this package's unmodified base `3af8219` (same failure class already proven at `7747d3c`) | **open (pre-existing, proven)** — words page outside permitted scope; never counted as a pass | owner waiver or fix assignment |
| SC-INFRA-01 suite flakiness | low | GC-07 Mobile Chrome failed in the 6.5m parallel suite, passed isolated (`isolated-gc07-mobile.log`) | **open (pre-existing)** | optional owner triage |
| BL-03 scheduler mismatch | — | untouched by design (`verb-challenge-engine.js` never opened for edit) | **deferred** (unchanged) | separate owner triage |

- Explicit confirmation: scheduler, grading, latency, persistence, vocabulary content, and stored IDs were NOT changed. The pure challenge engine (`js/core/verb-challenge-engine.js`) was never opened for edit; no storage key, schema or write path was touched; no vocabulary data was invented or translated (the adapters declare `translationLang: 'en'` for existing dataset translations; the renderer only derives direction metadata); all verb IDs and stored progress shapes are byte-identical.
- Remaining product risks: Mobile Chrome is emulation; keyboard focus after a full Guided re-render still falls back to `body` (pre-existing guided behavior, consciously preserved — the revealed card being intentionally non-focusable is the correction, not a regression); Playwright's own role selectors do not reflect `inert` (evidence therefore uses Chrome's real accessibility tree via CDP — documented in §2/§7).
- Environment restrictions/notes: sandbox reprovision on 2026-08-30 removed the pinned browser cache → `npx playwright install chromium` run once (recorded in the ledger); `npm.cmd` executed as `npm` (Linux); credential handling per GITHUB_DELIVERY.md for the push (token never printed/committed).
- Final diff/status: implementation commit `ebc90953c8f721d11faf6fbb91331277064614be` touches exactly the five permitted code/test files; `git diff --check` and `git diff --cached --check` clean; this report/evidence commit is docs-only.
- Next proposed WP: SHARED-CARD-003 (adopt shared cards in ordinary levels) — NOT started; requires assignment.

## 10. Owner/reviewer disposition — not executor approval

- Reviewer and reviewed exact revision: pending review of `ebc90953c8f721d11faf6fbb91331277064614be` (code) + this delivery branch tip (docs).
- Verdict: pending
- Criteria/findings accepted or declined; explicit waivers and reason: for the reviewer — (1) the pre-existing words-audio baseline failure needs an explicit waiver or a separate fix assignment (proven at both `7747d3c` and this package's base `3af8219`); (2) the CHAR-22 supersession (post-reveal no-op click moved to the displayed back face) follows directly from the approved inert isolation — reviewer confirmation requested; (3) BL-03, SC-INFRA-01 remain deferred/open as characterized. This report does not approve itself; the executor claims READY_FOR_REVIEW only.
