# AUDIO-002 — Attempt 01 Delivery Report

## 1. Identity and status

- Status: READY_FOR_REVIEW (all engineering work complete and evidenced; the final delivery push is executed after this docs-only commit — final status is returned in the handoff message)
- Assigned goal and task type: adapt the existing Verbs autoplay to the accepted AUDIO-001 speech planner through an explicit adapter (no duplicated ordering logic in the controller) while preserving the existing Verbs UX (repeat, examples, include-English, start-at, floating player, highlight/scroll, pause/resume/stop/completion, row-play, individual pronunciation, ordinary + Guided Verbs), and strengthen `SpeechQueue` ownership so stale callbacks from an old queue can never affect a replacement queue. Task type: **MODIFICATION + BUGFIX** with REFACTOR discipline (behavior-preserving planner adoption). CM gates applied: CM-PRE-001..006, CM-MOD-001..004, CM-FIX-001..005, CM-REF-001..006.
- Owner assignment reference: owner task messages "TASK: AUDIO-002 — Verbs Autoplay Adapter Compatibility" and the AUTHENTICATION_BLOCKED resume message with a fresh PAT; accepted base `4bd13a94df52755c27bb352ef5b87d1198a4fe21` (AUDIO-001-C2 delivery).
- GitHub task branch: `codex/glm-audio-002-01`; base commit `4bd13a9…`; commits: tests `d5e5949` (RED-first, per DR-007), final tested code `e32352c` (adapter + queue hardening), tests supplement `300a9c2` (adds the B11 empty-list characterization after mapping review of verification item 22 — the WORK_PACKAGES "Tests" row explicitly lists an "empty list" case; production source unchanged). The docs-only commit that follows adds the report and evidence and does not move the tested code revision.
- Evidence index: `docs/cefr/evidence/AUDIO-002/01/INDEX.md`
- Final delivered SHA is returned after commit/push in the handoff message, not embedded into its own commit.
- Executor/model and sandbox OS/runtime versions: GLM executor (Super Z), Debian sandbox, Node v24.19.0, npm 11.17.0, Playwright 1.61.1 (lockfile), Python 3.12.14 (static server). Environment event: Playwright's Chromium build `1228` was missing and was installed once via `npx playwright install chromium` (download only; system deps already present) before any browser evidence.
- Start/end time: 2026-09-01 ~00:10–02:30 (sandbox clock); work directory: repository root.
- Dependency acceptance references: AUDIO-001-C1 accepted (planner `2f51c07` lineage; module sha256 at this base `1c2f26ab…` — byte-identical here, unchanged by this package); AUDIO-001-C2 accepted (base `4bd13a9`).

## 2. Required reading and pre-edit plan

- Paths fully read (order per AGENTS.md and the assignment): `AGENTS.md`; `contracts/README.md`; `contracts/PORTABLE_AGENT_EXECUTION.md`; `contracts/CHANGE_MANAGEMENT.md`; `contracts/CODE_FINGERPRINT.md`; `contracts/TESTING_AND_SUCCESS.md`; `contracts/DEAD_CODE_AND_REFACTORING.md`; `contracts/DELIVERY_REPORTING.md`; `contracts/LEVEL_FLASHCARD_STANDARD.md`; `docs/cefr/BASELINE.md`; `docs/cefr/WORK_PACKAGES.md` (AUDIO-002 section + common requirements); `docs/cefr/ACCEPTANCE_MATRIX.md` (AC-11, AC-12, AC-03, AC-21, AC-22 rows); `docs/cefr/GLM_HANDOFF.md`; `docs/cefr/GITHUB_DELIVERY.md`; report template and the AUDIO-001-01/C1/C2 reports and evidence INDEXes (conventions). Source and tests read completely for the touched boundary: `js/core/speech-plan.mjs`; `js/core/tts.js`; `js/core/verbs-engine.js` (autoplay/audio paths in full; queue construction, `_getExamplePairs`, renderTable rows, bindEvents row-play and keyboard context); `verbs.html` (audio settings drawer, floating player, module import); `tests/unit/speech-plan.test.mjs`; `tests/e2e/verbs.spec.js`; `tests/e2e/phrases-conversations.spec.js` (speech-mock harness); `js/core/app.js` SpeechQueue consumers (playAllWords/playAllPhrases/stopAudioQueue/togglePausePhrases/speakPhrase); `tests/unit/shared-card.test.mjs` (VM-loader pattern); `package.json`, `playwright.config.js`, the verbs dataset JSON (fixture fidelity).
- Relevant contract IDs: FP-DESIGN-010 (planning is a pure calculation; the adapter mapping is kept pure and module-level), FP-DESIGN-004 (verbs-engine owns orchestration; ordering delegated to the planner), FP-FLOW-005 / TS-TEST-004/005 (no timing sleeps; mock the platform not the app; independent oracles), LF-AUDIO (autoplay options; no mixed text under a single-language voice), AC-11/AC-12 (deterministic steps; lifecycle agreement and stale-callback immunity), TS-MUT-001..007 (probe discipline), DR-006/007/008 (evidence, tests-first, delivery), PX-005/006 (task branch, named new files inside authorized families).
- Observed conflicts: none. The words-audio:48 baseline failure is a known pre-existing gap (below), not a conflict with this package.
- Before behavior (measured at base `4bd13a9`): Verbs autoplay ordering is hand-built inside `playAllVerbsAudio` (item/repeat loops with inline example/translation emission — logic the accepted planner already owns); the queue's no-synthesis fallback timer is UNTRACKED, so `pause()`/`stop()` cannot cancel it (a stale fallback advances the paused cursor and fires completion early; a replaced queue's stale fallback advances the replacement); an out-of-range Start-At value produces an empty play list, leaving the controls in a playing state with no queue; empty-text steps are queued for the runtime to skip. Approved after behavior: the controller maps verb records onto the planner input boundary and maps planned steps back to queue records (shape-identical to the previous items); every start source is clamped; a plan with no speakable steps leaves the controls untouched; `SpeechQueue` mints a generation on every `stop()`/`pause()`, tracks the fallback timer, and guards all five async boundaries.
- Explicit non-goals: no redesign of the Verbs audio controls or UI (same buttons, drawer, floating pill, highlight classes), no changes to `app.js`/Phrases/Conversations behavior, no Guided Challenge scheduler/grading changes, no storage/SRS changes, no `speech-plan.mjs` changes (no compatibility defect demonstrated — its input boundary already fits the adapter), no verbs.html/CSS changes (the module import chain already supports the planner import), no content/ID/dependency changes, no AUDIO-003 scope (level autoplay, `app.speakText` language forwarding).
- Exact write paths (pre-edit plan, as delivered): modify `js/core/tts.js`, modify `js/core/verbs-engine.js`, new `tests/unit/verbs-audio.test.mjs`, new `tests/e2e/verbs-audio.spec.js`, new `docs/cefr/reports/AUDIO-002-01.md`, new `docs/cefr/evidence/AUDIO-002/01/**`. Not written: `speech-plan.mjs`, `speech-plan.test.mjs`, `verbs.html`, verb styles, `verbs.spec.js`, `phrases-conversations.spec.js`, `app.js`.
- Risk: high (shared queue semantics + adapter correctness) → five fault-probe families mandated and delivered; the 25 verification items below are the acceptance-to-test map.
- Baseline tests and known failures at base: units 115/115; verbs.spec.js chromium 5/5; phrases-conversations chromium 11 pass + 1 skip; words-audio chromium 0/1 — the documented pre-existing words-page highlight-clear failure (F-A002-1).

## 3. Changes and rationale

| File | Change/purpose | Contract/WP criterion | Compatibility impact |
|---|---|---|---|
| `js/core/tts.js` | `SpeechQueueClass` ownership hardening: new `_generation` token minted on every `stop()`/`pause()`; new tracked `_noSynthesisTimer` cleared like the other timers; generation guards added to the delayed-speak callback, utterance `onend`, utterance `onerror`, the watchdog, and the no-synthesis fallback. | AC-12; AUDIO-002 "Invalidate stale callback tokens when queue ownership changes"; B1–B10 | Public queue API unchanged (`playAll/pause/resume/stop/speakSingle/isPlaying`), so app.js Phrases/Conversations consumers are unaffected (regression-proven). Purely additive guards; all previously-correct behaviors retained. |
| `js/core/verbs-engine.js` | Planner adoption: new pure module-level adapter `mapVerbToSpeechCard` (verb + example pairs → normalized planner card) and `mapSpeechStepsToQueueItems` (planner steps → queue records with stable verb identity, planner language, preserved label shape); `playAllVerbsAudio` now calls `planSpeechSequence` with the control-derived options instead of hand-building the sequence; every start source is clamped to the last verb and synced into the Start-At dropdown; an empty planned sequence returns before touching the controls. | AC-11; AUDIO-002 "adapt existing Verbs autoplay to the planner without redesigning controls"; A1–A12 | Queue-record shape is byte-for-byte the same fields as before (`verbId/verbInfinitive/verbIndex/text/lang/label`); highlight/scroll/floating-pill callbacks unchanged; behavior differences are the three defect fixes (clamp, empty-plan guard, planner-consistent skipping of empty text) — all RED-proven and disclosed. |
| `tests/unit/verbs-audio.test.mjs` (new) | 23 tests: A1–A12 adapter/planner integration via the tracked VM-loader pattern (real controller source, real planner injected, recording SpeechQueue stub, fake controls) and B1–B11 queue lifecycle via the real tts.js in a VM with mock `window.speechSynthesis` and `node:test` mock timers (B11: empty-`playAll` cleanup). | TS-TEST-004/005/006; AC-11/AC-12 | New file; unit total 115 → 138. |
| `tests/e2e/verbs-audio.spec.js` (new) | 8 real-controller tests with a deterministic speechSynthesis double installed before page load (text+lang recorded per utterance; `finishCurrent`; stale-utterance handles). New file justification (pre-edit plan): the harness must be installed via `addInitScript` before `verbs.html` boots and must not perturb `verbs.spec.js`, which stays an untouched regression anchor for the same controls. | TS-TEST-003/004; AC-12; C1–C3 | New file; both configured projects run it. |
| `docs/cefr/evidence/AUDIO-002/01/**` + this report | Sanitized evidence and the delivery report | DR-001..008 | None |

Root cause / why this approach: AUDIO-001 delivered the pure planner but left it unwired; the Verbs controller still duplicated the ordering logic (item-major repeats, example scope, translations, start index) inside controller code mixed with DOM and queue concerns, and the shared queue's async callbacks were only partially owned (`isPlaying` + utterance identity; the no-synthesis fallback timer was completely untracked). The adapter closes both gaps with the smallest structural change: the controller keeps orchestration (controls, UI state, highlight) and delegates ordering to the planner through two pure mapping functions, while the queue gains an explicit ownership token so every async boundary self-invalidates. The three behavioral fixes (clamp, empty-plan guard, skip-empty-text) are the concrete manifestations of "visible controls and actual utterance queue agree" and were reproduced RED before the fix. Scope deviations: none beyond the disclosed boundary refinements (F-A002-5). Tests or old assertions superseded: none (the new tests extend; no existing test was changed or weakened).

## 4. Acceptance-to-evidence mapping — the 25 verification items

All items PASS at the final tested code `e32352c` (unit evidence at tests revision `300a9c2`). Unit items ran via `node --test tests/unit/verbs-audio.test.mjs` (`04-green-focused-unit.log`, 23/23); E2E items ran on both projects (`05-green-focused-e2e-chromium.log`, `07-affected-mobile-chrome.log`, `09-full-e2e-both-projects.log`).

| # | Verification item (assignment) | Test path + exact name | Result | Log |
|---|---|---|---|---|
| 1 | Item-major repeat blocks preserved (term, English translation, first example, example-translation per repetition; repetitions before the next verb) | unit `AUDIO-002 A1: default controls plan term, English translation, and first-example steps per repetition in item-major order` | PASS | 04 |
| 2 | Repeat count honored (3×) | unit `AUDIO-002 A2: repeatCount 3 emits three full repetitions of a verb before anything else` | PASS | 04 |
| 3 | Example mode none | unit `AUDIO-002 A3: exampleMode none emits only the term and translation steps` | PASS | 04 |
| 4 | Example mode first | unit `AUDIO-002 A4: exampleMode first emits only the first example pair` | PASS | 04 |
| 5 | Example mode all | unit `AUDIO-002 A5: exampleMode all emits every example pair in source order` | PASS | 04 |
| 6 | Include-English on/off → `includeTranslation` | unit `AUDIO-002 A6: include-English unchecked omits every English translation steps` | PASS | 04 |
| 7 | Start-at selection (dropdown) | unit `AUDIO-002 A7: the Start-At dropdown selection begins the plan at that queue position` | PASS | 04 |
| 8 | Row-play start + dropdown sync | unit `AUDIO-002 A8: a row-play startIndex begins at that row and syncs the Start-At dropdown` | PASS | 04 |
| 9 | Out-of-range start clamped (never an empty queue / stuck controls) | unit `AUDIO-002 A9: out-of-range start values clamp to the last verb instead of producing an empty queue` (RED at base) | PASS | 04, 01 |
| 10 | Stable verb ID + source index + planner language + text on every queue record | unit `AUDIO-002 A10: planned queue items carry the stable verb identity, the planner language, and the text` | PASS | 04 |
| 11 | Missing text skipped by the planner, never substituted | unit `AUDIO-002 A11: verbs with missing text are skipped by the planner without substituting another language` (RED at base) | PASS | 04, 01 |
| 12 | Empty planned sequence never starts the queue or the playing UI | unit `AUDIO-002 A12: an empty planned sequence leaves the controls unchanged and never starts the queue` (RED at base) | PASS | 04, 01 |
| 13 | Ordered single speaks, per-item highlight, completion exactly once | unit `AUDIO-002 B1: playAll speaks each item once in order, highlights each item, and fires completion exactly once at drain` | PASS | 04 |
| 14 | Stop-before-start (delayed speak) prevents any utterance | unit `AUDIO-002 B2: stop during the delayed-speak window prevents any utterance` | PASS | 04 |
| 15 | Pause preserves cursor; late onend is a no-op | unit `AUDIO-002 B3: pause preserves the cursor and queue; a stale onend cannot advance or complete the paused queue` | PASS | 04 |
| 16 | Resume: same item exactly once, no skip/duplicate | unit `AUDIO-002 B4: resume after a pause during the speak delay re-speaks the current item exactly once` (plus B3's resume assertions) | PASS | 04 |
| 17 | Cancellation errors don't advance; genuine errors advance | unit `AUDIO-002 B5: canceled and interrupted utterance errors do not advance; a genuine error advances` | PASS | 04 |
| 18 | Watchdog advances stuck speech; cleared by pause | unit `AUDIO-002 B6: the watchdog advances a stuck utterance and is cleared by pause` | PASS | 04 |
| 19 | No-synthesis fallback advances with highlight; pause/stop fully cancel it | unit `AUDIO-002 B7: the no-synthesis fallback advances items with highlight and completes at drain`; `AUDIO-002 B8: pause and stop fully cancel the pending no-synthesis fallback timer` (B8 RED at base) | PASS | 04, 01 |
| 20 | Rapid restart starts the replacement exactly once at its first item | unit `AUDIO-002 B9: rapid restart speaks the replacement queue first item exactly once` | PASS | 04 |
| 21 | Stale callbacks cannot advance or complete the replacement (utterance + fallback variants) | unit `AUDIO-002 B10: stale callbacks from a replaced queue cannot advance or complete the replacement` (fallback variant RED at base) | PASS | 04, 01 |
| 22 | Empty input cleans the queue without completion | unit `AUDIO-002 B11: playAll with an empty list stops and cleans the current queue without firing completion` (characterization — passes at base, see the supplement log); E2E `[AUDIO-002] an empty filtered queue never enters the playing state` covers the controller-level empty queue | PASS | 04, 01 (supplement), 05 |
| 23 | Real controller: exact utterance text AND language sequence through the actual adapter (repeat, first example, include-English), floating player and highlight agree | E2E `[AUDIO-002] real controller speaks the exact planned sequence with per-step language` — asserts the 9 first utterances byte-exact (`werden`/de-DE … `haben`/de-DE), row highlight, pill text `Playing: #1 werden` | PASS (both projects) | 05, 07 |
| 24 | Pause/resume position, stop cleanup, completion resets controls exactly once, all steps spoken exactly once | E2E `[AUDIO-002] pause and resume keep the current position without skipping or duplicating an item`; `[AUDIO-002] stop cancels future speech, clears highlights, hides the floating player, and resets the controls`; `[AUDIO-002] completing the queue resets the controls exactly once and speaks every planned step exactly once` (start-at 47, 3-step drain, speakCount exactly 3) | PASS (both projects) | 05, 07 |
| 25 | Row-play start + rapid replacement + stale-utterance immunity + individual pronunciation stops autoplay + empty filtered queue never plays | E2E `[AUDIO-002] row-play starts at that row and syncs the Start-At dropdown`; `[AUDIO-002] a rapid restart replaces the queue exactly once and stale utterance callbacks cannot disturb it`; `[AUDIO-002] individual pronunciation stops the autoplay queue and speaks only that item`; `[AUDIO-002] an empty filtered queue never enters the playing state` | PASS (both projects) | 05, 07 |

Supporting criteria: ordinary + Guided Verbs regression — full tracked suite (303/2/1, only the pre-existing words-audio failures) covers `verb-guided-challenge.spec.js` and `verbs.spec.js`; individual pronunciation path (`speakText`/`speak`) is exercised by the E2E individual-pronunciation test and unchanged by the diff.

## 5. Complete command ledger

Every test command attempted, in execution order. Playwright runs use the config's own webServer (`python -m http.server 9012`, one suite at a time; PIDs awaited). "—" = not applicable.

| Command (exact) | Phase + revision | Exit | Passed/failed | Artifact | Interpretation |
|---|---|---|---|---|---|
| `git fetch origin`; `git ls-remote origin refs/heads/codex/glm-audio-002-01`; `git cat-file -t 4bd13a9…`; `git status --porcelain` | checkpoint gate @ 4bd13a9 | 0 | — | — | base present; branch absent; tree clean |
| `git checkout -b codex/glm-audio-002-01 4bd13a9…` | gate | 0 | — | — | branch created exactly from base |
| transient-credential push: `git -c credential.helper= -c credential.helper='!f(){ cat <chmod-600 file outside repo>; }; f' push origin HEAD:refs/heads/codex/glm-audio-002-01` | initial checkpoint @ 4bd13a9 (single push attempt, fresh PAT) | 0 | — | — | new remote branch; remote SHA verified `4bd13a9…` == local HEAD; credential file deleted immediately and verified absent; token never printed/committed/URL-embedded |
| `npm run test:units` | baseline @ 4bd13a9 | 0 | 115/0 | — (transcript-recorded) | pre-edit unit baseline |
| `npx playwright test tests/e2e/verbs.spec.js --project=chromium` | baseline @ 4bd13a9 | 1 | 0/5 | — (transcript-recorded) | ENVIRONMENT failure: `browserType.launch: Executable doesn't exist` (chromium_headless_shell-1228 missing); no test logic ran; disclosed as the environment event |
| `npx playwright install chromium` | environment setup | 0 | — | — | browser build 1228 downloaded (system deps already validated); fixes all subsequent browser runs |
| `npx playwright test tests/e2e/verbs.spec.js --project=chromium` | baseline retry @ 4bd13a9 | 0 | 5/0 | — (transcript-recorded) | verbs baseline green |
| `npx playwright test tests/e2e/phrases-conversations.spec.js --project=chromium` | baseline @ 4bd13a9 | 0 | 11 pass + 1 skip | — (transcript-recorded) | shared-consumer baseline green |
| `npx playwright test tests/e2e/words-audio.spec.js --project=chromium` | baseline @ 4bd13a9 | 1 | 0/1 | — (transcript-recorded) | pre-existing words-audio:48 failure reproduced at the assigned base (same class as SHARED-CARD-002/003); never counted as a pass |
| `node --check tests/unit/verbs-audio.test.mjs`; `node --input-type=module --check < tests/e2e/verbs-audio.spec.js` | tests authoring | 0 | — | 03 (re-captured later) | both new test files parse |
| `node --test tests/unit/verbs-audio.test.mjs` | RED attempt 1 (base source) | 1 | 1/21 | — (transcript-recorded) | harness defects found by the RED run itself: `clearTimeout` is not a mockable `apis` entry (10× TypeError), cross-realm strict deepEqual against VM objects, two test-authoring bugs (A2 expected array missing sehen repeats 2–3; A8 strict-equal on the number the base writes into the fake select). All corrected in the test file before the RED state was accepted; no production file touched |
| `node --test tests/unit/verbs-audio.test.mjs` | RED attempt 2 | 1 | 15/7 | — (transcript-recorded) | A2/A8 fixed; 5 intended defect failures remain + 2 stale copies of the same (captured cleanly next run) |
| `node --test tests/unit/verbs-audio.test.mjs` | **RED accepted** @ d5e5949 content | 1 | **17/5** | `01-red-unit.log` | failing set = exactly the five genuine defects: A9, A11, A12, B8, B10 |
| `npx playwright test tests/e2e/verbs-audio.spec.js --project=chromium` | RED E2E attempt 1 (base source) | 1 | 6/2 | — (transcript-recorded) | two test-authoring bugs (pause/resume drove one utterance too few before asserting; empty-queue test awaited a "No verbs match" text that renderTable never emits for a search-emptied queue). Corrected in the test file; no production file touched |
| `npx playwright test tests/e2e/verbs-audio.spec.js --project=chromium --reporter=line` | **RED E2E accepted** (base source) | 0 | **8/0** | `01-red-e2e.log` | characterization suite green on the UNMODIFIED base — the refactor's behavior-preservation baseline |
| `git add tests/…` + commit | tests commit | 0 | — | — | `d5e5949` (tests first per DR-007) |
| `node --check js/core/speech-plan.mjs`; `node --input-type=module --check < js/core/tts.js`; `node --input-type=module --check < js/core/verbs-engine.js` | implementation syntax @ e32352c content | 0 | — | `02-syntax-production.log` | all three parse in correct module modes |
| `node --test tests/unit/verbs-audio.test.mjs` | GREEN attempt 1 | 1 | 21/1 | — (transcript-recorded) | A9: the select-driven path did not sync the clamped start into the dropdown; fixed by writing the clamped start back after the clamp (controls agree with the queue) |
| `node --test tests/unit/verbs-audio.test.mjs` | **GREEN** @ e32352c content | 0 | **22/0** | `04-green-focused-unit.log` | all unit items pass |
| `npx playwright test tests/e2e/verbs-audio.spec.js --project=chromium` | GREEN E2E | 0 | 8/0 | `05-green-focused-e2e-chromium.log` | real-controller suite green on the adapter |
| `npx playwright test tests/e2e/{verbs,phrases-conversations,words-audio}.spec.js --project=chromium` | affected regression | 1 | 16/1 + 1 skip | `06-affected-chromium.log` | only the pre-existing words-audio failure (identical to baseline) |
| `npm run test:units`; `npx playwright test tests/e2e/verbs-audio.spec.js --project="Mobile Chrome"`; affected files on Mobile Chrome | ladder | 0 / 0 / 1 | 137/0; 8/0; 25/1 | `07-affected-mobile-chrome.log` (and 08) | full units 137; mobile new spec 8/8; mobile affected 25/1 (words-audio pre-existing) |
| `npx playwright test` (both projects, full tracked suite) | broad regression, run 1 (uncaptured full output; summary read from terminal) | 1 | 303/2 + 1 skip | — (transcript-recorded; superseded by captured runs) | only the two pre-existing words-audio failures; full output not retained — the captured runs below are the evidence |
| ladder capture script `/home/z/my-project/scripts/audio002-ladder.sh` (re-runs steps 02–10 with output capture) | evidence capture @ e32352c | — | as listed per log | `02..10` logs | one step timed out in the TOOL RESULT STREAM (the script kept running in the background and completed; no duplicate suite was launched — the stream-size error is a tooling artifact, not a test failure) |
| `npx playwright test` (captured, first capture run) | broad, captured | 1 | 300/5 + 1 skip | `09-full-e2e-first-run-flakes.log` | 2 pre-existing words-audio + 3 load flakes: R2 (chromium), GC-07 (mobile), verbs:33 (mobile) |
| `npx playwright test tests/e2e/verbs.spec.js:33 --project="Mobile Chrome"`; `…verb-guided-challenge.spec.js:1088 --project=chromium`; `…verb-guided-challenge.spec.js:736 --project="Mobile Chrome"` | TS-LOOP-005 failure protocol (isolation) | 0 ×3 | 3/3 pass | `09-flake-isolation-reruns.log` | all three first-run flakes pass in isolation → flakes, not regressions (verbs:33 is the OLD spec's dataset-readiness race; the empty-queue early return it hits is unchanged base code) |
| `npx playwright test` (both projects, final broad rerun) | final broad regression | 1 | **303/2 + 1 skip** | `09-full-e2e-both-projects.log` | only the two pre-existing words-audio failures; the flakes did not reappear |
| `python3 /home/z/my-project/scripts/audio002-probes.py` (worktree add → 5 probes → restore → worktree remove) | fault probes @ 300a9c2 copy | 0 | baseline 23/23; probe kills: 11, 4, 1, 2, 3 failing tests | `11-fault-probes.log` | all five KILLED; restores sha256-verified; one transparent probe correction (probe 2) documented in the log; probe 5's third failing test (B11) is documented cross-test collateral from B10's mid-test abort (B11 passes in isolation under the mutant — verified with `--test-name-pattern B11`) |
| B11 mapping-review cycle: add test → run at base (disposable worktree at `d5e5949` + copied final test file) → run in tree → refresh affected evidence | supplement | 0 | 18/5 at base (B11 passes); 23/23 in tree; 138/0 full units; 8/0, 16/1+1 skip, 25/1 affected runs re-captured | `01-red-unit-b11-supplement.log`, refreshed `04/05/06/07/08` | the E2E logs (05–09) remain valid: Playwright's `testDir` is `tests/e2e`, the supplement is a unit file no browser run loads, and the implementation revision is unchanged; unit-level evidence was re-captured after the supplement. During the B11-collateral investigation several instrumented diagnostic runs were made in disposable worktrees (two early attempts self-failed by referencing the undefined test-realm `window`; the corrected instrumentation identified the mock-timer cascade) — all discarded, none published as evidence |
| `git diff --check`; `git diff --cached --check`; `git status --short` | hygiene @ 300a9c2 and at the docs commit | 0 | clean | `10-diff-check.log` | no whitespace errors; the in-commit log cannot embed the final docs-commit check (same disclosed self-reference constraint as AUDIO-001-C2); the final-HEAD rerun is reported in the handoff/worklog |

No background processes were left running (each Playwright invocation starts and stops its own server; the port was verified free before the first run). No broad rerun was launched while another suite was running; the single full-suite rerun followed the TS-LOOP-005 isolation protocol and is documented above.

## 6. Regression and integration

- A1 / B2 / ordinary Verbs / Guided Verbs: ordinary Verbs — `verbs.spec.js` 5/5 on both projects in the affected runs and in the final full suite; the new 8-test audio spec passes on both projects. Guided Verbs — `verb-guided-challenge.spec.js` fully green in the final full suite (one first-capture flake isolated and passing; SC-INFRA-01 family); no Guided code path is touched by the diff (guided listen/pronounce uses `speak()`, unchanged; scheduler, grading, persistence untouched).
- Phrases / Conversation / navigation / favorites / SRS: `phrases-conversations.spec.js` 11 pass + 1 skip (chromium) and 12/12 effectively on Mobile Chrome, identical to baseline; favorites/SRS/activity/streak/cefr-cards/cefr-card-reference/dashboard specs all green in the final full suite; `SpeechQueue`'s public API is unchanged for the app.js consumers.
- Legacy storage / refresh / level and account isolation: not applicable — no storage, schema, ID, or persistence file changed; storage/SRS unit suites pass unchanged inside the 137/137 run.
- Actual audio adapter text AND language: asserted end-to-end — the E2E suite records every utterance's `text` and `lang` at the real `speechSynthesis.speak` boundary and asserts the exact planned sequence with `de-DE` for German steps and `en-US` for English steps through the real verbs page (AC-03/AC-11/AC-12).
- Browser/module startup and console errors: `verbs.html` boots through its existing `<script type="module">` chain with the new planner import (all E2E tests wait for the real controller's dataset readiness); browser console output is preserved inside the captured E2E logs; no new console errors were introduced (the only `console.warn` lines in evidence are the queue's intentional genuine-error/watchdog warnings fired by the unit tests' synthetic error injection).
- Desktop/mobile/themes/keyboard/reduced-motion/screenshots: desktop + Mobile Chrome (emulation) both run and pass the new audio tests; themes/keyboard/reduced-motion/screenshots not applicable — no UI, CSS, or HTML change (the control surface is byte-identical); the existing keyboard-focus specs remain green.
- For each inapplicable item a reason is given above. Untested/blocked: real-device and hardware-voice behavior (Mobile Chrome is emulation; deterministic speech doubles cannot establish real voice availability — TS-DONE-006), and real network speech services.

## 7. Test-quality and fault-probe evidence

| Probe | Risk/contract | Production target + exact patch | Detecting test | Baseline / syntax | Actual failure | Classification | Integrity |
|---|---|---|---|---|---|---|---|
| 1. language-mislabel | LF-AUDIO / AC-03 / AC-11 | `verbs-engine.js` (worktree): `lang: step.language` → opposite language | A10 + A1..A11 | 23/23; parses | 11 tests fail — every language-bearing expectation | **KILLED** | sha256 `d3f2aecc…` restored identical |
| 2. stopped-delayed-speech-executes | AC-12 cancellation | `tts.js` (worktree): `stop()` drops the `_speakDelayTimer` clear AND the callback guard | B2 (also B3/B9/B10) | 23/23; parses | 4 tests fail — a stopped queue's delayed speech executes and the orphaned timer corrupts replacements | **KILLED** | sha256 `8ca70a9e…` restored identical |
| 3. cursor-advances-during-pause | AC-12 pause semantics | `tts.js` (worktree): `pause()` drops `_generation++` and `currentUtterance = null` | B3 | 23/23; parses | 1 test fails — late `onend` advances the paused cursor and fires completion | **KILLED** | sha256 restored identical |
| 4. repeat-adapter-bypass | AC-11 control wiring | `verbs-engine.js` (worktree): planner call hardcodes `repeatCount: 1` | A1, A2 | 23/23; parses | 2 tests fail — repeat blocks missing | **KILLED** | sha256 restored identical |
| 5. stale-callback-affects-replacement | AC-12 stale immunity | `tts.js` (worktree): `onend` drops both ownership guards | B10, B3 (B11 as documented collateral) | 23/23; parses | 3 tests fail — a stale `onend` advances a replacement/paused queue; B11's extra failure is cross-test mock-timer contamination after B10's abort (passes in isolation under the mutant) | **KILLED** | sha256 restored identical |

Probe selection/count: exactly the five mandated families (language, cancellation, cursor, repeats, stale callbacks), each at a distinct production site; risk was pre-classified high (shared queue semantics) so five probes apply (TS-MUT-001). Probe 2's first attempt (removing only the callback ownership guard) SURVIVED as an equivalent mutant — `stop()` still clears the timer, so the guard is defense-in-depth; per TS-MUT-005 this is recorded in `11-fault-probes.log` and the corrected behavioral mutation (both cancellation layers removed) is the reported KILLED probe. The mutants touch production code only, in a disposable worktree at `300a9c2`; tests, expectations, and runner config were never modified; the main tree's hashes match the probe log's before/after values. False-pass review: the new tests use hand-written expected arrays (independent oracles), event/state waits rather than sleeps, exact text+language assertions, and no conditional assertions, blanket skips, or try/catch hiding; the one weak spot found and fixed during RED was the harness itself (disclosed in §5), and the mapping review after GREEN added the B11 empty-list case (the only test-suite addition after the RED state — committed as `300a9c2` and disclosed with refreshed evidence). The RED→GREEN cycle modified no expectation after the RED state was accepted (the two pre-RED test-authoring fixes and the A9 dropdown-sync implementation fix are all disclosed above).

## 8. Dead-code and dependency inventory

| Candidate symbol/path | Searches + dynamic caller checks | Classification | Disposition | Regression proof |
|---|---|---|---|---|
| Hand-built autoplay loop in `playAllVerbsAudio` (removed by this change) | `rg` for the old emission patterns (`Verb (${r+1}`, `Example (DE)` push sites) → only the new segment-based label mapping remains; ordering now exists solely in the planner | removed by the current change (the package's purpose) | removed | A1–A12 + full suites |
| `planSpeechSequence` (was unwired per AUDIO-001 A001-F2) | `rg -n "planSpeechSequence"` → planner definition, its unit tests, and the new production import in `verbs-engine.js` | DYNAMICALLY_USED (production consumer delivered) | keep | 04, 05, 09 |
| `mapVerbToSpeechCard` / `mapSpeechStepsToQueueItems` (new) | `rg` → definition + call sites in `playAllVerbsAudio`; captured by the unit VM loader | DYNAMICALLY_USED | keep | 04 |
| queue-record `label` field | `rg` across `js/`, `tests/`, `verbs.html` for `.label`/`label:` consumers of autoplay items → zero readers (pre-existing, not created by this change) | DUPLICATE_BUT_LIVE (data shape) | preserved for exact record-shape fidelity; removal would be a silent contract change for any external consumer | A1/A2 deep-equal on full record shape |
| `SpeechQueue` public surface | `rg` → verbs-engine.js + app.js consumers (playAll/pause/resume/stop/speakSingle/isPlaying) | DYNAMICALLY_USED | unchanged API | 06, 07, 09 |

Search boundary: repository-wide text search excluding `node_modules`/`.git`, plus HTML inline handlers and `window.*` export patterns. No proven orphan was created or removed beyond the superseded loop; no dependency manifest, lockfile, runner config, or CI file changed; `js/core/speech-plan.mjs` is byte-identical to the accepted AUDIO-001-C1 revision (sha256 `1c2f26ab…`).

## 9. Findings, limitations, and handoff

| Finding ID | Severity/impact | Reproduction and evidence | Disposition | Owner decision needed |
|---|---|---|---|---|
| F-A002-1: words-audio:48 words-page highlight not cleared on stop | medium (evidence hygiene; inherited) | `06/07/09` logs: fails identically at base `4bd13a9` and at `e32352c` on both projects; same class documented and waived-pending-triage since SHARED-CARD-002/003 (`app.js` stopAudioQueue does not clear glossary row highlights; out of AUDIO-002's authorized write scope) | open (pre-existing; never counted as a pass) | owner waiver or a narrow fix assignment |
| F-A002-2: first full-suite capture run had three load flakes | low (test stability) | `09-full-e2e-first-run-flakes.log` + `09-flake-isolation-reruns.log`: R2 (chromium), GC-07 (mobile) — SC-INFRA-01 family; verbs.spec.js:33 (mobile) — the OLD spec lacks a dataset-readiness wait and races the 2 MB verbs JSON against `window.verbsEngine.queue`; the empty-queue early-return it hits is unchanged base code; all three pass in isolation and the final broad rerun is clean | not reproduced with proof (flakes); no source change made | optional: authorize a readiness wait in verbs.spec.js |
| F-A002-3: no-synthesis fallback timer was untracked at base | high (fixed defect) | RED `01-red-unit.log` B8/B10 (pause/stop fire completion early; a replaced queue's cursor advances) → GREEN `04` | fixed (root cause: no timer handle + no ownership token) | none |
| F-A002-4: out-of-range Start-At produced an empty queue and a stuck playing UI at base | medium (fixed defect) | RED `01-red-unit.log` A9/A12 → GREEN `04` | fixed (clamp + empty-plan guard; controls now agree with the queue) | none |
| F-A002-5: queue-record boundary refinement | low (documented behavior refinement) | A11 RED at base: empty-text steps were queued for the runtime to skip; with the adapter the planner skips them with `SKIPPED_EMPTY_TEXT` warnings (LF-AUDIO-consistent; no substitution) | documented MODIFICATION (end-user speech output unchanged: nothing was spoken for those steps before either) | none |
| F-A002-6: probe 2 first attempt was an equivalent mutant | low (test-quality documentation) | `11-fault-probes.log` attempt-history note | documented per TS-MUT-005; corrected behavioral mutation KILLED | none |
| F-A002-7: environment events | low (disclosed) | missing chromium build 1228 (installed once); one tool-stream timeout during evidence capture (script completed in background; no duplicate suites); probe script's first run hung on a dead stdin-waiting syntax call (fixed) | disclosed in §5/§1 | none |
| F-A002-8: node:test cross-test mock-timer contamination after a mid-test abort | low (test-infra observation; no product impact) | probe 5: B11 fails after B10's mid-test abort (pending mocked timers poison the next test's context) but passes in isolation; the direction is fail-more, never fail-less, so it cannot mask a defect | documented in §7/`11-fault-probes.log`; no source change | optional: a future test-harness hardening could reset mock timers defensively |

- Remaining product risks: none introduced by this package within its scope; the planner is now wired to its first production consumer, so future planner changes must keep the verbs adapter's input boundary in mind. Environment restrictions: no real device, no hardware voice (emulation + deterministic speech doubles only — TS-DONE-006). Untested requirements: AUDIO-003 scope (level autoplay, `app.speakText` language forwarding) is deliberately untouched.
- Final diff/status vs base: `js/core/tts.js`, `js/core/verbs-engine.js` (implementation commit `e32352c`), `tests/unit/verbs-audio.test.mjs`, `tests/e2e/verbs-audio.spec.js` (tests commits `d5e5949` + supplement `300a9c2`), `docs/cefr/reports/AUDIO-002-01.md`, `docs/cefr/evidence/AUDIO-002/01/**` (docs commit). No unintended ID, content, storage, dependency, contract, or snapshot changes; `git status` clean at every commit; both `git diff --check` forms exit 0.
- GitHub delivery branch and report/evidence paths: branch `codex/glm-audio-002-01`; report `docs/cefr/reports/AUDIO-002-01.md`; evidence `docs/cefr/evidence/AUDIO-002/01/` (15 sanitized logs + `INDEX.md`). The initial unchanged checkpoint was pushed at `4bd13a9` before any edit; the final push and remote-SHA verification are returned in the handoff message.
- Next proposed WP: AUDIO-003 (level autoplay and end-to-end language correctness) — NOT started; owner assigns.

## 10. Owner/reviewer disposition — not executor approval

- Reviewer and reviewed exact revision: owner review of `codex/glm-audio-002-01` at tested code `e32352c` (tests `d5e5949`, docs commit after this report).
- Verdict: pending.
- Criteria/findings accepted or declined; explicit waivers and reason: awaiting owner review. Suggested acceptance checks: (a) confirm the 25-item map in §4 and the five KILLED probes in §7 satisfy the mandated matrix; (b) confirm the three disclosed behavior refinements (clamp + dropdown sync, empty-plan guard, planner-consistent skipping) match the intended "controls and queue agree" semantics; (c) confirm F-A002-1/F-A002-2 remain under the existing words-audio/SC-INFRA-01 triage decisions; (d) inspect `git diff 4bd13a9..HEAD --stat` (only the six authorized path families).
